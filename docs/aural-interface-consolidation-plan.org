#+title: Aural Interface Consolidation — Delivery Contract
#+date: 2026-09-10
#+startup: content

* Status and working agreement

The maintainer authorized implementation and commits in slices after the
[[file:aural-interface-review.org][interface review]]. [[file:adr/0018-unified-voice-palette-model.org][ADR 0018]] settles the root, names, inheritance,
missing voices and retirement boundary. Implementation and focused integration
review are complete; spoken acceptance is the next slice. The XHI review changed
no live personal settings.

The earlier palette wording and selected-column navigation fixes are committed
as =430b075e9=. The existing uncommitted Home announcement change is separate
work and must remain preserved unless the maintainer includes it in this task.

Use High for the substantive implementation slices below. Complete each slice,
run the checks its behavior needs, and commit the exact relevant files. Do not
repeat broad investigations or test suites after success without a new change or
unresolved concern. Update this plan with the commit and concise evidence so the
next slice can proceed from the recorded contract.

Stop at the stated reasoning-level boundaries because the maintainer requested
them. This is a level handoff, not a new request for permission to do already
authorized work. Return to XHI earlier only for an unresolved cross-component
design problem; give the concrete question and evidence. Max is not planned.

* Acceptance cases fixed before implementation

| Case | Required result |
| =a → b → c → acss-default= | Nearest whole entry wins; the row names its defining palette. |
| Edit inherited bolden in =a= | Create a complete entry and correctly owned local choices in =a=; ancestors remain unchanged. |
| Reset that entry | The inherited row remains visible and can be edited again. |
| Copy =a= independently | Copy effective definitions and ordered choices; keep per-choice tuning; new parent is the standard root. |
| New Voice in an empty personal palette | Open one complete editable draft; no definition-only rejection after collecting values. |
| =bolden= and =voice-bolden= requests | Same named entry and choices; terminal personality decoding does not recurse. |
| Registered nonstandard personality | Exact declared identity remains usable and visible; no arbitrary prefix guessing. |
| Generated =voice-from-acss= handle | Issued identity resolves to copied raw ACSS independently of the palette; maths and sampler speech retain their style. |
| Generated preset followed by a style adjustment | Replace the previous base, then apply the adjustment; retain raw-record partial composition and nil/zero distinctions. |
| Palette entry or stable personality named =acss= or =acss-*= | Reject the declaration before mutation, even before such a handle is issued; an unissued lookalike remains unknown. |
| Generated handle after adapter cache reset | Recreate from retained raw ACSS and use existing registration for both lanes; do not parse the name. |
| Persisted generated-handle reference | Report it before saving/importing/conversion; reusable saved voices carry complete definitions or stable references. |
| Unknown named voice | Authoring rejects it; unexpected runtime use preserves content and supplies a diagnostic. |
| Ordinary unstyled speech, explicit ACSS and inaudible | Preserve established semantics independently of named-voice lookup. |
| Current =bart= conversion | Keep identity, stored representations, ordered local choices, tuning and profile references; enumerate added standard inheritance. |
| Save an inactive palette | Save without selecting is available; Save and Apply reports session selection separately from startup selection. |
| Select a palette for startup | Change the intended profile field; preserve unrelated saved profile fields and current divergences. |
| Export/import | Portable export declares omitted local choices; local copies retain complete choices; replacement requires explicit selection. |
| Navigation in a non-first column | Read the new selected cell; full-row speech remains a separate action. |
| Delayed or failed application | Saved state stays distinguishable from playback/application; old callbacks cannot clear newer edits. |

* Commit slices

** DONE 0. Palette wording and navigation — prior work

=430b075e9=. Thirteen palette tests, fresh compiled navigation verification,
byte-code preflight and documentation checks passed before this commit. The
later review also passed 400 selected Aural tests with these changes present.

** DONE 1. Review and design contract — XHI

Record the review, ADR 0018 and this delivery plan. Inspect current data only as
needed to constrain preservation. The reviewed =bart= has schema 2, 29 direct
entries, 15 personality references, no reserved alias-name collision, and no
direct =animate= entry. This inventory is evidence, not permission to overwrite
the user's files without checking their latest state.

Design validation: local document links, =git diff --check= and
=make docs-release-check=. Reports and ADRs require no application rebuild.

** DONE 2. Establish current standard data and shared lookup — High

Foundation =d717313ad=: resource lookup, owned data resolution and palette source
inspection now use the same whole-entry inheritance reader. Independent tests
cover the four-level chain, nearest source, replacement of all choice metadata,
raw nil/zero preservation, isolation, missing parents and cycles. The 143
resource/data/palette tests and two fresh compiled inheritance tests pass;
=make bytecode= and =make docs-release-check= pass. This preparatory commit
does not switch the standard root or change personal files. The existing older
data reader remains temporarily until the root and personal-data steps below.

Current-file inspection also found seven local-choice references in =bart= and
17 retained snapshots. The four saved shared bindings all name direct entries
already owned by =bart=, so none supplies its current choices. Recheck fingerprints
before conversion; this observation does not authorize silently capturing session
bindings.

Current-root sub-slice =75204aa98=: =acss-default= and the supported definition constructor
now produce schema 3 with Automatic choices. Registered personalities extend
the root through a post-definition hook; declared aliases retain canonical
identities. Terminal personality commands retain adapter behavior. Source tests
cover actual transport, registration, late declarations and complete draft saves.
Validation passed: 469 affected source tests, nine focused draft lifecycle tests,
ten fresh compiled checks including both speech lanes, the full byte-code rebuild
followed by compilation of the final lifecycle correction, byte-code preflight,
=make docs-reference= and =make docs-release-check=.

New Voice had to move forward from slice 3 because the old definition-only dialog
cannot save into the current root. It now opens a neutral complete draft without
writing. Saving from the root creates a personal child with just the edited/new
entry. Existing independent-copy and older route retirement work remain below.

Explicit transitional boundary: schema-1 palettes may read definition-only
standard entries while the old experiment implementation and fixtures remain.
Schema-2 owned palettes still load unchanged. Remove this reader projection and
the experiment's explicit schema-1 constructor in slice 4; do not expand them.
Unknown-name enforcement is completed below; remaining ownership-format readers
and obsolete authoring routes are still removed in slice 4.

The one-time current-file conversion moved forward from slice 4 so the strict
activation checks can accept the maintainer's palette. Both original fingerprints
were unchanged, and both running sessions again reported no temporary per-name
routes or dirty voice drafts. The existing save coordinator preserved all 29
direct definitions, ordered selectors, languages and profile references. It
retained 17 snapshots and added seven schema-3 snapshots. Only =animate= is newly
inherited. Both saved files reopened exactly as proposed. Private exact originals
and the proposal are retained under =~/.emacsvox/backups/standard-root-20260910-AV7LCf/=.
Live sessions were not reloaded; use a fresh restart for the completed interface.

The bounded XHI handoff requested in =1fca47d6f= is resolved by ADR 0018's generated
concrete ACSS contract. An isolated fresh-process probe confirmed that the current
compiler loses raw metadata for these symbols and accepts a colliding palette
entry that changes their pitch in compilation and registration. Maths and sampler
callers need their concrete style identity preserved. Current personal data has
no reserved generated-name references. No runtime enforcement or personal data
changed during this design handoff.

The maintainer switched back to High for the implementation below. The tests use
real style API issuance, including unrelated cached names, collision ordering,
neutral and zero styles, mutation isolation, static family projection, palette
changes, adapter reset, maths/sampler callers and context composition. The
maintainer's Home edit remains separate and untouched.

Design verification: selected-Emacs and byte-code preflights, the isolated
collision probe, =git diff --check= and =make docs-release-check= passed.
This documentation-only decision requires no application rebuild.

Generated-style implementation =7676b4ebc=: the style API now owns copied raw snapshots,
distinguishes issued handles from adapter cache entries, rejects ambiguous
generated names and reconstructs missing adapter definitions. Palette declarations,
New/Rename and stable personality declarations reserve the generated namespace;
persisted personality fields reject generated handles. Compilation retains the
concrete preset's metadata, and Omnivox registration retains issued handles across
cache resets without shared per-name choices. Maths, the actual sampler, context
composition, mutation isolation and both speech lanes have regression coverage.

Validation: 579 affected source tests and 14 fresh compiled tests passed. The full
byte-code rebuild was followed by incremental compilation of the final compiler
and registration changes; final byte-code preflight and reference generation
passed. That commit changed no personal files or live sessions.

Name enforcement now shares a classifier across compilation and validation.
Bound variables alone cannot supply named voices. Unexpected names preserve
content with ordinary speech in both lanes, omit invalid wire identities, and
retain up to twenty quiet diagnostics shown by Doctor and explanations. Missing
terminal implementations also retain their identity in diagnostics; preview
refuses to claim that either unresolved request was heard. Nested presets and
speech actions receive the same reference checks.

The rule editor checks its target palette before saving. Stored rules must have
a durable provider in the saved collection; selecting a palette or applying a
profile checks the selected palette against known enabled rule references before
publication. Renames stage their saved rule changes before proposal validation.
Runtime fixtures now inherit the read-only standard root, and persistence fixtures
exclude that built-in record from personal data.

Validation: 751 affected source tests, four focused unknown/terminal checks, and
13 fresh compiled checks passed. Final byte-code preflight passed. The remaining
complete operations, parent authoring constraints and old-route retirement are
the next High slices.

Create schema-3 standard entries without changing their raw style/personality
representations. Make constructors produce current complete data. Give aliases,
root inventory, inherited metadata and terminal personalities one resolution
contract used by browser, editor, runtime registration and explanation.

Preserve supported definition APIs by projecting definition-only arguments into
the current complete model with Automatic choices, rather than retaining a
second editable ownership model. Check fresh loading and compiled dependencies.
Do not delete low-level personality or adapter support just because its name
contains “legacy”.

Before the root switch, account for current saved data and every maintained
constructor. A bounded transitional reader may keep intermediate commits usable;
record it explicitly and remove it in slice 4. Prepare the exact personal-data
conversion against file fingerprints, with retained originals and before/after
definition/choice comparisons. Do not silently convert on load or first edit.

Tests: independent schema fixtures, multi-level inheritance, aliases and collisions,
late personality registration, unknown names, raw styles, and owned local data.
Include the paths that actually deliver named and unstyled speech; testing only
the palette row builder is insufficient.

** DONE 3. Complete palette and voice operations — High

The manager's Copy Palette now uses the complete copy and two-store save services.
It copies effective entries and their ordered tuning, allocates independent local
owners, uses the standard root for current palettes, and leaves selection alone.
Parent completion excludes unrooted chains, missing parents, cycles, the current
palette and its descendants. The obsolete test that required current palette copy
to fail is replaced by manager-key persistence, isolation and write-failure tests.
The common individual-adjustment editor also uses a valid local row setter.

Validation: 130 affected source tests and three fresh compiled operation tests
passed; incremental byte-code compilation, its final preflight and documentation
release checks passed. Remaining slice-3 work is parent-change impact,
common editor entry points and explicit Reset/Delete behavior.

The remaining operations are now implemented. Both Edit entry points use the
complete editor; copying a built-in voice opens an unsaved personal-child draft.
Reset is a separate =R= action in both managers and requires a real parent entry;
Delete accepts only direct custom voices without an inherited replacement. Clean
obsolete editor contexts are removed when their source is reset. Composite rule
and face references are checked and renamed, while terminal personality
implementations retain their independent identity.

Changing parent validates known references and reports changed effective voices
in the palette and descendants before the normal confirmation. It rejects dirty
affected drafts, freezes sources and files through the save service, and discards
clean obsolete contexts after successful publication. The end-to-end editor test
edits, saves, resets, retains the inherited row and opens it for editing again.

Validation: 388 affected source tests passed, including the complete operations,
save recovery, preview callbacks and speech transport fixtures. One additional
manager-to-editor interaction check and eight fresh compiled checks passed. The
full rebuild was followed by an incremental pass for the final declaration;
its byte-code preflight passed. Command references were regenerated and the
documentation release check passed.

New Palette creates an empty current palette with an explicit parent. Editing
the built-in root prepares a personal child draft; browsing and previewing write
nothing. New Voice, Edit, Copy Voice and Copy Palette use the common complete
data services. Parent changes exclude invalid targets and report affected entries.

Copy Palette makes an independent effective copy. Do not add another “linked
copy” operation: New Palette with a selected parent already expresses sharing.
New custom voices start with an explicit neutral definition and Automatic choices,
not with an implicit reference to a missing or guessed personality.

Offer Reset where the parent provides the voice, and Delete only for direct custom
voices with no inherited replacement. Reference checks cover loaded declarations,
rules and descendants and state the limit for arbitrary Lisp references. Retain
transactional rename/copy/delete, draft guards and immutable local snapshots.

Tests: actual manager keys through draft, save, reopen, reset and subsequent edit;
copy isolation including duplicate physical choices and individual adjustments;
failed writes and conflicts; standard root remains intact.

** DONE 4. Retire the old voice-management routes — High

Screen retirement =aabde8de9=: the standalone tuner and old physical-experiment screen have been removed,
including their startup/build inventories. Shared adjustment helpers remain for
the complete editor and contextual controls. The Workbench's Assign, shared
route editing, migration and preset actions are removed. Its named rows read
complete palette entries and canonical names; engine preference, fallback,
disablement, preview, recovery and policy save tools remain available.

Old screen and route-authoring tests were retired with their product behavior.
The common editor now performs the saved-style/preview/final-wire comparison.
Current fixtures supply owned choices, and action tests check that named editing
and retained engine controls are reachable. All 719 affected source tests pass.
The isolated compiled parity harness, complete byte-code rebuild, final
preflight and fresh build-tree check passed. Command references were regenerated.
Reader and routing retirement =d62d83c84=: the remaining readers and per-name state are now retired. Palette files accept
only complete schema-3 entries, reserve declared aliases, and require an explicit
parent except for the read-only standard root. Runtime lookup, editing, copying
and save preparation no longer branch on ownership format or convert on first
edit. Workstation profiles contain global engine policy only. Historical local
snapshots remain preserved but cannot supply a current entry's choices.

Maintained fixtures now contain current records. Retired conversion, shared
binding and preset tests were replaced by rejection and current ownership
contracts. Actual save/retry, both-lane registration, preview, generated ACSS and
standalone adapter checks remain. All 724 affected source tests and 335 isolated
compiled contracts pass; the complete rebuild, byte-code preflight and fresh
build-tree startup check pass. Public command references were regenerated.

The inspected routing proposal removed four redundant per-name bindings. Both
file fingerprints still matched, and neither live session had temporary routes
or dirty drafts. All 24 local snapshots, global policy and the palette file were
preserved. The strict reader reopened exactly the proposed result. Private
originals and the proposal are in
=~/.emacsvox/backups/routing-retirement-20260910-Lm2Ugz/=. No live restart, reload
or selection change was performed. The earlier standard-root backup is retained.

** DONE 5. Finish source, persistence and sharing tools — High

The voice list now names the actual Source. =O= opens that source without
activation, =e= edits here, and =R= resets to the parent. =u= shows loaded rule,
face, scheme, feature and palette uses, declared aliases, and descendant
palettes. Saving an entry shared by descendants reviews the affected palettes
and known uses first; cancellation retains the draft.

=U= in the palette manager saves Use at Startup. For the selected saved
Presentation Profile, only its palette field changes. Other saved fields,
current palette selection and current divergences remain intact. External file
or profile changes cause a conflict instead of an overwrite. With no selected
profile, the command reviews a named setup captured from current selections
before saving it. There is no separate startup-palette preference.

=w= exports an independent effective portable palette after declaring omitted
local choices. =W= writes a full local backup containing required ancestry and
referenced snapshots. =i= imports either format under an explicitly chosen,
unused name as an independent palette; local choices receive new ownership and
snapshot IDs. It reports choices absent from current inventory and retains them.
The existing frozen save coordinator handles both stores and retry; a pending
import offers Retry or Discard. Existing IDs are never silently replaced, and
imports do not select or apply the result. Full backup import intentionally
materializes its effective voices; it does not replace the saved ancestry tree.

Validation passed: 750 affected source tests plus the focused Open Source user
journey, and the isolated compiled parity checks. Coverage includes saved/current/
startup separation, whole-file preservation on startup updates, external and
post-confirmation conflicts, portable omission, local ownership and tuning,
missing ancestry/snapshots, atomic export failure and frozen partial-import retry.
The complete rebuild and byte-code preflight pass. A fresh normal build-tree
startup passes, and all 12 new tool journeys pass against that compiled tree.
Public references were regenerated; the documentation release preflight passes.
The original Home announcement edits remain separate.

** DONE 6. Simplify tables and finish common navigation — Medium

Use selected-column vertical speech consistently in tables. Put voice or engine
names first. Move aliases, repeated adapters, native IDs and detailed capability
fields into Details. Keep uncertainty, availability and actual playback visible
where they affect a choice. Preserve selection, preview cancellation and return
positions. No data-format or routing changes belong in this slice.

Clarify output-setting labels, including “use main output” versus disabling
notifications. Group existing workstation controls coherently. A new ordinary
physical-voice policy is not to be inferred by reusing a final-fallback setting;
if a new runtime policy is needed, record that bounded follow-up separately.

Tests: arrows, actions/help agreement, nested return and draft retention across
all changed managers. Refresh an already open profile table after Use at Startup
so its displayed saved palette follows the updated registry. Update maintained
manual text and generated artifacts under
the normal documentation workflow once wording settles.

Completed in =5940ff522= plus the accompanying manual update. Selected-column
movement now takes precedence over name/state summaries outside the first
column. Compact Workbench tables lead with names, retaining choice order,
availability, health and playback evidence. Details retains technical fields
and captures the actual Workbench state before opening Help. Palette tables no
longer repeat the adapter. Use at Startup refreshes an open Profiles view.
Notification Customize says Use main output (notifications remain audible).

The maintained user and developer guides now describe the common editor,
whole-entry inheritance, aliases, source/uses, Reset versus Delete, startup and
sharing. Existing output controls remain together in Home. The developer
backlog records a bounded ordinary preferred physical-voice follow-up; no new
runtime policy was inferred from final fallback.

Validation: 767 source-preferred tests passed, isolated compiled parity passed,
and 55 UI/tool tests passed in a fresh process asserting compiled application
functions. =make bytecode=, =make build-aural-test=, Org preview,
=make docs-reference=, =make docs-update= and =make docs-release-check= passed.
The original Home announcement edits remain separate. Live spoken acceptance
is still pending. Stop here for the XHI focused integration review.

** DONE 7. Focused integration review — XHI

Review the changed code against ADR 0018 and the acceptance table, concentrating
on name resolution, root/default behavior, whole-entry ownership, reset/deletion,
startup selection and asynchronous publication. Check that obsolete entry points
are gone and useful workstation controls survived. Do not repeat the broad
original investigation unless new evidence warrants it.

The [[file:aural-interface-integration-review.org][focused review]] records six integration gaps corrected in =4b4ec550b=:
palette mutations preserve other saved settings; Reset names and rechecks its
actual source; active deletion validates the baseline and protects drafts;
startup validates saved rules and selected options. Nine focused regressions
cover these behaviors, with six verified to fail against the preceding code.
The broader 776 tests, compiled parity and fresh compiled startup passed.
Public wording and manuals were updated through the normal documentation gates.
No remaining blocker was found in the inspected contract paths. Audible behavior
and the ordinary preferred physical-voice follow-up remain explicitly separate.
Stop here and switch to Medium for the spoken walkthrough.

** TODO 8. Spoken acceptance and corrections — Medium or High

The maintainer walks through creating, copying, editing, resetting, selecting,
restarting and recovering. Include unavailable engines, icons disabled, rapid
navigation during previews and both speech streams. Use Medium for wording and
navigation corrections; High for substantive state or runtime defects. Commit
each correction with appropriate checks.

* Deferred additions

Bulk replacement across many named voices, a complete sound-pack authoring UI,
new tone management and a redesigned ordinary-speech policy are not prerequisites
for removing the contradictory voice-management model. Keep them as identified
follow-ups instead of adding speculative tools during these slices. Existing
sound, spatial, semantic-rule and notification functionality stays available.

* Required checks and stopping point

Follow =AGENTS.md= for the selected Emacs, byte-code rebuilds after interface
changes, fresh-process verification, public reference generation and documentation
release checks. Run relevant focused tests per slice, then the broader affected
voice/transport and documentation gates before the final review. Never infer
audible equivalence solely from valid data or successful registration.

After committing slice 1, ask the maintainer to switch to High before slice 2.
The contract is complete enough for implementation; no further broad review or
additional model level is required at that boundary.
