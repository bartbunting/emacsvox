#+title: Per-Fallback Tuning: Client Dispatch Handoff Review
#+date: 2026-09-09
#+startup: content

* Status and next reasoning level

The reviewed lifecycle, registration, codec and structured runtime slices are
implemented at High. All 490 selected source tests and the isolated compiled
suite pass. Muted real-server acceptance passes on both lanes and after reconnect.
The subsequent XHI preparation review is complete. Twenty diagnostic observations
cover cancellation before owner allocation, during the policy stop, in nested
hooks and legacy dispatch, plus pending quits during bookkeeping. The selected
refinement uses cancellable preparation scopes, explicit own-stop preservation
and unwind-safe reservations. Implementation resumed at High. Generic scopes,
Stop ownership, generic reservation cleanup and shared choice metadata cleanup
are implemented. The subsequent ordinary-consumer audit adds eight connected
contracts; all 490 selected source tests pass. It exposes whole-transaction
legacy fallback when raw named speech is mixed with prepared speech, and the
separate isolated-letter protocol. The maintainer returned to XHI; the
[[file:voice-editor-legacy-queue-review.org][legacy queue review]] adds sixteen observations, including lost dispatch
intent after late raw commands. The maintainer selected A: upgrade complete
captured submissions and retain explicit cross-call compatibility limits. Isolated
characters supply no named palette voice and retain their existing selection.
The [[file:voice-editor-queue-handoff-contract.org][selected queue contract]] completes the XHI handoff. The maintainer resumed
at High. Dispatch intent/owner binding is implemented, with fifteen new contracts
and all 505 selected source tests passing. The
[[file:voice-editor-queue-proof-review.org][completed XHI protocol-boundary refinement]] now specifies separate queue/framing
state, conservative local/remote recovery, birth observation and exact own-stop
receipts. The High proof implementation now passes 526 selected source tests,
including connected producer/Stop-hook and remote lifecycle checks. Muted
client/server acceptance passes on both lanes, locally and remotely, including
reconnect. Guarded named promotion is now implemented, with 536 selected source
tests and muted named playback on both lanes locally/remotely after reconnect.
Scope A remains selected. The [[file:voice-editor-preview-handoff-review.org][XHI private-preview handoff]] is complete,
with twelve reproducible observations and forty independent desired cases.
The maintainer resumed at High. The common preview coordinator and startup
framing guard are implemented, with sixteen new contracts, 582 selected tests
and muted local/remote preview acceptance through reconnect. Strict v2
projection/readback and optional editor controls remain pending. No new
product decision is required.
The server bundle is complete at Omnivox =31a0b9a=. Emacsvox =8064c7443= adds
lossless value conversion and =ccf193879= adds per-lane registration with frozen
acknowledged definitions and explicit older-server limitations. Acknowledged structured submissions now use layered transport and correlated
marker-3 readback. Complete named queues are promoted under scope A; cross-call
and unsupported shapes retain their reviewed compatibility limits. Private
preview v2 and optional editor controls are pending. No live runtime or personal
configuration was changed.

The accepted [[file:adr/0017-per-fallback-voice-tuning.org][ADR 0017]], [[file:voice-editor-fallback-wire-contract.org][wire contract]] and
[[file:voice-editor-implementation-plan.org][implementation plan]] remain authoritative. This review concerns client
ownership during submission and receipt handling; it does not reopen the
shared -> actual choice -> context precedence or the server wire grammar.

* Concrete finding

=tts--prepare-structured-dispatch= in [[file:../lisp/tts-speak.el][tts-speak.el]] allocates an ID and
returns a registration effect. Its documented contract installs tracking only
after the complete timeline write. [[file:../lisp/emacsvox-aural-transport.el][Aural transport]] retains that effect until
=emacsvox-aural--commit-delivery-effects= runs after sending the commands.

=tts--dispatch-playback-marker-event= requires an installed marker dispatch.
A deterministic isolated probe used a real pipe process and the normal dispatch
preparation function, then delivered an event from a substituted
=process-send-string= before invoking the returned registration effect:

#+begin_example
early-event-delivered=nil; callback-count=0; dispatch-installed-after-write=t
#+end_example

The probe sent no speech and changed no live process. It demonstrates the
reentrant-write ordering gap; it is not a claim that this race was reproduced
with a live speech engine. Existing control/apply tests already model replies
arriving during submission, so the new receipt path must cover that ordering.

There is a second ownership distinction: current Omnivox route diagnostics are
recorded before dispatch callback ownership is checked. Ordinary structured
speech can also have no marker callback. Merely adding a new recognized event
name would therefore neither preserve early receipts nor establish sufficient
ownership for new per-choice evidence.

A receipt belongs to the frozen registration used by its submitted timeline,
not whatever registry is current when the receipt arrives. Reconnect, later
registration, cancellation, failed writes and repeated physical selectors must
not erase that distinction. Playback observation and successful client submission
are different facts: an observed consumed source cannot be declared unheard
merely because a later write or callback failed.

* Audited paths and reproducible findings

All accepted Emacsvox ADRs 0001--0017 were read together. The reviewed source
baseline is Emacsvox =b0ca10afe= and the development server is =31a0b9a=.
Unrelated Agent Shell edits and the untracked quality audit remain untouched.

| Path | Finding and required consequence |
|------+----------------------------------|
| =tts--prepare-structured-dispatch= | Allocates an ID before framing, but installs callbacks and timing after sending. Preparation must remain inactive until validation finishes. |
| =emacsvox-aural--submit-delivery-entries= | Navigation/urgent policy can stop the lane after preparation. Arm the new owner after this intentional interruption, immediately before writing; otherwise the new dispatch cancels itself. |
| =emacsvox-aural--send-delivery-entries= | Adjacent commands for one process, including all multipart fragments, are concatenated into one Lisp write. One Lisp call is not an atomic operating-system write. |
| =emacsvox-aural--commit-delivery-effects= | Still runs inside the dynamic delivery transaction. Callback-generated speech here can join the already-sent transaction and never be flushed. |
| =tts--complete-tracked-dispatch= | An early terminal finds no tracking; the later effect creates an owner for speech that has already finished. Terminal receipt must close observation without waiting for callback publication. |
| Stop, sentinel and retirement | They remove existing tracking before callbacks. A delayed registration effect can currently recreate it. Publication must never allocate a replacement owner or reopen a retired one. |
| =emacsvox--tracked-reading-next= | Stores the returned ID after =tts-speak-tracked=. Synchronous early completion cannot match the session's ID. Post-write callback invocation is still too early. |
| Omnivox marker/filter handling | Route hooks currently precede callback ownership checks. New evidence needs validation before either route mutation or external notification. |

The selected Emacs's =process-send-string= documentation explicitly permits
process output between partial writes. The local GNU Emacs source at
=/home/bart/src/emacs/src/process.c= also documents =send_process='s reentrant
wait and ordered =write_queue=. It queues subsequent writes behind the existing
remainder. A second client byte-stream scheduler is unnecessary; a stop cannot
retract bytes already handed to Emacs. This source inspection is not a claim
of measured live speech latency or cross-platform audible acceptance.

Run [[file:../test/diagnose-client-handoff.el][the standalone diagnostic]] in a fresh batch process using the Emacs selected
by =local.mk=, for example on this workstation:

#+begin_src sh
/home/bart/opt/emacs-31/bin/emacs -Q --batch -l test/diagnose-client-handoff.el
#+end_src

It uses real, isolated pipe-process objects and substitutes the write function;
it starts no engine, sends no speech and writes no configuration. Observations
at the reviewed baseline are:

| Probe | Observed result |
|-------+-----------------|
| Marker during structured write | ID returned; no callback or source-onset observation. |
| Ordinary speech without callbacks | ID returned; early source-onset observation lost. |
| Terminal during write | ID returned; no completion callback; lifecycle remains installed. |
| Local stop during write | Stop issued; later effect installs lifecycle again. |
| Process exit during write | Later effect installs lifecycle for the dead process. |
| Simulated consumed source followed by write error | No ID or lifecycle; existing route diagnostics nevertheless record the actual source. |
| Delivery effect starts a nested transaction | Nested command is captured; only the original transaction is written. |
| Tracked-reader completion before ID return | Reader stores ID 99 but remains at position 1 after completion. |

These are fault-injection observations, not live reproductions or passing
acceptance tests for the future fix. Keep the diagnostic observational rather
than asserting that these defects must persist.

* Selected internal ownership contract

** Prepare, arm, publish and retire

Use one private dispatch owner for structured speech, with or without callbacks.
It owns process identity and process generation, dispatch/submission IDs, frozen
receipt context, sequence state, bounded evidence and callback delivery state.
The existing tracked/marker tables may remain indexes; they must not become
independent authorities for publication or retirement. Existing public speech
signatures and terminal status symbols remain unchanged.

Track three independent facts: the write outcome, whether more server events
may be accepted, and whether client callbacks may run. One combined
pending/sent/done flag cannot represent an early terminal followed by a failed
write. The required transitions are:

| Boundary | Observation ownership | Public result and callbacks |
|----------+-----------------------+-----------------------------|
| Prepared | Allocate an inactive record and ID; validate the entire framed payload and reserve bounded retention capacity. | No sent/history claim and no callbacks. Validation failure releases everything without a write. |
| Armed | After policy interruption, recheck the exact live process, cancellation epoch and frozen registration; install the same record immediately before the first write. | Callback publication remains held. Early valid events may update evidence. |
| Complete write | Mark the existing record sent. Commit the existing successful-delivery effects; never recreate removed tracking. | Eligible successful submissions retain their ID return. Schedule callback draining outside the submission stack. |
| Early server terminal | Record terminal status once and close incoming-event ownership immediately; retain only the bounded callback/evidence ticket. | After a successful uncancelled write, publish the ID and deliver preceding events followed by this terminal asynchronously. No new live observer is installed. |
| Write error or quit | Close ownership and discard unpublished callback work in unwind cleanup. Keep already validated consumed-source evidence as historical evidence. | Preserve failure/no-ID behavior; no successful delivery effects and no retry. Lack of evidence means unknown, not proof that nothing played. |
| Local cancellation or retirement before publication | Invalidate the record and any held terminal ticket. Later success effects cannot revive it. | No ID is published for the invalidated submission; tracked wrappers retain their existing not-submitted error path. |
| Local cancellation after publication | Close incoming events and invalidate queued marker notifications before calling external code. | Process stop/failure delivers at most one cancelled/failed terminal. Explicit =tts-cancel-tracked-dispatch= remains silent. |

Server terminal status and local cancellation are separate. A terminal received
before return can describe a successfully submitted request; a locally invalidated
owner cannot be resurrected because its write later returns. A cancellation
epoch is process-local, advances before stop/retirement callbacks, and includes
held terminal tickets as well as live observers. Ordinary server-side keyed
replacement relies on correlated terminal records, not a process-wide epoch.

Ensure cleanup covers nonlocal exits from framing, sending and post-send
effects. If an effect fails after the complete write, retain that write outcome
and evidence in diagnostics, suppress unpublished callbacks and propagate the
error; do not reinterpret it as permission to resend. History and configuration
apply remain their existing owners. Lifecycle cleanup cannot depend on the last
effect in a list being reached.

Give =tts--protocol-dispatch-tracked= and =tts--protocol-dispatch-marked= the
same pre-write ownership discipline, both inside a captured transaction and
for direct callers. Do not leave those entry points with the same early-terminal
gap. Captured preparation creates inactive tokens; actually sending the batch
arms its tokens. No observer exists for unsent, discarded idle work. Version-4
native replacement must be recognized before enabling its transport.

** Deliver callbacks after the submission stack unwinds

Recording source consumption is immediate, internal and data-only. Server marker
callbacks, server terminal callbacks and playback route/timeline hooks run from one
bounded notification drain per process. In particular, recording a route must
not synchronously speak through a hook while its parent write is active.

Schedule an ordinary next-turn timer, not an input-idle timer. Its drain must
defer again while a submission, delivery transaction, process-input drain or
notification drain is active. A zero-delay timer alone is insufficient because
Emacs can run timers during a blocked write. Add private dynamic depth guards at
the common submission and direct dispatch boundaries; do not merely bind the
old transaction flag to nil while the actual stack is still active.

When safe, deliver events in observed per-process order, preserving each
dispatch's sequence and placing its terminal last. Remove or mark each item
consumed before calling it. A callback that submits new speech owns a fresh
transaction and source context. A callback that waits for process output can
enqueue further events, but cannot recursively drain them ahead of the callback
already executing. Catch ordinary callback errors as today; preserve consumed
items and reschedule remaining work on quit.

Local lifecycle control retains its existing ordering: remove owners first,
notify already-published cancellation callbacks, run stopped observers, then
delete a deliberately retired process. Do not put =tts-stopped-hook= into a
delayed dispatch queue: it could otherwise cancel a newer reading session on
the same process. Unpublished dispatch callbacks remain suppressed. When a
local cancellation callback or lifecycle hook runs within a submission, use a
private independent callback context for any speech it initiates: fresh capture
lists, history and source-policy bindings. Preserve the ambient speech-process
handle and derive the fresh lane from it; pass the event owner as callback data.
Do not temporarily bind that handle to the event emitter: doing so would lose
a replacement process installed by a recovery hook when the binding unwinds. Keep the outer depth guard active so server events
still cannot drain there. Resetting only the delivery flag is insufficient.
Preserve synchronous local retirement tests alongside the new deferred server
callback tests; this is not a wholesale asynchronous rewrite of TTS hooks.

Local cancel/stop/reconnect invalidates queued notifications through owner
identity and epoch checks before each invocation. A server-terminal ticket can
drain after its observation record closes, but cannot bypass a later local
cancellation. A stop after terminal receipt may suppress that undelivered
completion; preserve the server terminal in diagnostics and report the local
cancellation to a still-published callback at most once. This prevents an old
completion starting the next reading chunk after a user stop.

The two line filters must consume a line/remove its saved fragment before
dispatching external control callbacks. Guard reentrant input drains so later
output cannot overtake the unprocessed remainder of the current chunk or
restore an old fragment. Preserve control correlation and the existing remote
filter/sentinel chain. This is a small filter-ordering change, not a replacement
protocol or an unbounded queue for unknown dispatches.

** Freeze registration and span identity

At projection, capture the lane's acknowledged immutable version-2 registration
and use that exact snapshot for both timeline construction and receipt checking.
Capture the raw owner/provenance sidecar in the same registration operation as
the normalized definitions: palette, canonical named voice and aliases, ordered
choice IDs/selectors/patches, and raw shared values needed for explanation.
Current registration retains normalized wire content only; that alone cannot
later reconstruct an old palette's edit provenance. Sidecars stay client-local
and must never become extra fields in the strict server request.

Include changes of owner/provenance in the client registration signature, even
when two palettes happen to project identical wire values. Freeze strings,
vectors and nested values, not just cons cells, and give callbacks copies of
public values. Do not rebuild sidecars from current palettes on acknowledgement.
Reuse an immutable snapshot across its dispatches with explicit retention
accounting. Terminal cleanup releases it when no active or draining owner needs it.

Each dispatch also retains a compact span-ID index: legacy/layered mode, logical
ID, raw and wire context, and placement. It need not retain sample text, complete
render plans or duplicate chains per span. Capture enough source provenance for
an explanation to distinguish shared, choice, context and adapter default.
Reject a changed/missing acknowledgement before writing; do not guess a choice
or replay a server rejection if a newer registration overtakes admission.

An acknowledgement received after admission affects future work. Receipts for
an active older dispatch remain valid against its frozen snapshot, even after
re-registration or palette edits. A replacement process never owns the older
snapshot. Identical logical/choice IDs on the other lane do not establish
identity. Never recover a choice ID by comparing physical engine and voice.

** Accept actual choice evidence and publish truthful readback

For version 3 markers, first validate the exact new grammar, duplicate keys,
types, u64 identities and encoded/decoded bounds in the wire contract. Then
check active process/dispatch ownership and increasing sequence. No route or
callback state changes on a rejected event. Continue to support legacy marker
versions under their existing grammar; do not demand a choice receipt for a
legacy chunk in a mixed timeline.

Keep one pending start identity per dispatch, including sequence, utterance,
logical voice and realized physical voice, without retaining its sample text
unless the marker callback needs it. A =voice_choice_applied= event must directly
follow that dispatch's start in sequence, agree with its identity, reference a
layered span with that logical ID, and match the dispatch's frozen registry
generation. The selected ID/reason/index must identify the original row;
policy fallback requires a null choice ID even when the physical voice happens
to match a saved row. Validate selectors where the request constrains an exact
target; do not turn an engine-default/property selector into a guessed exact ID.

A start and its receipt may arrive in separate process-output chunks. Update
source-consumption timing on the validated start; publish the detailed choice
summary only on its validated receipt. A missing, invalid or nonadjacent receipt
means individual tuning is unverified. It does not erase the start or manufacture
a successful application. Preserve sequence-gap diagnostics; a gap breaks the
pending pair, and a later complete start/receipt pair can establish new evidence.

Keep bounded distinct per-dispatch observations and a separately retained latest
started identity. Update historical evidence before notifying observers. Include
lane, process generation, dispatch, registry, palette/voice/choice and span
identity in the internal record. Update degradation only for that exact
utterance; an older chunk or the other lane must not modify a newer route's
summary just because the logical name matches. Route hooks receive copies.

After cancellation/terminal/retirement, reject late new evidence. Previously
observed playback remains labelled historical; changing the active palette
must not present it as the current draft's result. Record write-start, write-end,
source-observed and terminal times separately. Early source evidence may precede
write-end; compute onset from write-start instead of producing negative latency
or claiming the write was already complete.

** Bound retention and handle overload explicitly

Use private named limits and test the boundaries. Initial implementation budgets
per process are 128 retained dispatch owners/tickets, 32 MiB of accounted frozen
metadata, and 8192 queued callback/hook records with a 16 MiB encoded-data budget.
The latter accommodates the server's existing bounded marker burst. Account
semantic values and retained strings as well as event envelopes; fixed record
counts and validated shapes also bound Lisp container overhead. Count shared
snapshots once and release their charge at the last owner.

Reserve terminal bookkeeping with each owner so notification overflow cannot
discard the only completion. Keep at most 32 distinct choice observations per
dispatch plus a separately reserved last-started summary and a truncation flag.
The ordinary no-callback path consumes markers without queueing their text.
Keep latest summaries bounded across logical voices and lanes; they must not
retain whole registries or dead process objects indefinitely.

Reject a new submission that cannot reserve ownership metadata before writing
or interrupting older speech; never evict an active callback owner silently.
Do not expire an otherwise healthy long utterance by a guessed playback timeout.
Notification overflow records an explicit tracking failure, suppresses further
marker callbacks for that owner and uses its reserved terminal slot. An
unpublished tracked submission then fails without an ID; an already-published
one receives one failed tracking result. Retain actual evidence independently;
tracking failure is not proof of synthesis failure. Overflow must not replay
speech or stop the other lane. Ordinary bounded observation truncation alone
does not fail speech.

* Implementation slices and acceptance at High

1. Implement dispatch ownership and deferred notification ordering using the
   current version-3 transport first. Keep wire negotiation unchanged. Cover the
   three dispatch entry points, whole-write success/failure, history effects,
   callback-free speech, cleanup, process filters and tracked-reader reentrancy.
   Commit only after source tests, rebuilt byte-code and fresh compiled tests.
2. Add the frozen registration sidecar, strict marker-3 codec/correlation and
   timeline-4 projection together before enabling ordinary layered transport.
   Retain sparse context and reset legacy effect transitions at layered
   boundaries. Test old/new capability combinations independently on both lanes.
3. Connect private preview v2 and original-row auditions to the existing preview
   operation ownership, preserving terminal correlation and bounded last-started
   evidence. Then add optional per-choice editor controls and experiment keeps.
4. Complete current documentation and the existing spoken acceptance slice.
   The installed server, published dependency minimum and personal settings are
   separate from these development commits.

The first two implementation slices must add regression tests for:

- start/receipt/terminal before the write returns, including a terminal split
  across filter calls and ordinary speech with no callback;
- incomplete multipart prefix followed by error or quit, and complete simulated
  consumption followed by write/effect failure, with zero replay;
- validation failure before a navigation stop, intentional stop before arming,
  reentrant stop after arming, cancel after terminal but before callback drain,
  repeated cancellation and dead/replaced process cleanup;
- callbacks that enqueue another transaction, cancel themselves, stop the lane,
  signal an error/quit, or wait for output while more events arrive;
- synchronous local retirement ordering and lifecycle-hook speech with fresh
  capture state, including a notification stop inside a foreground submission;
- tracked reading that actually advances after an early completion and cannot
  resume after cancellation; successful-send/history regressions remain valid;
- later acknowledgements and changed palettes, mutable string/vector inputs,
  duplicate physical rows, invalid row/reason/index, wrong span/registry/process,
  per-dispatch sequence gaps and older-utterance degradation;
- interleaved foreground/notification output, lane-local interruption, remote
  reconnect without replay, strict size limits and bounded overflow cleanup;
- one-part versus multipart version-4 parity, mixed legacy/layered spans, and
  callback/evidence behavior in a fresh compiled Emacs.

Do not present the eight diagnostic probes as satisfying this future matrix.
They justify the reviewed contract and provide reproducible starting cases.

* Alternatives and review outcome

| Alternative | Benefit | Cost and outcome |
|-------------+---------+------------------|
| Provisional owner with deferred, owned notifications (selected) | Validates early evidence once; preserves caller initialization and lifecycle ownership; supports callback-free readback. | Requires explicit write/publication/retirement states and bounded notification draining. |
| Register early and invoke callbacks immediately | Small code change and low callback latency. | Fails the tracked-reader and nested-transaction probes; callbacks can stop their parent write before it publishes. Rejected. |
| Register only after writing and buffer unowned input globally | Leaves the old registration effect largely intact. | Cannot safely associate unknown events with frozen metadata; adds stale-event, overflow and reconnect ambiguity. Rejected. |
| Retain the current ordering and add only marker-3 decoding | Smallest feature patch. | Loses early events, recreates completed/cancelled owners and permits unowned route changes. Rejected. |

The selected design preserves the accepted ownership, precedence, wire and
no-replay decisions. No public command, storage schema, server protocol or
dependency decision needs reopening. The lifecycle slice is implemented;
the implemented slices and new XHI preparation boundary are recorded below.

* Validation of this review

=make check-emacs= passes using Emacs 31.0.90. The standalone diagnostic runs
all eight cases against explicitly loaded source. A fresh batch run of
=emacsvox-tts-tests=, =emacsvox-speak-tests=,
=emacsvox-aural-transport-tests=, =omnivox-remote-tests= and
=omnivox-choice-registration-tests= passes all 429 tests after loading the
normal =emacsvox-aural-provider-workflows= vocabulary. The initial selective
runner omitted that provider and failed one mail-view vocabulary test; adding
the fixture dependency resolved it without changing application or test code.

This slice changes a design record, plans and a standalone diagnostic only.
No production Lisp or Omnivox runtime fix is claimed by these passing baseline
checks. Implementation must add the future behavior tests listed above.

* Lifecycle implementation at High

The new private dispatch owner is reserved during preparation and armed after
intentional interruption, immediately before the packet write. Early source and
terminal records are retained; successful delivery effects finish before callback
publication. Deferred notifications cannot drain inside submission, input or
notification stacks. Local stop/retirement keeps its synchronous ordering and
invalidates held tickets before external callbacks. History/effect errors, quit,
write failure and cancellation release ownership without replay.

Both filters consume the current line before dispatching it and preserve
reentrant input order. Ordinary callback-free speech now has an observation
owner. Existing marker-2 route hooks are deferred and late retired events cannot
relabel routes. Process-generation/epoch checks guard queued notifications;
sequence-gap diagnostics are retained. Frozen registry/span provenance and
marker-3 choice correlation are intentionally still pending.

The new =tts-dispatch-handoff-tests= exercise substituted writes on isolated
pipe processes, including the real tracked-reader consumer. These checks do not
establish audible acceptance or change the installed Omnivox runtime.

Validation: all 453 selected source tests pass, including 24 new handoff tests.
=make bytecode-rebuild=, the final incremental =make bytecode= and
=make compiled-aural-test= pass with Emacs 31.0.90. The compiled gate includes
the handoff tests and checks that the ownership/filter functions came from its
isolated compiled tree. =make docs-release-check= (including version and byte-code
preflight), =make headers-check= and =git diff --check= pass. Optional-package
compiler warnings remain; no live Emacs restart or server installation was done.

* Registration provenance implementation at High

Each registration now freezes client-only palette, canonical voice, aliases,
raw shared definition/style and ordered choice records at projection. Local,
portable and session origin remains explicit. The signature includes provenance,
so a different owner with equal wire settings advances the generation. Request
encoding removes this sidecar; acknowledgement uses the original operation's
snapshot, with process generation. Deep copying includes mutable strings,
vectors and hash keys/values. Metadata bounds are checked before any policy or
registry write. Shared per-dispatch retention and compact span context are next.

All 457 selected source tests and the fresh isolated compiled gate pass, including
four new registration tests. =make bytecode= and its preflight pass. This slice
does not enable timeline 4, marker 3 or individual editor controls.

* Explicit layered projection and receipt codec at High

The strict marker-3 decoder rejects duplicate/unknown/missing fields, malformed
identities/reasons, invalid enum arrays and oversized records. Null and booleans
remain distinct. Explicit snapshot projection builds layered spans with raw
context and placement; it never recovers context by subtracting the shared
value. It retains compact span/rule provenance, separates equal values with
different rule sources, ends legacy effect continuity at layered boundaries and
frames the same version-4 document in one or multiple commands.

All 474 selected source tests and the isolated compiled gate pass, including
11 new codec/projection contracts. Negotiation and normal submission still use
version 3. The next slice must connect ownership retention, start/receipt
correlation and bounded readback before enabling the new path.

* Structured runtime integration at High

Acknowledgement activates timeline 4 only on a process advertising the complete
bundle. Dispatch preparation attaches the exact immutable registration and a
compact span index. The shared registration is charged once per retained set of
owners and released after the final ticket. Preparation reserves bounded space
for 32 distinct observations and a separate latest-started record; byte and owner
limits reject before navigation interruption. Admission rechecks the frozen
acknowledgement. Equivalent same-generation acknowledgement refreshes reuse the
snapshot; replaced-process acknowledgements cannot establish a new owner.

Strict start/receipt correlation precedes sequence or route mutation. The
receipt must match process/dispatch ownership, the immediately preceding start,
utterance, registry, layered span, logical voice, realized physical voice and
original row/reason/index. Exact selectors also constrain the physical voice;
property selectors do not imply an exact voice ID. Policy fallback carries no
row patch even when it selects a stored physical voice. Older marker versions
cannot relabel a layered dispatch. Later registration changes do not rewrite
an already admitted dispatch's evidence.

Historical summaries retain palette, canonical voice, lane/process generation,
choice, raw layers and source provenance without retaining a process or whole
registry. Public queries and hooks receive deep copies. Summaries have count
and byte bounds; oversized rule provenance is explicitly truncated. Per-dispatch
observation truncation preserves the separate latest consumed source. Late
receipts after cancellation are discarded; validated consumed evidence survives
a subsequent write error without replay. Degradation from an older utterance or
the other lane cannot change the newer route.

All 490 selected source tests pass, including 16 new actual-playback contracts.
The full =make bytecode-rebuild= and isolated =make compiled-aural-test= pass.
The opt-in [[file:../test/verify-omnivox-choice-runtime.el][real-server acceptance runner]] starts separate eSpeak workers with
=--audio-output null= and uses the actual apply/registration, transport, filters
and callback path. Both source and fresh compiled runs select the explicit
fallback after an unavailable primary on both lanes. Replacing the foreground
process requires fresh registration; the notification lane remains usable.
The four dispatches complete with verified choice identity and zero retained
metadata. This is server-consumption evidence, not an acoustic listening test.

Example (use the Emacs selected by =local.mk=):

#+begin_src sh
EMACSVOX_CHOICE_TEST_SERVER=/path/to/staged/omnivox \
  /path/to/selected/emacs -Q --batch -l test/verify-omnivox-choice-runtime.el
#+end_src

Set =EMACSVOX_CHOICE_TEST_SOURCE=1 only for deliberate source acceptance.
An initial temporary runner omitted the inventory/policy preparation needed by
the ordinary registration service and timed out without submitting a registry.
Using the real acknowledged apply service resolved that fixture setup error.
No application workaround, live server replacement or personal save was used.

* Preparation review baseline: explicit Stop during the policy stop

The [[file:../test/diagnose-prepared-cancellation.el][prepared-cancellation diagnostic]] substitutes all writes and starts no engine.
For each of timeline versions 3 and 4 it compares a normal urgent submission
with a public =tts-stop t= invoked reentrantly from the policy stop's write.
The prepared owner already exists, but it is not in the armed event indexes.
Current observed output is:

#+begin_example
version=3 second-stop=nil returned-id=1 timeline-written=t observing=t
version=3 second-stop=t returned-id=1 timeline-written=t observing=t
version=4 second-stop=nil returned-id=1 timeline-written=t observing=t
version=4 second-stop=t returned-id=1 timeline-written=t observing=t
#+end_example

The normal case should still submit. The second explicit Stop should suppress
the prepared request. Currently cancellation visits armed indexes only; arming
later captures the advanced cancellation epoch and allows the request to write.
This is a deterministic ordering probe, not a report of audible reproduction.
It narrows an incompletely handled preparation boundary in the existing contract;
it is not a new marker-3 decoding defect or permission to replay.

The XHI follow-up below settles preparation cancellation without making a request
cancel itself through its own required policy interruption. Its scope includes all direct/captured
entry points, nested stopped-hook speech, both lanes, reentrant output, sentinel
retirement and nonlocal exits during reservation/metadata attachment. Keep the
existing normal policy-stop and synchronous local teardown contracts passing.
Implementation and regression tests remain the next High slice.

The starting alternatives were:

| Approach | Benefit | Cost / concern |
|----------+---------+----------------|
| Cancellable inactive owners with an explicit exemption for their own policy stop (preferred starting point) | Expresses which prepared request a stop may preserve; a nested public Stop has no implicit exemption. | Adds a preparation index and cleanup responsibility; ensure it is not an active playback observer. |
| Detect additional cancellation by counting epoch changes around policy interruption | Smaller apparent change to the owner indexes. | Current interruption advances the epoch at several stages; nested callbacks and cleanup make expected-count arithmetic fragile. |

The maintainer's intended Stop behavior needs no new product decision. This is
an ownership review stop under the agreed reasoning-level workflow. The feature
is not complete: ordinary-consumer coverage, private previews, optional editor
controls, current guidance and spoken acceptance remain after this fix.

* Completed XHI review: cancellable preparation and safe reservation

** Additional evidence at Emacsvox 23ece51d1

All accepted ADRs 0001--0017 were read together. The extended diagnostic uses
the selected Emacs 31.0.90, source-loaded implementations and isolated pipe
fixtures. No engine, audio device, live profile or personal save is involved.
The original four observations above remain reproducible. The added cases are:

| Injected boundary | Current observation | Required result |
|-------------------+---------------------+-----------------|
| Public Stop during projection, before owner allocation; versions 3 and 4 | ID 1 returned and timeline written in both cases. | Cancel the encompassing preparation before it can allocate or send a new dispatch. |
| Public Stop during framing; versions 3 and 4 | ID 1 returned and timeline written. | No timeline, publication or successful-delivery effects. |
| Public Stop from the policy stop's stopped hook | Parent ID 1 returned and written. | Cancel the parent preparation. |
| Ordered announcement from that hook | Parent ID 1 and nested ID 2 both written. | Preserve this independent, non-interrupting announcement and the parent. |
| New urgent announcement from that hook | Parent ID 1 and nested ID 2 both written. | Only nested ID 2 proceeds; its interruption supersedes the older parent. |
| Stop before the legacy dispatch command; tracked/marked, direct/captured | All four return ID 1 and write a dispatch. | All four suppress the cancelled request. |
| Stop inside final choice admission | ID 1 returned and timeline written. | Recheck cancellation after admission validation, before installing observers or writing. |
| Silent forget of a prepared dispatch ID | ID 1 returned and dispatch written. | Retire that owner silently, including before arming. |
| Pending quit after owner-count update | Count 1 remains, with no bytes or owner available to transaction cleanup. | No retained count, owner or metadata. |
| Pending quit after first snapshot reference insertion | Snapshot count 1 remains after the owner count returns to zero. | Release the last snapshot reference and its charge. |
| Pending quit after metadata charge | 273968 bytes and one snapshot remain with owner count zero. | Release both, without any write. |

The three quit cases set the actual =quit-flag= after a bookkeeping primitive.
They respect =inhibit-quit= and catch the eventual quit; they do not force a quit
through an otherwise protected critical section. The runner disables native
subroutine trampoline creation before substituting primitives, so fixture writes
cannot intercept an unrelated compiler subprocess. These are fault-injection
observations, not acoustic acceptance or assertions that bugs should persist.

** Start cancellation ownership before allocating a dispatch

Select a small private /preparation scope/ for each outer delivery transaction,
registered against its exact process and generation before calling its body,
history preparation or timeline projection. Direct legacy tracked/marked entry
points establish the same scope before reservation; captured calls join their
existing scope. Independent callbacks start a fresh scope. Never infer ownership
from whichever transaction happens to be dynamically visible during a Stop.

The scope records irrevocable cancellation and owns every unpublished dispatch
reservation created within it. Give active scopes a separate bound of 128 per
process; keep the existing 128-owner and 32 MiB metadata limits. Scope records
contain ownership references, not another copy of text or the registration.
Admission failure precedes the native policy stop. An empty or failed transaction
still unregisters its scope. Standalone private preparation callers must use the
same guarded scope service; they must not create an unowned reservation.

Index prepared owners for cancellation and silent ID-based forgetting without
installing marker, completion or timing observers. A prepared owner is never a
valid source of playback evidence. Preserve the existing armed indexes and
deferred terminal tickets. Scope lifetime ends on delivery handoff or unwind;
detach its owner references so a published owner cannot retain sibling metadata.
Published owners retain their existing completion/retirement lifetime.
Silent ID-based forgetting retires only the named owner. If an unsent atomic
packet still references that owner, discard that packet and clean up its other
unpublished reservations; do not cancel unrelated published dispatches.

Recheck scope cancellation, process identity/generation and all owner admission
conditions after any validation or hook that can reenter. Check the complete
packet's owners before the write, including multiple captured legacy dispatches
and multipart timelines. A cancelled scope cannot allocate another owner,
perform its later policy stop, install a pending delivery or commit success
effects. This also protects captured cue-only and untracked legacy transactions.
Idle delivery keeps its existing scheduler ownership: transfer only an uncancelled
transaction, and remove only that transaction's pending generation on failed
handoff. Tracked/structured owners must never be left waiting for idle delivery.

** Make own-policy preservation explicit and non-inheritable

Pass one private, one-use preservation token from the fully prepared transaction
to its required lane interruption. It names the exact scope and its still-inactive
owners. Validate it before interrupting; a cancelled, foreign, already-armed or
previously used scope cannot acquire protection. Apply the token explicitly to
that interruption's invalidation and unwind cleanup. Do not bind a global or
dynamic "ignore cancellation" flag, and never reset a cancelled scope to live.

Public Stops, input failure, sentinel handling and deliberate retirement have
no preservation token; ID-based forgetting also cannot be exempted. Invalidate affected scopes
before a stop write can accept output, and again at existing cancellation cleanup
boundaries before external callbacks. A nested public Stop therefore cancels the
parent even inside its own policy stop. A nested urgent submission protects only
its new scope and cancels the parent; an ordered callback that performs no stop
does not cancel the parent. Preserve synchronous local teardown ordering and the
independent callback bindings already implemented.

Scope invalidation is process-local. Explicit stop-all first marks existing
preparations for both distinct target processes before either stop write can
yield, then preserves the existing per-lane stop/callback order. A normal main
interruption must not invalidate notification preparations. No exception extends
to another lane, a replacement process, retirement, or a later public Stop.

The source audit found an important compatibility call site:
=tts--speak-transaction= currently calls public =tts-stop= for
=tts-stop-immediately= when source policy does not own interruption. Record that
automatic-stop intent on its preparation and perform it once at the prepared
delivery boundary through the private token path. Do not let adding scope
cancellation make ordinary =tts-speak= cancel itself. Keep the setting's existing
choice of whether older speech is interrupted, its lane scope and stop-before-new
output ordering; validate the new packet before that automatic stop. Explicit
public Stop always retains its cancelling meaning. Audit the legacy fallback
preview's stop-before-queue call separately when connecting preview ownership.

Retain process cancellation epochs for armed observers and callback tickets.
Their increment count is not the authority for preserving a preparation; no
"expected number of increments" calculation is required. Admission can capture
the current epoch only after the scope and exact owners pass the final check.
Cancellation before writing returns no dispatch ID and commits no success effects;
it is not a server-send failure to announce. Existing tracked wrappers retain
their not-submitted error contract. Once a packet is handed to Emacs, bytes may
already have reached the server: invalidate observation/publication, keep any
previously validated consumption as historical evidence, and never replay.

** Make reservation and release unwind-safe

Establish the scope's unwind cleanup before registering it or allocating owners.
Build copies, serialize for size and allocate proposed context before mutating
shared counters. Then recheck capacity and cancellation against current state.
Publish the owner-to-scope link, cancellation index and accounting in one short,
data-only critical section. A pending quit must leave either no reservation or
a fully cleanup-owned reservation, including direct calls before they receive
the newly allocated owner.

Use the same pattern for choice metadata: commit the immutable context/release
ownership, snapshot reference count, unique snapshot charge and per-owner charge
together. Cover both a first snapshot reference and sharing an existing one.
Record exactly which reservations were acquired so rollback neither subtracts an
unacquired charge nor releases another owner's reference. Repeated abandonment,
cancellation and scope cleanup must be harmless.

Limit =inhibit-quit= to these short bookkeeping sections and their matching
release. Never inhibit it across projection, serialization, process writes,
waiting, diagnostic I/O or external hooks. Keep an enclosing unwind guard active
when the binding ends, so a deferred quit releases committed reservations before
propagating. Ordinary errors and throws need that guard too; quit inhibition
alone is not rollback. Release bookkeeping must finish even if lifecycle logging
or another cleanup step fails, and one failed cleanup must not strand peer owners.
Do not mark release complete before its accounting and shared references are
settled. Preserve cancelled state immediately and retry only remaining cleanup
steps; never retry speech. Source headers and public speech signatures stay intact.

** Alternatives and implementation handoff

| Approach | Benefit | Cost / conclusion |
|----------+---------+-------------------|
| Preparation scope plus explicit own-stop token (selected) | Covers Stop before an owner exists, multiple captured owners, nested speech and exact cleanup ownership. | Adds bounded scope bookkeeping and requires the legacy automatic-stop call site to declare its intent. |
| Index only inactive dispatch owners | Smaller patch for the original second-Stop probe. | Still misses projection before owner allocation and ownerless captured output; insufficient after the expanded probes. |
| Compare expected epoch increments around the policy stop | Few additional objects. | Couples admission to nested stop/cleanup implementation details and does not fix unowned reservation windows; rejected. |
| Let all speech created inside a policy stop inherit protection | Minimal call-site plumbing. | A public Stop or newer urgent request cannot reliably supersede its parent; rejected. |

Return to High for two tested, committed slices:

1. Add guarded scopes, exact cancellation/preservation and atomic generic owner
   reservation/release. Cover all dispatch entry points, legacy automatic stop,
   both lanes, ownerless capture and pending handoff before proceeding.
2. Make shared choice metadata attachment/release atomic, integrate its final
   admission with the same scope, and rerun layered runtime acceptance.

Convert the 20 diagnostic observations into behavior tests with expected results;
keep the diagnostic observational. Extend the matrix with stop-all while the
other lane is prepared, process replacement/sentinel during policy stop, repeated
cancel, ID forgetting with multiple owners, interrupted scope registration,
error/quit/throw during reservation and release, last versus shared snapshot
release, framing failure before interruption, and Stop from final admission.
Check no callbacks or success history for suppressed work, zero released charges,
no revived indexes, and preserved independent newer speech. Keep existing early
receipt/terminal, after-write failure and tracked-reader regressions passing.

After Lisp changes, rebuild byte-code for changed structs/macros, run the source
matrix and isolated compiled gate, then the muted real-server both-lane/reconnect
runner. No server change, dependency upgrade, storage migration or product choice
is required. The remaining consumer audit, private previews and editor controls
follow these slices; the preparation fix is still pending at this design commit.

** Validation of the preparation review

The standalone diagnostic completes all 20 observations. A fresh Emacs explicitly
loading the reviewed TTS, transport, codec and Omnivox source passes all 461
selected TTS, transport, handoff and choice-playback baseline tests. The reduced
runner initially omitted =emacsvox-aural-provider-workflows=, leaving =mail-view=
unregistered in one notification test; loading that normal provider fixes the
runner without any application change. These baseline tests do not yet assert
the new cancellation and reservation outcomes.

=make check-emacs=, =make bytecode-check=, =make docs-release-check= and
=make headers-check= pass. Runtime Lisp is unchanged by this review, so no new
runtime build or acoustic acceptance is claimed. The implementation plan and
backlog mark the review complete and both implementation slices pending at High.

* Generic preparation implementation at High

Delivery transactions now register bounded scopes before their bodies, and
direct tracked/marked calls use the same guarded service. Prepared owners have
a cancellation index without playback observation. Automatic policy interruption
preserves only its exact ready scope, while public Stop, nested urgent speech,
process retirement and stop-all invalidate the affected scopes. Legacy automatic
stop intent is carried to the validated delivery boundary. Final admission
rechecks cancellation, and interrupted pending handoff preserves a newer pending
generation. Cancelled preparation returns no ID, writes no stale packet and
commits no success effects.

Generic reservation links the owner to unwind cleanup before charging counters.
Short bookkeeping sections defer pending quits and roll back ordinary nonlocal
exits. Release cleanup handles peers and repeated abandonment. Sixteen new ERT
contracts cover the diagnostic cancellation cases, both lanes, final admission,
ordinary legacy speech, pending handoff, full owner capacity and quit/error/throw
injection during scope/owner registration and release. All 477 selected source
tests pass. At this checkpoint the two shared-choice snapshot quit probes still
reproduced their known leaks, reserved for the following slice.

The isolated compiled gate and muted real-server both-lane/reconnect runner
pass. Full byte-code regeneration completed; a final indentation edit to the
transport required the ordinary incremental refresh before its current-bytecode
preflight. Documentation and header gates pass after that refresh.

* Shared choice reservation implementation at High

Choice context copies and sizing precede mutation. Attachment rechecks ownership
and current reservations, then commits context/release responsibility, snapshot
references and all byte charges together. Short critical sections defer pending
quits, and error/throw rollback restores the exact prior reservation, including
when another dispatch already owns the snapshot. Reattachment is rejected without
altering the first context. Shared snapshots reuse their already measured size.

Release similarly restores an interrupted reference/charge mutation and finishes
remaining cleanup before propagating the original exit. Generic accounting and
indexes settle even when a published owner's first cleanup attempt fails. Cleanup
never resubmits speech. Five additional contracts cover first/shared attachment,
last/shared release, both snapshot and generic byte-charge failures, cancellation
during copying and duplicate attachment. All 482 selected source tests pass.
All 20 diagnostic observations now show the intended cancellation/control results
and zero leaked reservations in the three pending-quit cases.
Current byte-code, the isolated compiled gate, fresh compiled real-server
both-lane/reconnect acceptance, documentation and header checks all pass.
