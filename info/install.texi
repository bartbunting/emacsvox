@c $Id$
@node Installation
@chapter Installation Instructions

This chapter describes a source-checkout installation.  Emacsvox requires
Emacs 31 or newer; use the same selected Emacs for configuration, compilation,
tests, and startup.

@menu
* Obtaining Emacsvox::         Obtaining Emacsvox.
* Quick Installation::         Configure, build, and start Emacsvox.
* Building And Testing The Speech Server::
@end menu

@node Obtaining Emacsvox
@section Obtaining Emacsvox

The maintained Emacsvox repository is
@url{https://github.com/bartbunting/emacsvox}.

@example
git clone https://github.com/bartbunting/emacsvox
cd emacsvox
@end example

@node Quick Installation
@section Quick Installation

@enumerate
@item
Select Emacs 31 or newer.  If it is not the system @command{emacs}, copy
@file{local.mk.example} to the ignored @file{local.mk} and set @code{EMACS}:

@example
EMACS=/path/to/emacs-31/bin/emacs
@end example

Verify the selection before diagnosing a compiler failure:

@example
make check-emacs
@end example

@item
Configure and compile the checkout:

@example
make all
@end example

Do not compile Emacsvox with an older system Emacs.  Tests prefer source and
therefore do not prove that the byte-code used by a normal startup is current.

@item
Install or build a speech server.  The common choices are:

@itemize @bullet
@item
@strong{Omnivox on Windows/WSL.}  Keep the Omnivox repository beside Emacsvox
at @file{../omnivox}, or set @code{OMNIVOX_DIR}, then run
@command{make windows-omnivox}.  This stages a content-addressed Windows
runtime beneath @file{servers/omnivox-bin}.  Select server name
@code{omnivox}.  See @file{Readme.org} and
@file{servers/omnivox-release/README.org} for the engine, helper, licensing,
and reproducibility details.

@item
@strong{eSpeak on GNU/Linux.}  Install the system eSpeak development packages
and run @command{make espeak}.  Select server name @code{espeak}.

@item
@strong{SwiftMac on macOS.}  Install Swift 5.9 or newer and run
@command{make swiftmac}.  Select server name @code{swiftmac}.  See
@file{servers/mac-swiftmac/Readme.org}.

@item
@strong{Eloquence/Outloud.}  A separately licensed compatible IBM TTS or
Eloquence runtime is required; it is not included with Emacsvox.  Run
@command{make outloud} for the GNU/Linux server.  Voxin information is at
@url{https://voxin.oralux.net}.  Cerence and Vocalizer voices are not supported
by the Outloud adapter.

@item
@strong{Software DECtalk.}  Install the engine separately and run
@command{make dtk}.  See @file{servers/software-dtk/Readme.org}.
@end itemize

Native Windows audio, standalone Eloquence, and standalone DECtalk bridges are
also available through @command{make windows-audio},
@command{make windows-outloud}, and @command{make windows-dtk}.  Their READMEs
under @file{servers/} document the WSL-specific setup.  The optional Piper
notification path is experimental and is not a primary speech-server adapter.

@item
Select the server before loading Emacsvox.  In a shell startup file, set
@env{TTS_PROGRAM} to the server name, for example:

@example
export TTS_PROGRAM=espeak
@end example

The equivalent Emacs form, evaluated before setup, is:

@lisp
(setenv "TTS_PROGRAM" "espeak")
@end lisp

The retired DECtalk-named generic environment selector is not recognized; see
@file{etc/tts-modernization.org} for the migration table.  Names for actual
DECtalk servers, including @code{dtk-exp} and @code{dtk-soft}, remain valid
server choices.

@item
For a changing source checkout, load the setup source explicitly:

@lisp
(load-file "/path/to/emacsvox/lisp/emacsvox-setup.el")
@end lisp

The setup entry point can then warn and prefer source when newer Lisp would
otherwise be hidden by stale byte-code.  A stable installed copy with current
byte-code may instead use:

@lisp
(load-library "emacsvox-setup")
@end lisp
@end enumerate

After an ordinary Lisp edit, run @command{make bytecode}.  After switching
branches, pulling, or changing Lisp macros or public interfaces, run
@command{make bytecode-rebuild}.  The non-mutating
@command{make bytecode-check} verifies that byte-code exists, is current, and
was produced by the selected Emacs.

@node Building And Testing The Speech Server
@section Building And Testing The Speech Server

Speech-server launchers live in @file{servers/}.  Build them through the
repository-root targets above so the selected paths and staging rules remain
consistent.

The standalone Tcl-compatible servers implement a legacy line protocol.  To
smoke-test one outside Emacs, run its launcher from the repository root, for
example:

@example
./servers/espeak
@end example

Then enter:

@example
q @{This is a test.@}
d
s
@end example

The @code{q} command queues text, @code{d} dispatches it, and @code{s} stops
speech.  Type @kbd{C-d} to close the server.  Backend-specific READMEs may
provide stronger diagnostics and should take precedence over this generic
smoke test.

From Emacs, use @code{M-x tts-speak-version} to verify the selected adapter and
@code{M-x tts-restart} after changing server configuration.  Omnivox also
records per-process diagnostic logs below
@file{$XDG_STATE_HOME/emacsvox/omnivox}, defaulting to
@file{~/.local/state/emacsvox/omnivox}; spoken text is omitted unless explicit
text logging is enabled.
