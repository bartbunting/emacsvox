# -*- mode: snippet -*-
# name: ems-after
# key: ea
# --
(defun ems--${1:fun}-after (&rest _)
  "Provide speech feedback after `${1:fun}'."
  (when (ems-interactive-p)
    $0))

(advice-add '${1:fun} :after #'ems--${1:fun}-after)
