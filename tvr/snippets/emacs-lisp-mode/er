# -*- mode: snippet -*-
# name: ems-around
# key: er
# --
(defun ems--${1:fun}-around (orig-fun &rest args)
  "Provide speech feedback around `${1:fun}'."
  (when (ems-interactive-p)
    $0)
  (apply orig-fun args))

(advice-add '${1:fun} :around #'ems--${1:fun}-around)
