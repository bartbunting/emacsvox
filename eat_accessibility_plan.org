#+title: Emacsvox EAT Accessibility Implementation Plan
#+date: 2026-08-23
#+options: toc:2 num:nil

* Objective

Resolve the EAT correctness, lifecycle, accessibility, performance, privacy, and
speech-interference issues that can be addressed inside Emacsvox without
requiring major EAT changes.

The evidence baseline and design rationale are in
[[file:eat_review_report.org][eat_review_report.org]].  Work is delivered as
small commits.  Source, tests, documentation, and compiled-code verification are
kept distinct so each behavioral change remains reviewable.

* Constraints

- Preserve all pre-existing tracked and untracked work.
- Do not modify the user's Emacs configuration or existing live terminal
  sessions.
- Use the Emacs selected by ~local.mk~.
- Run ~make check-emacs~ and ~make bytecode-check~ before compiler diagnosis.
- Run ~make bytecode~ after ordinary Lisp changes and verify in a fresh Emacs.
- Use ~make bytecode-rebuild~ after macro/public-interface changes, branch
  switches, or pulls.
- Do not introduce dependencies or rely on unstable EAT internals without an
  explicit decision.
- Pause for a user decision before a broad refactor, public API change, or
  materially different user-facing default.

* Baseline

| Item | Value |
|------+-------|
| Emacsvox starting commit | ~0a19bac66dfc5e0b519f0157a28d271878ea2304~ |
| Emacs | GNU Emacs 31.0.90, ~1bd56ff93273~ |
| EAT | NonGNU ELPA 0.9.4, ~c91451f2d174~ |
| Focused EAT tests | 6 passed |
| Full unit suite | 2354 expected passes, 1 skip, 1 unrelated agent-shell failure |
| Byte-code | Current at start |

Pre-existing untracked files at start:

#+begin_example
audit.org
aural_report.org
code_review_1_report.org
code_review_prompt.org
notes.org
notes.org~
#+end_example

* Work breakdown

** DONE Phase 0: Audit and tracked implementation plan

Deliverables:

- complete evidence-based audit;
- phased implementation plan;
- baseline and preservation record.

Acceptance:

- Org lint reports no issues;
- no tracked source/configuration changes;
- only the two scoped documentation files are committed.

** DONE Phase 1: Correctness and lifecycle foundation

Issues:

- F-01 nil cursor on empty/reset/alternate display;
- pending completion state surviving reset/exit/reload;
- process exec/exit generation identity.

Planned slices:

1. [X] nil-safe update fallback with real terminal-model tests;
2. [X] buffer-local generation/reset helpers;
3. [X] process lifecycle observation and stale timer/state cancellation.

Acceptance:

- empty terminal, reset, and empty alternate display never signal;
- existing reset announcement completes;
- exit/restart cannot reuse completion or screen state;
- source tests, byte-code build, and fresh compiled verification pass.

** DONE Phase 2: Public-API screen observer and pure diff

Issues:

- F-02 lost multiline output;
- resize/output/reset indistinguishable;
- no previous cursor/screen/style state;
- bounded-performance requirement.

Planned slices:

1. [X] immutable bounded visible-screen snapshot from public EAT getters;
2. [X] pure logical-row/text/style diff;
3. [X] update generations, resize/alternate-state comparison, and quiescence;
4. [X] deterministic unit and real-terminal ERT.

Acceptance:

- observer does not read ~eat--t-*~ structs or private wrap/prompt state;
- work is bounded by visible terminal size;
- arbitrary output chunk splits converge on the same quiesced diff;
- unchanged repaint and resize-only events are classified without content
  speech.

** DONE Phase 3: Semantic foreground output

Issues:

- F-02 multiline output loss;
- F-03 arbitrary/flooding speech;
- mixed interruption and transaction semantics.

Planned slices:

1. [X] native EAT aural facts and delivery roles;
2. [X] bounded complete-line output;
3. [X] prompt-redraw and unchanged-repaint suppression;
4. [X] progress/status coalescing and rate limiting;
5. [X] selected/follow-live policy.

Acceptance:

- shell/REPL results are spoken before a redrawn prompt;
- rapid repaint cannot flood or repeatedly stop unrelated speech;
- resize-only/unchanged repaint stays quiet;
- selected scrollback review is not interrupted by live output;
- punctuation and voice information survive presentation.

** TODO Phase 4: Terminal-side completion transaction

Issues:

- F-04 ambiguous candidates silent;
- F-05 first-update snapshot loss;
- F-06 token/line parsing failures.

Planned slices:

1. [X] pre-Tab public screen snapshot and generation/deadline;
2. [X] unique completion from logical screen change;
3. [X] quiesced candidate/help-row extraction;
4. [X] count, truncation, repeated-Tab deduplication, and retained review;
5. Bash/readline PTY and router-shaped fixtures.

Acceptance:

- ~~/sr<Tab>~ and ~git pul<Tab>~ remain concise;
- ~git pu<Tab><Tab>~ announces and retains ~pull~ and ~push~;
- split chunks, bell, resize, wrapping, spaces, quoting, Unicode, punctuation,
  and same-length changes are covered;
- no assumption of Bash or structured remote completion is required.

** TODO Phase 5: Lifecycle and interaction semantics

Issues:

- F-07 prompt status;
- F-09 process/alternate-screen lifecycle;
- F-12 bell, mode labels, paste, title/CWD, input editing, secure input;
- optional background monitoring.

Planned slices:

1. process/app enter/exit and alternate-screen announcements;
2. human mode labels and paste/yank state;
3. bell deduplication;
4. title/CWD comparison and verbosity policy;
5. safe password command feedback and state clearing;
6. optional per-buffer output monitoring.

Acceptance:

- semantic state changes are distinct from terminal content;
- no password characters or retained secure snapshots reach speech/logs;
- background content is silent by default and monitoring is rate-limited;
- no EAT callback is replaced without preserving its behavior and testing it.

** TODO Phase 6: Highlight, focus, and manual review

Issues:

- F-08 style semantics;
- HT-01 active item/highlight inference;
- missing manual access when inference is uncertain.

Planned slices:

1. normalized rendered style signatures;
2. conservative cursor/style/input/timing focus score;
3. current row, changed region, likely highlight, candidate, and frozen-screen
   review commands;
4. terminal verbosity/output-monitor commands;
5. less/Vim/whiptail/status fixtures.

Acceptance:

- style-only selection can be spoken;
- static inverse headers are not assumed to be focus;
- confidence is conservative and manual review always remains available;
- full-screen refresh is bounded and usable.

** TODO Phase 7: EAT-backed Eshell

Issue:

- F-10 separate ~eat-eshell-update-hook~ is currently unwired.

Acceptance:

- shared observer handles EAT visual regions in Eshell;
- generic Emacsvox Eshell speech is not duplicated;
- visual command enter/exit, resize, background, and process exit are tested.

** TODO Phase 8: Consolidated validation and documentation

Deliverables:

- pure unit, real EAT terminal, PTY Bash/readline, deterministic TUI, and
  representative real-app tests;
- fresh compiled Emacs verification;
- user documentation for modes, completion ownership, review commands,
  verbosity, monitoring, shell integration, and password safety;
- final audit-to-implementation traceability table.

Acceptance:

- all scoped tests pass;
- full suite has no new failures;
- ~make bytecode-check~ reports current;
- every finding is fixed, explicitly mitigated, or documented as requiring an
  upstream EAT API/major change.

* Deferred to upstream EAT

These do not block conservative Emacsvox implementations:

- structured update causes and dirty text/style ranges;
- public input-mode getter/hooks;
- additive bell/title/CWD/alternate-screen events;
- semantic prompt/OSC 133 API;
- OSC 8 hyperlink support;
- remote TERM/terminfo fixes;
- synchronized-output and terminal parser/performance fixes.

* Decision log

| Date | Decision | Reason |
|------+----------+--------|
| 2026-08-23 | Use current public EAT snapshots and small optional adapters; do not make private terminal structs architectural. | Upstream mainline is quiet and the public getters cover a conservative implementation. |
| 2026-08-23 | Preserve silent-by-default background behavior. | It is the safer privacy and speech-interference default. |
| 2026-08-23 | Separate the nil-cursor fix from output/completion redesign. | Lowest-risk confirmed correctness fix with an unambiguous regression oracle. |
| 2026-08-23 | Prefer deterministic local router/TUI fixtures over external sessions. | Reproducible, safe, and independent of device/network state. |

* Progress log

| Date | Commit | Phase | Result / validation |
|------+--------+-------+---------------------|
| 2026-08-23 | ~d1f52159f2~ | 0 | Audit and project plan committed; both Org files lint clean. |
| 2026-08-23 | ~a62ff846d~ | 1.1 | Nil-safe empty/reset/alternate updates; 9 focused EAT tests pass; byte-code rebuilt/current; fresh Emacs loaded ~emacsvox-eat.elc~ and passed empty/reset verification. Full 2359-test suite has only the pre-existing agent-shell failure. |
| 2026-08-23 | ~c5ad4f3a6~ | 1.2 | Buffer-local generations, process identity, and reset/reload/exec/exit invalidation; 12 focused tests and compiled real-reload verification pass. |
| 2026-08-23 | ~18d284a83~ | 1.3 | Native selected-buffer process exit/restart lifecycle; duplicate/stale exits and background disclosure suppressed; 15 focused tests and a fresh compiled real-process exit pass. |
| 2026-08-23 | ~13f5836c4~ | 2.1 | Data-only visible-screen snapshot from public EAT accessors and normalized rendered style properties; no speech-policy change. |
| 2026-08-23 | ~093a1e9bc~ | 2.2 | Linear bounded text/row/style diff with independent cursor, resize, alternate-screen, title, CWD, and generation classification; 23 focused tests and fresh compiled verification pass. |
| 2026-08-23 | ~a85bd7095~ | 2.3 | Buffer-local snapshots and serial-checked 60 ms quiescence transactions; lifecycle/focus loss cancels delivery, and arbitrary text/escape chunking converges; 26 focused tests and the real compiled timer pass. |
| 2026-08-23 | ~6b9af956b~ | 2.4 | Real EAT unchanged repaint, resize-only, reflow, reset, and alternate-screen observer coverage; 29 focused tests pass. Full suite: 2377 expected passes, 1 skip, and only the pre-existing agent-shell failure. |
| 2026-08-23 | ~e13ccc283~ | 3.1 | Bounded, control-sanitized terminal rows use one native ~command-output~ / ~command-output-received~ transaction; no observer policy change; 31 focused tests pass and byte-code is current. |
| 2026-08-23 | ~cadc1adc7~ | 3.2 | Quiesced inserted main-screen rows before the cursor are spoken once; cursor prompt is excluded, input-correlated legacy feedback remains, arbitrary async character speech is removed, and raw Tab/Return events are handled correctly. 37 focused tests and a compiled Bash PTY pass. |
| 2026-08-23 | ~783c0ac9c~ | 3.3 | Linear suffix/prefix row alignment avoids replaying visible history after scroll; cursor prompt, unchanged repaint, conservative full replacement, resize/reflow, and alternate-screen repaint stay quiet. 39 focused tests, 2,000 randomized overlap comparisons, and compiled verification pass. |
| 2026-08-23 | ~d0f5f7afc~ | 3.4 | Conservative same-row progress/status recognition uses stable replaceable transactions, retains the latest value, rate-limits intermediate updates to 1.5 seconds, presents terminal states immediately, suppresses input/resize/alternate-screen changes, and forces classification after at most 250 ms of continuous repaint. 43 focused tests, including a real EAT carriage-return rewrite, pass. |
| 2026-08-23 | ~c4eee0947~ | 3.5 | Automatic foreground output now requires the selected EAT window to follow the public terminal cursor.  Selected scrollback review stays silent while retaining the latest diff, screen, and status; another live-follow window cannot override the selected review window. 46 focused tests pass. |
| 2026-08-23 | ~d10d0653b~ | 4.1 | Terminal Tab now captures a bounded public-screen transaction with process generation, transaction serial, start/deadline, and a generation-safe two-second expiry timer. Compatible intermediate updates no longer consume it; non-Tab input, background selection, lifecycle changes, and success cancel it. The private semi-character-mode check is gone, so character-mode Tab follows the same path. 50 focused tests pass. |
| 2026-08-23 | ~63fe396fb~ | 4.2 | Inline terminal completion is classified once at screen quiescence from the pre/post public screens. Physical line numbers and shell-token parsing are removed; public cursor offsets plus full-width visual-row inference handle wrapping, while conservative displayed-field labeling covers paths, escaped spaces, Unicode, punctuation, quotes, separators, and same-length replacement. Inserted pre-input rows, shorter changes, alternate screens, and generation changes fail closed. 53 focused tests pass, including real EAT wrap and semantic transaction cases. |
| 2026-08-23 | ~03e660df5~ | 4.3 | Quiesced candidate/help output is anchored by suffix/prefix alignment of the old screen through its input row and the rows before the redrawn input. Prompt/history rows are stripped, columns and descriptions remain exact, scrolled-away anchors are marked uncertain, partial chunks wait silently for redraw, and the singular semantic completion transaction is retained for review instead of duplicated as command output. 59 focused tests pass, including real split EAT output. |
| 2026-08-23 | this commit | 4.4 | Token-like cells separated by stable visual whitespace receive candidate counts; descriptive/ambiguous layouts keep completion-row counts. Automatic speech is capped at eight inferred items or eight rows plus 1,000 characters, while full visible rows remain retained. Unanchored overflow reports an honest lower bound, normalized item signatures deduplicate reflowed repeated lists, and identical repeats speak only their count. 63 focused tests pass, including a real 20-item overflow fixture. |

* Current next action

Exercise the complete transaction against real Bash/readline in a disposable
PTY plus deterministic router-shaped fixtures: unique and ambiguous Git-like
completion, repeated Tab, arbitrary chunking, bells, quoting, Unicode,
punctuation, resize/focus changes, and overflow.
