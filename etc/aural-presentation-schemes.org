#+title: Semantic Aural Presentation Schemes
#+author: Emacsvox Contributors
#+date: <2026-07-25 Sat>

* Purpose

Build one semantic aural-presentation system for Emacsvox.

Speech labels, voice changes, auditory icons, pauses, ordering, and spatial
placement are alternative and composable ways to present meaning.  Modules
should describe what an object, state, or event means; a user-selected scheme
should decide how that meaning is rendered.

For example, an Org heading may be presented with any combination of:

- a distinct voice for its level;
- spoken text such as "Heading 1" before its contents;
- an auditory icon before or after its contents;
- a pause;
- a stable spatial position;
- a spoken or sounded indication that it is folded.

The system must not force those choices into mutually exclusive "voice",
"speech", or "sound" settings.  It must produce an ordered render plan that can
contain all of them.

This plan supersedes the narrower semantic auditory-icon theme plan.  Auditory
icon themes become resource providers within the broader system, and existing
ACSS personalities become one source of voice styling.  Both remain usable
through a staged, backward-compatible migration.

* Design Summary

The proposed flow is:

#+begin_example
semantic facts
      |
      v
matching scheme rules
      |
      v
backend-independent aural render plan
  +-- before: speech, cue, pause
  +-- content: speech plus voice and spatial style
  `-- after: speech, cue, pause
      |
      v
backend compilation and queueing
      |
      v
speech and audio services
#+end_example

The central design invariant is:

#+begin_quote
Resolve semantic facts, scheme rules, context, icons, and voice styles while
the source buffer and Emacs state are available.  Queue only a concrete,
ordered render plan.  The speech server must not need to understand modes,
modules, semantic names, schemes, inheritance, or user override precedence.
#+end_quote

* Goals

1. Define a stable semantic vocabulary for aural objects, events, states, and
   their attributes.
2. Let a module express meaning once without choosing whether the user hears
   speech, a voice change, a sound, spatial placement, or a combination.
3. Let schemes compose ordered actions before, during, and after spoken
   content.
4. Give users deterministic global, module, major-mode, derived-mode, and
   buffer-local customization.
5. Preserve ACSS as the device-independent voice description and preserve
   existing face-to-personality behavior during migration.
6. Turn auditory-icon themes into validated resource packs with documented
   semantic intent and fallback.
7. Resolve concrete voice commands and sound resources before queueing them.
8. Provide portable stereo positioning and optional richer spatial audio with
   documented fallback.
9. Make the system discoverable through completion, validation, preview,
   explanation, and an accessible scheme editor.
10. Keep existing one-argument icon calls, =personality= properties, face
    mappings, and bundled sound directories working while modules migrate.

* Non-Goals

- Replace the speech server or audio server in the first implementation.
- Rewrite every Emacsvox integration in one change.
- Move or rename the existing =sounds/= tree during the initial migration.
- Redesign the bundled audio samples.
- Make pronunciation, punctuation, capitalization, or language selection part
  of the first rule schema.  The render-plan contract should leave room for
  them, but existing TTS preprocessing remains authoritative initially.
- Require full three-dimensional audio on every backend.
- Require every scheme or sound pack to implement every third-party semantic
  extension.
- Infer module ownership from the Lisp call stack.
- Store arbitrary executable Lisp forms in portable scheme data.

Minor-mode selectors are deferred from the first implementation.  Several
minor modes may be active simultaneously, so they require an explicit ordering
and conflict policy rather than an accidental list order.

* Motivating Org Example

An Org module should be able to expose facts equivalent to:

#+begin_src emacs-lisp
(:role heading
 :level 1
 :states (folded))
#+end_src

The presentation context is captured separately:

#+begin_src emacs-lisp
(:module org
 :mode org-mode
 :occasion navigation)
#+end_src

The exact public API and data representation are implementation decisions, but
semantic facts and presentation context must not prescribe a presentation.

One scheme might add rules equivalent to:

#+begin_src emacs-lisp
(:id org-heading-level-1
 :match (:role heading :module org :level 1 :occasion navigation)
 :render
 (:before ((:kind speech :text "Heading 1")
           (:kind cue :name heading-1))
  :content (:voice voice-bolden
            :space (:balance -0.2))))

(:id org-folded-heading
 :match (:role heading :module org :state folded)
 :render
 (:after ((:kind speech :text "folded"))))
#+end_src

Another scheme could omit the spoken level, put the cue after the contents,
represent =folded= with a sound, or leave the heading unchanged.  The second
rule demonstrates composition: state presentation is independent of heading
level presentation.

This example establishes requirements rather than committing to these keyword
names:

- selectors can match roles, modules, attributes, and states;
- before and after actions are explicitly ordered;
- content can retain its text while receiving voice and spatial styling;
- independently matched rules can contribute to one render plan;
- a user can replace or suppress each contribution.

** Presentation occasions

The same semantic object may need different presentation when it is reached
through different interactions.  Presentation occasion is context, not part of
the object's identity.

The initial occasions are:

| Occasion       | Meaning                                             |
|----------------+-----------------------------------------------------|
| =navigation=   | Point or focus moved to the object                  |
| =continuous=   | Continuous reading reached the object               |
| =state-change= | An action changed a registered state                 |
| =edit=         | The object was created or changed                    |
| =inspection=   | The user explicitly requested detail or explanation |
| =notification= | An asynchronous event requested user attention      |

For an Org heading:

| Occasion       | Facts retained                    | Additional event context |
|----------------+-----------------------------------+--------------------------|
| =navigation=   | heading, level, folded state      | focus entered            |
| =continuous=   | heading, level, folded state      | continuous reading       |
| =state-change= | heading, level, new folded state  | folding changed          |
| =edit=         | heading, level, folded state      | heading changed          |
| =inspection=   | heading, level, folded state      | explain presentation     |

This distinction lets a user retain level voices during continuous reading
while hearing "Heading 1" or a cue only during navigation.  Modules must use a
registered occasion rather than treating the command name as presentation
semantics.

* Prior Art

** JAWS Speech and Sound Schemes

JAWS Speech and Sound Schemes associate controls, states, document elements,
and text attributes with voices, spoken text, or sounds.  Published examples
include piano notes for heading levels and a deeper voice for bold text.
Schemes can be selected by application.

The useful ideas for Emacsvox are:

- one user-facing scheme spans speech, voices, and sounds;
- rules are based on recognized semantics rather than file names;
- application-specific selection is visible to the user;
- preview is part of editing;
- training mode plays the configured presentation and then identifies the
  underlying semantic object or state.

The published JAWS editor exposes a fixed taxonomy and appears oriented toward
selecting a presentation for each recognized item.  Emacsvox needs an explicit
composition and ordering contract so that a heading can use speech, a voice,
and a cue together and so independent state rules can add presentation.

References:

- [[https://www.freedomscientific.com/SurfsUp/Speech_Sounds_Schemes.htm][JAWS Speech and Sound Schemes]]
- [[https://support.freedomscientific.com/support/technicalsupport/bulletin/513][JAWS scheme configuration by application]]
- [[https://blog.freedomscientific.com/increase-productivity-with-new-sound-features-in-jaws-and-fusion-2022/][JAWS split sound]]

** Aural CSS and CSS Speech

The CSS2 aural style model combines voice properties, =cue-before=,
=cue-after=, pauses, and spatial azimuth and elevation.  Its examples style
headings with both a voice and a preceding cue.  The CSS Speech module retains
voice styling, pauses, and cues and defines a portable stereo =voice-balance=
property.

Emacsvox already uses ACSS concepts for device-independent voice definitions.
The proposed system extends that architectural idea from voice alone to an
ordered aural presentation.

References:

- [[https://www.w3.org/TR/CSS2/aural.html][CSS2 Aural Style Sheets]]
- [[https://www.w3.org/TR/css-speech-1/][CSS Speech Module Level 1]]

* Current Architecture

** Voice and personality

=lisp/voice-setup.el= defines ACSS with the dimensions =family=,
=average-pitch=, =pitch-range=, =stress=, and =richness=.  =defvoice= creates a
named personality and customizable ACSS settings.  Engine adapters translate
the ACSS values into device-specific voice commands.

Text can carry a =personality= property.  If it does not, =tts-get-style= maps
its =face= property through =voice-setup-face-voice-table=.  Explicit
=personality= therefore takes precedence over face-derived voice styling.
=tts-audio-format= identifies style runs and queues the corresponding voice
commands and text.

This is already a useful separation:

- a face or module selects a semantic personality;
- ACSS describes it without depending on one TTS engine;
- an engine adapter generates concrete commands.

The missing pieces are a common semantic registry, contextual scheme rules,
ordered non-speech actions, spatial styling, and an explanation of why a voice
was selected.

** Auditory icons

=lisp/emacsvox-sounds.el= maps an icon symbol such as =open-object= to an Ogg
file in the selected directory.  =emacsvox-icon= either invokes a local player
or sends the resolved resource to the speech server.

The current cache is populated from =sounds/prompts/= and then the selected
theme.  Unknown names silently use =button=.  Theme selection knows the
hard-coded names =chimes= and =3d=.  There is no canonical intent registry,
scheme inheritance, per-semantic override, or common validation.

** Queued properties

=tts-audio-format= handles an =auditory-icon= property at the beginning of a
text run separately from =personality=.  It queues the icon and then formats
the text and voice.  This supplies only an implicit "before" ordering and
cannot express an after cue or spoken state annotation.

=tts-speak= copies text and its properties into = *tts-scratch-buffer* =.  It
captures the source =major-mode= in =orig-mode= for limited preprocessing but
does not make the scratch buffer use that mode.  Future mode-, module-, and
buffer-specific scheme resolution must therefore happen before the source
context is lost, or the required context must be captured explicitly with the
semantic annotation.

** Spatial audio

The =sounds/3d/= theme contains Csound-generated HRTF assets.  Several source
files under =sounds/3d/src/= use azimuth, elevation, panning, or movement.
=etc/pipewire/= also contains experiments with HRTF virtual devices and
left/right or upper/lower speech routing.

Spatial behavior is currently baked into assets or output devices.  It is not
a semantic property that a scheme can apply independently to a cue or speech.

* Terminology

** Semantic fact

A registered statement about meaning.  Examples include =:role heading=,
=:level 1=, =:state folded=, =:event open-object=, and =:severity warning=.

** Aural object

Content with an identity and lifecycle that may be rendered before, during,
and after its spoken text.  A heading is an aural object.

** Event

A semantic occurrence such as opening an object, completing a task, reaching a
boundary, or receiving mail.  An event may have associated content but need
not correspond to a persistent text range.

** State

A fact that qualifies an object or event, such as =folded=, =checked=,
=selected=, or =unavailable=.

** Presentation occasion

Context describing why an object or event is being rendered, such as
=navigation=, =continuous=, =state-change=, =edit=, =inspection=, or
=notification=.  Occasion affects rule matching but does not change semantic
identity.

** Scheme

A named, versioned collection of declarative rules plus references to resource
packs and voice palettes.  A scheme may inherit from another scheme.

** Rule

A selector and presentation contribution.  A selector matches semantic facts
and context.  Its contribution adds, changes, reorders, replaces, or suppresses
parts of a render plan.

** Render phase

One of =before=, =content=, and =after=.  Phases make order explicit without
requiring modules to coordinate separate speech and sound calls.

** Render action

A backend-independent instruction such as:

- speak literal text;
- speak the object's content;
- apply a voice or ACSS style;
- play a semantic cue;
- insert a pause;
- apply volume or spatial placement.

** Render plan

The ordered, fully merged result of semantic and scheme resolution.  It still
uses backend-independent voice and spatial values, but contains no unresolved
scheme rules.

** Concrete queue plan

The render plan after backend compilation.  Cue names have become file paths
or sample identifiers, voices have become supported engine settings or
commands, and spatial values have been reduced to backend capabilities.

** Sound resource pack

A named collection of concrete assets implementing registered cue names.
Existing =chimes= and =3d= directories become bundled resource packs.

** Voice palette

A named collection of device-independent voice styles.  Existing ACSS
personalities provide the initial palette.

* Semantic Contract

** Registry entries

The semantic registry is the authority for documentation, completion,
validation, and development-time auditing.  It should describe:

- a stable identifier;
- whether the entry is an object role, event, state, or attribute;
- concise user-facing intent;
- owning module for extensions;
- valid attributes and value types;
- valid states;
- lifecycle or allowed render phases where relevant;
- semantic fallback;
- optional default presentation;
- usage guidance and examples.

Semantic identifiers must not encode a chosen modality.  For example,
=open-object= can remain a useful event name, but a new identifier should not
be called =play-open-sound=.

Core names should express reusable interaction semantics.  A module registers
an extension only when no core meaning is suitable.  Extension registrations
must document an owner and a core fallback where one exists.

Registration collisions, invalid attributes, unknown states, and fallback
cycles must be diagnosed.  Development checks should reject new unregistered
literal semantics in the repository.

** Module contract

A module may:

- annotate a text range with registered facts;
- present a registered event with optional content;
- provide default rules scoped to its own semantics;
- register module-specific roles, states, attributes, or events.

A module must not:

- choose a sound filename;
- assume a bundled sound pack;
- infer that a semantic event will necessarily be a sound;
- depend on a particular TTS engine;
- hide undocumented meaning inside a newly invented cue name.

The emitting module supplies module context explicitly.  The resolver obtains
the source buffer and major mode directly while they are available.  Context
must not be guessed from the call stack.

* Scheme and Rule Contract

** Selectors

The first implementation must support selectors over:

- semantic role or event;
- state;
- registered attributes and values;
- module;
- presentation occasion;
- exact major mode;
- derived major-mode ancestry;
- current buffer through a buffer-local rule layer.

Selectors may combine axes.  For example, a rule can target folded Org
headings without affecting headings rendered by another module.

Minor modes, window context, frame context, and workspace/project context may
be added after their ordering semantics are specified.

** Contributions and composition

Rules contribute to =before=, =content=, or =after=.  The merge contract must
distinguish scalar style properties from ordered action sequences.

Recommended initial behavior:

- scalar values such as voice, balance, volume, and speaking state use the
  most specific winning value;
- ordered actions compose in deterministic rule order;
- a rule can explicitly prepend, append, replace, remove, or suppress an
  action or phase;
- semantic identity is never changed by a presentation rule;
- silence can target a cue, speech annotation, content speech, a phase, or the
  complete presentation;
- duplicate suppression is based on stable action identifiers, not coincident
  text or file names.

The final data syntax and merge operators must be agreed and tested before
runtime integration.  Plain "last writer wins" is not sufficient because
independent rules such as heading level and folded state must compose.

** Cascade

Origin and selector specificity are separate concerns.  The implemented
origin layers, from strongest to weakest, are:

1. current-buffer overrides;
2. temporary session overrides;
3. persistent personal user rules;
4. active scheme rules;
5. module-provided defaults;
6. core fallbacks.

Within one layer, the resolver orders rules by documented selector
specificity:

1. combined module, exact mode, semantic identity, attributes, and states;
2. exact major mode;
3. nearest matching derived-mode ancestor;
4. module;
5. semantic identity, attributes, and states without context;
6. layer-wide defaults.

Additional selector fields increase specificity only according to a documented
comparison; registration order must not accidentally change behavior.  A
stable explicit rule order breaks genuine ties and is visible in the editor.

** Resolved rule representation and specificity

Schemes use safe, versioned keyword plists for persistence and interchange.
The pure resolver compiles them into validated internal structures.  Module
defaults are read-only scheme fragments in the same declarative format.

Schema version 1 rules accept optional =:enabled nil= or =:enabled t=, with
omission meaning enabled.  Disabled rules remain in scheme data, retain their
stable identifiers, and are fully validated, but do not match, contribute
actions, or add resource requirements.  Identifier uniqueness includes
disabled rules so disabling cannot hide an ambiguity that reappears later.

=content= is a distinguished scalar-style phase.  =before= and =after= contain
ordered actions.  Scalar values use the strongest matching contribution.
Sequences append by default and support explicit =prepend=, =append=,
=replace=, =remove=, and =suppress= operations.  Actions have stable
identifiers so inherited contributions can be removed without comparing text
or filenames.

Rules are applied from weakest to strongest using a fixed vector:

1. origin layer;
2. matched semantic identity;
3. combined module and exact mode;
4. exact mode or nearest derived-mode ancestor;
5. module;
6. matched states, attributes, and compatibility hints;
7. parent-to-child order within an inherited origin;
8. explicit visible rule order;
9. stable rule identifier for a genuine tie.

This makes resolution independent of hash-table and registration order.  The
resolved plan retains the matching rule identifiers, score vectors, action
sources, and scalar-property provenance.

** Storage and versioning

Persistent scheme data must:

- carry a schema version;
- have stable scheme and rule identifiers;
- be readable without evaluating arbitrary code;
- preserve comments or user-facing descriptions where practical;
- support validation without selecting the scheme;
- support inheritance without mutating the parent;
- provide explicit migrations when the schema changes.

The native representation may be declarative Lisp data.  Import/export can use
another safe format later, but the semantic and merge contracts must not
depend on the serialization.

The initial persistent implementation uses the data-only file
=~/.emacsvox/aural-schemes.el=, configurable as
=emacsvox-aural-schemes-file=.  It binds =read-eval= to nil before reading,
accepts one validated data form, saves through a mode-0600 temporary file and
atomic rename, and retains the previous file as =~=.  User data and individual
schemes are independently versioned; migration functions must explicitly
advance an older version.  Invalid replacement data is fully staged and
validated before it can replace the live personal registry.

=emacsvox-aural-active-scheme= is a Customize option.  Startup loads the
personal data, validates scheme inheritance and providers, and selects the
active scheme's inherited resource pack.

* Voice Integration

ACSS remains the device-independent voice layer.  The first implementation
should not replace =defvoice=, engine-specific voice adapters, or existing
=personality= text properties.

The renderer should accept:

- a named existing personality;
- a named voice-palette entry;
- an ACSS style or partial ACSS override;
- the default voice;
- =inaudible= where content suppression is intended.

Compatibility rules:

1. Text with no registered semantic annotation follows the current
   =personality=, then face-to-personality behavior.
2. Existing =personality= values become legacy presentation hints when the
   text also has semantic facts.
3. A user rule can deliberately override a legacy personality for matching
   semantics.
4. Face mappings remain a fallback source of presentation; visual face names
   do not become the canonical semantic vocabulary.
5. Backend compilation uses existing ACSS-to-engine adapters wherever
   possible.

The pure resolver should output a named or ACSS voice style.  The TTS adapter
then converts it to concrete engine commands before queueing.  This keeps
engine codes out of schemes while keeping the speech server simple.

* Cue and Resource Integration

The registry distinguishes a semantic cue from its concrete asset.  A resource
pack maps registered cue names to files and declares:

- name, version, summary, and optional parent;
- file or sample mapping;
- supported cues;
- required and optional coverage;
- format, channels, and duration where known;
- whether an asset is position-neutral, stereo, or already spatialized;
- fixed position or trajectory metadata for pre-spatialized assets;
- semantic fallback and aliases.

The default mapping may remain =CUE.ogg=.  Existing sound directories remain
valid resource sources during migration.

Theme selection must build a fresh effective resource cache.  It must not
retain an asset merely because the previous pack supplied it.  Unknown names,
unreadable files, aliases, missing required assets, and fallback cycles must be
diagnosed rather than silently becoming =button=.

A scheme may select a default resource pack and voice palette.  Users may
override either component without changing the scheme's semantic rules.

** Resolved resource-pack completeness

Resource packs use requirement profiles rather than one universal all-or-none
definition of completeness.

- Every standalone sound pack must resolve =button= as its emergency fallback.
- =legacy-complete= contains the 55 cues shared by =chimes= and =3d=.
- Both bundled sound packs satisfy =legacy-complete=.
- =shutdown= is optional: a direct resource wins, otherwise it falls back to
  =close-object=.
- A scheme derives additional required cues from its compiled rules.
- Parent packs may satisfy requirements for focused child packs.
- Partial packs remain valid providers but do not claim profiles they cannot
  satisfy.

All bundled filenames, prompt names, and compatibility aliases have registered
intent.  =emacsvox= falls back to =startup=, =repeat-stop= to =repeat-end=, and
=unmark-object= to =deselect-object=.  The existing 25 ACSS personalities are
exposed through the =acss-default= voice palette.

* Spatial Presentation

Spatial styling belongs to render actions and applies independently to speech
and cues.

The implemented portable level is normalized stereo balance.  =-1.0= is full
left, =0.0= is centre, and =+1.0= is full right.  Scheme data uses either
=(:balance NUMBER)= in that range or listener-relative
=(:azimuth DEGREES)= from =-180= through =+180=.  Positive values are right,
negative values are left, zero degrees is directly in front, and either 180
degree value is directly behind.

Azimuth is currently a portable input rather than a claim of 3D output.  It
reduces to stereo using:

#+begin_example
balance = sin(azimuth in radians)
#+end_example

This preserves front and rear at the centre, maps 90 degrees right to =+1.0=,
and maps 90 degrees left to =-1.0=.  A richer future level may add native:

- elevation;
- distance;
- width or spread;
- a time-varying trajectory.

The coordinate system must be listener-relative and documented.  Backend
capability fallback should be deterministic:

#+begin_example
3D position or trajectory -> stereo balance -> centred mono
#+end_example

Backends report capabilities.  The resolver does not inspect PipeWire, SoX,
Csound, or speech-server details; the backend compiler reduces the requested
style to what is available and records the degradation for explanation.

The concrete compiler reports =stereo=, =mono=, or =centered= independently
for speech, ordered speech-server cues, and standalone local cues.  The
current local SoX adapter implements balance using two-channel =remix=.
Current speech and ordered-cue transports centre requests unless an adapter
installs the corresponding ordered balance function.  A declared mono output
always centres requests.  The concrete plan records requested space, compiled
balance, capability, and every reduction before anything is queued.

Pre-spatialized assets must not be spatialized a second time by default.
Resource metadata must distinguish position-neutral material from assets such
as the current HRTF-generated =3d= sounds.  The compiler obtains this metadata
from the pack that owns the resolved file, including inherited and prompt
resources; a requested balance is not applied to a =pre-spatialized= asset.

Users need independent controls to:

- disable all spatial presentation;
- disable spatial speech while retaining spatial cues;
- reduce the maximum left/right separation;
- remap positions for unilateral hearing or speaker layouts;
- choose a non-spatial speech or cue fallback.

The stable schema uses audible centred output as its non-spatial fallback.
Whether a critical alert may instead be suppressed remains a separate policy
decision.  =emacsvox-aural-spatial-enabled=, the independent speech and cue
toggles, =emacsvox-aural-spatial-maximum-separation=,
=emacsvox-aural-spatial-remapping=, and =emacsvox-aural-spatial-output=
implement the other controls without changing scheme data.

Stable positions are preferable to arbitrary motion because they can convey
source or role without reducing speech intelligibility.  Motion should be an
optional later capability, not a requirement for the first portable contract.

* Resolution and Queueing

Resolution has four stages:

1. Capture registered semantic facts and source context.
2. Match and merge scheme rules into a backend-independent render plan.
3. Resolve cues, voices, pauses, and spatial properties through selected
   resource and backend capabilities.
4. Queue a concrete ordered plan.

Concrete cue files or sample identifiers must be selected before queueing.
The speech server should only receive actions it can execute, such as a voice
command, text, silence duration, concrete sound resource, and supported spatial
parameters.

The queue contract must preserve the order of:

- before actions;
- content speech and its style runs;
- after actions.

If audio and speech use separate processes, the adapter must define whether
actions are strictly serialized, overlapped, or mixed.  A scheme must not rely
on accidental process scheduling.  Concurrent and background actions should
be explicit future action modes.

Immediate event presentation and text-property presentation must use the same
resolver and concrete-plan compiler.  A plain symbol-valued =auditory-icon=
property remains valid and maps through a compatibility adapter.

Context must be captured before =tts-speak= moves text into its scratch
buffer.  The implemented boundary is entry to =tts-speak= or =tts-notify=,
before process initialization, notification logging, or stream switching can
change the current buffer.  Every formatted source-text run is resolved and
compiled there, and carries an immutable concrete-plan property into the
scratch buffer.  Pronunciation, punctuation, capitalization, and Unicode
cleanup may still replace the text payload, but must preserve that property
and must not rerun semantic, scheme, resource, or voice resolution.

All multi-action plans use one ordered speech-server queue.  This includes
spoken annotations, pauses, concrete cues, content speech, and trailing
actions.  The only local-player exception is a compatibility call whose
entire presentation is exactly one standalone cue.  A scheme cannot split one
ordered presentation across independently scheduled local and server
processes.

Pulse/PipeWire samples use readable content-addressed identifiers of the form
=emacsvox-PACK-CUE-DIGEST=.  Emacsvox records only identifiers it successfully
uploaded, unloads obsolete owned samples when the selected pack changes, and
releases remaining owned samples at exit.  It never unloads samples owned by
another application.

* Discovery, Explanation, and Editing

The user interface is part of the contract, not a follow-up convenience.  The
implemented discovery and management commands are:

- =emacsvox-list-aural-semantics=: list roles, events, states, attributes,
  owners, and intent;
- =emacsvox-describe-aural-semantic=: describe registration, defaults, and
  available presentations;
- =emacsvox-explain-aural-presentation=: infer the most informative occasion,
  speak a concise before/content/after explanation, and show captured facts,
  matching rules, cascade order, concrete resources, and backend fallback in
  a Help buffer; a prefix argument selects the occasion explicitly;
- =emacsvox-describe-aural-spatial-capabilities=: show speech, ordered-cue,
  local-cue, final-output, and user-policy spatial settings;
- =emacsvox-preview-aural-rule=: render a rule against representative or
  current content;
- =emacsvox-list-aural-schemes=: open the spoken manager for viewing,
  editing, copying, deleting, renaming, activating, previewing, and validating
  schemes;
- =emacsvox-validate-aural-scheme=: report unknown semantics, invalid
  selectors, conflicts, unavailable voices, and missing assets;
- commands to add, edit, reorder, copy, enable, disable, and remove user rules
  at global, module, mode, combined, and buffer scopes.

=emacsvox-set-aural-scheme=, =emacsvox-reset-aural-scheme=,
=emacsvox-copy-aural-scheme=, =emacsvox-delete-aural-scheme=,
=emacsvox-rename-aural-scheme=, and =emacsvox-reset-aural-overrides= provide
the lifecycle operations.  Scheme copying creates an editable inheriting
child by default; a prefix argument creates a flattened copy.

The scheme editor should use an accessible Emacs buffer and completion rather
than requiring users to edit raw data.  Its workflow should be:

1. choose the semantic object, event, state, or current item;
2. narrow its context and attributes;
3. add and order before, content, and after actions;
4. select voice, speech, cue, pause, and spatial settings;
5. preview;
6. inspect conflicts and effective fallback;
7. save to the desired scope.

Customize may expose simple settings, but a dedicated editor is needed for
ordered actions, inheritance, rule explanations, and previews.

Add a training mode inspired by JAWS.  It should perform the normal
presentation and then speak a concise semantic explanation.  A more detailed
command should identify every matched rule and why it won or composed.

The implemented =emacsvox-aural-training-mode= adds its concise semantic
identification after the concrete plan, including after a standalone cue
played through a local adapter.  It queues concrete explanatory text directly
and never recursively invokes semantic resolution.  Detailed inspection uses
=emacsvox-explain-aural-presentation=.

** Accessible scheme-manager contract

=emacsvox-list-aural-schemes= is the normal entry point for scheme
management.  Each row reports the active state, built-in or personal
ownership, parent, effective sound pack, direct and effective presentation
counts, validation state, and summary.  Opening the manager speaks a concise
description of the active row.

The manager exposes these stable operations:

| Key       | Operation                                                  |
|-----------+------------------------------------------------------------|
| =n= / =p= | Move to the next or previous scheme                        |
| =Arrows=  | Move by row or column, speaking the column title and value  |
| =RET=     | View direct, inherited, effective, and resource details    |
| =e= / =A= | Open the simple or advanced editor                         |
| =c=       | Copy as an inheriting personal scheme                      |
| =C-u c=   | Copy as a flattened personal scheme                        |
| =d= / =r= | Delete or rename a personal scheme                         |
| =a=       | Activate the scheme                                        |
| =P= / =v= | Preview a representative presentation or validate          |
| =SPC= / =.= | Speak the current row, or the titled current cell        |
| =g= / =?= | Refresh, or display and speak manager help                  |

Moving above or below the list announces the top or bottom instead of
repeating the current value.  Horizontal movement announces the first and
last column boundaries.  Preview uses uppercase =P= so lowercase =p= remains
the conventional previous-row command.

Built-in schemes are viewable and copyable but cannot be edited, renamed, or
deleted.  A personal scheme cannot be deleted while another scheme inherits
it.  Deleting the active scheme activates its parent, or =default= when it has
no parent.  Renaming a scheme updates direct child schemes atomically.  Copy,
delete, and rename changes are saved through the same data-only atomic
persistence contract as the editors.

** Accessible editor contract

=emacsvox-edit-aural-scheme= opens a spoken field form for a persistent
personal scheme.  When no suitable personal scheme exists, the form offers to
make a flattened editable copy of a built-in scheme.  The form describes each
rule as what it applies to and what happens before, during, and after content;
it does not expose raw plists or restart a complete rule wizard for a
single-field change.

The simple form exposes these stable operations:

| Key                  | Operation                                            |
|----------------------+------------------------------------------------------|
| =TAB= / =Shift-TAB=  | Move to and speak the next or previous field         |
| =RET= or =e=         | Edit only the field at point                         |
| =n=                  | Add a presentation through natural prompts           |
| =d= / =t=            | Delete, or enable/disable, the presentation at point |
| =p= / =x=            | Preview or explain the presentation at point         |
| =v= / =s=            | Validate or atomically save                          |
| =a=                  | Activate the edited scheme                           |
| =A=                  | Switch to the advanced editor without losing changes |
| =?=                  | Display and speak the field-oriented workflow        |

Scheme summary, inheritance, sound pack, and voice palette are individual
fields.  Presentation fields cover semantic context, before feedback, content
speech/voice/position, and after feedback.  Common label, cue, ordering, and
left/centre/right placement choices use natural prompts.  A clearly labelled
advanced-details field appears for legacy selectors, phase merge/removal,
pauses, per-action voice/volume, or advanced content styling.

=emacsvox-edit-aural-scheme-advanced= opens the complete
=emacsvox-aural-scheme-editor-mode= rule view.
=emacsvox-edit-aural-rules= uses the same advanced view for a persistent
personal, temporary session, source-buffer-local, or explicitly selected
personal-scheme scope.  Built-in schemes remain read-only.

The advanced buffer exposes these stable operations:

| Key                  | Operation                                      |
|----------------------+------------------------------------------------|
| =n=                  | Add a guided rule                              |
| =RET= or =e=         | Edit the complete rule at point                |
| =c=                  | Copy a rule under a new stable identifier      |
| =d=                  | Remove a rule                                  |
| =t=                  | Enable or disable a rule without losing it     |
| =M-up= / =M-down=    | Reorder rules and their explicit visible order |
| =m=                  | Edit scheme inheritance and providers          |
| =p= / =x=            | Preview or explain the unsaved rule            |
| =v= / =s=            | Validate or atomically save/apply               |
| =A=                  | Return to the simple editor                     |
| =?=                  | Describe the completion-driven workflow         |

Selectors are assembled from registered semantic, module, major-mode,
occasion, and compatibility fields.  Before and after phases explicitly
choose append, prepend, replace, remove, suppress, or unchanged behavior and
then build ordered speech, cue, and pause actions.  Content prompts distinguish
unchanged, spoken, silenced, default voice, named voice, and inaudible
presentation.  Guided spatial prompts offer unchanged, centred, normalized
balance, or listener-relative azimuth values and validate them against the
portable contract.

* Compatibility Policy

1. Existing =(emacsvox-icon ICON)= calls remain valid.
2. Existing symbol-valued =auditory-icon= text properties remain valid.
3. Existing =personality= properties and face-to-personality mappings remain
   valid.
4. Existing =defvoice= and ACSS settings remain supported.
5. Existing =chimes=, =3d=, and =prompts= directories remain valid resource
   sources.
6. Existing player selection remains supported.
7. Unknown legacy icons warn once and fall back during migration; new in-tree
   semantics must be registered.
8. Module context and richer semantics are optional until a module is
   deliberately migrated.
9. No repository-wide mechanical caller rewrite is part of an initial slice.
10. Compatibility adapters are removed only through a separately reviewed
    public API change.

* Baseline Inventory

The earlier read-only inventory found:

| Asset or reference                        | Count |
|-------------------------------------------+-------|
| =sounds/chimes/*.ogg=                     |    56 |
| =sounds/3d/*.ogg=                         |    55 |
| =sounds/prompts/*.ogg=                    |    17 |
| Direct =emacsvox-icon= calls               |  1750 |
| Direct literal icon references            |  1659 |
| Direct literal icon symbols found in Lisp |    58 |
| =voice-setup-add-map= forms                |   106 |
| Explicit personality property writes      |    50 |
| Literal =tts-speak= string calls           |   104 |
| Lisp references to =auditory-icon=         |    22 |
| Csound HRTF or panning instructions       |    40 |

=shutdown.ogg= is present in =chimes= but absent from =3d=.  Static inspection
also found direct requests for =emacsvox=, =repeat-stop=, and =unmark-object=
without a matching file in =chimes= or =prompts=.  They currently fall back to
=button=.

The difference between all icon calls and literal icon references confirms
that some names are computed dynamically and require semantic inspection.
The voice, speech, property, and spatial counts are static search baselines;
they measure references rather than distinct semantics.

This remains a useful implementation inventory but is not itself a semantic
vocabulary.  Direct speech labels, face mappings, explicit =personality=
properties, pauses, computed icon symbols, and state announcements require
review as modules migrate.

** Initial semantic vocabulary

The first registry seed is deliberately small:

| Identifier       | Kind      | Intent                                      |
|------------------+-----------+---------------------------------------------|
| =heading=        | role      | A titled structural section                 |
| =level=          | attribute | One-based structural depth                  |
| =folded=         | state     | Descendants are currently hidden            |
| =focus-entered=  | event     | Navigation arrived at an aural object       |
| =state-changed=  | event     | A registered state changed                  |
| =object-changed= | event     | A registered object was created or modified |

The registered presentation occasions are =navigation=, =continuous=,
=state-change=, =edit=, =inspection=, and =notification=.  =heading= and
=level= are core semantics; the Org module initially owns the interpretation
of Org folding and maps it to the core =folded= state.

This seed is sufficient to exercise an object, an attribute, a state, separate
events, and occasion-sensitive rules without prematurely assigning semantics
to all 58 legacy icon names.  Legacy icon compatibility remains presentation
input until each name is reviewed.

** Representative use-case fixtures

These fixtures bound the initial architecture without making every candidate a
core registry entry immediately:

| Integration   | Semantic facts or events                         | Occasion                          | Existing presentation source                 |
|---------------+--------------------------------------------------+-----------------------------------+----------------------------------------------|
| Org           | heading, level, folded, focus/state/object change | navigation, continuous, edit      | level faces, spoken line, movement/open cues |
| Notmuch       | message, field, unread, flagged, attachments      | navigation, continuous            | field faces, ordered speech, status cues     |
| Programming   | code construct, syntax role, boundary             | navigation, continuous, inspection | font-lock faces, speech, movement cues       |
| Completion    | candidate, selected, accepted, list opened/closed | navigation, state-change          | candidate faces and complete/select cues     |
| Agent Shell   | response, thought, tool, permission, processing   | continuous, notification          | verbosity choices, speech, lifecycle cues    |

Org supplies the first registry and vertical-slice implementation.  The other
rows are fixtures for testing that the contract can later handle field order,
transient lists, face-derived presentation, asynchronous lifecycle events, and
module-specific verbosity without changing the core model.

** Legacy semantic decisions

The unresolved legacy names use semantic cleanup with compatibility aliases:

| Legacy name     | Canonical semantic   | Initial resource and fallback               |
|-----------------+----------------------+---------------------------------------------|
| =emacsvox=      | =product-identity=   | =startup= prompt                            |
| =repeat-stop=   | =activity-ended=     | =repeat-end=                                |
| =unmark-object= | =selection-cleared=  | =deselect-object=                           |
| =shutdown=      | =game-over=          | =shutdown= when available, then =close-object= |

The canonical semantics describe intent rather than filenames.  In-tree
callers migrate in their representative-module slices.  Old names remain
warning compatibility aliases so external integrations continue to work.

Add =product-identity=, =activity-ended=, =selection-cleared=, and =game-over=
as core events when the registry is seeded.

=utils/count-icons.pl= is historical tooling derived from =sounds/chimes/= and
source scraping.  It should not remain the authority once the semantic
registry exists.

* Implementation Plan

Each slice should be independently reviewable.  The first slices establish
pure data contracts and characterization tests before changing presentation.

** DONE Slice 0: Confirm Use Cases and Vocabulary
CLOSED: [2026-07-26 Sun]

- Record representative aural objects, events, states, and attributes from
  Org, Notmuch, programming modes, completion, and Agent Shell.
- Include at least the Org heading-level and folded-state variants described
  in this plan.
- Distinguish semantic identity from presentation occasion, including
  navigation, continuous reading, state change, editing, inspection, and
  notification.
- Inventory direct speech labels, literal and computed icons, =personality=
  properties, face mappings, pauses, prompts, and spatial assets.
- Classify reusable core semantics and module extensions.
- Define intent and fallback metadata without choosing presentation modality.
- Decide the semantics of unresolved legacy names such as =emacsvox=,
  =repeat-stop=, and =unmark-object=.

Deliverable: reviewed semantic vocabulary and use-case fixtures with no
runtime behavior change.

Checks: static repository inventory, =org-lint=, and =git diff --check=.

** DONE Slice 1: Characterization Tests
CLOSED: [2026-07-26 Sun]

- Add focused tests for current icon caching, theme switching, fallback,
  immediate playback, and queued =auditory-icon= properties.
- Characterize explicit =personality= precedence over face mappings, ACSS
  definition, style-run boundaries, =inaudible=, pauses, and engine command
  generation.
- Characterize property copying and source-context loss in =tts-speak=.
- Add a regression demonstrating that switching sound themes cannot leak an
  asset from the previous theme.
- Preserve existing canonical-process and speech protocol tests.

Deliverable: tests protecting existing public behavior and identifying exact
compatibility boundaries.

Checks: 19 focused ERT results on the available Emacs 30.2 runtime, including
one expected failure recording the stale theme-cache bug.  The repository-wide
=make unit-test= gate could not run because =test/run-tests.el= requires Emacs
31 or later; the current environment provides Emacs 30.2.

** DONE Slice 2: Semantic Registry
CLOSED: [2026-07-26 Sun]

- Implement registration and lookup for roles, events, states, and attributes.
- Seed the reviewed core and module-extension definitions.
- Add completion, description, ownership, fallback, and collision validation.
- Add a development audit for unregistered literal semantics.
- Provide internal adapters that describe legacy icon calls and personality
  hints as semantic or presentation inputs without changing their output.

Deliverable: one canonical, discoverable vocabulary with no intentional user
presentation change.

Checks: 11 focused semantic-registry ERT tests, 30 combined aural
characterization and registry results with one expected stale-cache failure,
=check-parens=, and =git diff --check=.  The Emacs 31 full-suite gate remains
unavailable in the current Emacs 30.2 environment.

** DONE Slice 3: Pure Rule and Render-Plan Engine
CLOSED: [2026-07-26 Sun]

- Finalize the safe, versioned rule data shape.
- Implement selector matching independently of playback.
- Implement the origin layers, mode ancestry, specificity comparison, and
  stable tie ordering.
- Implement explicit scalar merge and sequence prepend, append, replace,
  remove, and suppress operations.
- Represent before, content, and after phases as an ordered render plan.
- Record provenance for every plan value and action.
- Add pure tests for composition, conflicts, inheritance, and explanation.

Deliverable: semantic facts and rules compile deterministically into an
explainable backend-independent render plan.

Checks: 12 focused rule-engine ERT tests covering validation, occasions,
heading/state composition, origin layers, combined context, derived modes,
stable ties, scalar provenance, sequence operations, suppression, and input
validation; =check-parens= and =git diff --check=.  The Emacs 31 full-suite
gate remains unavailable in the current Emacs 30.2 environment.

** DONE Slice 4: Resource Packs and Voice Palettes
CLOSED: [2026-07-26 Sun]

- Register =chimes=, =3d=, and prompts as resource providers.
- Replace the hard-coded sound-theme list with registry completion.
- Build a fresh resource cache on selection.
- Add inheritance, aliases, semantic fallbacks, coverage reporting, and asset
  spatial metadata.
- Expose existing ACSS personalities as the initial voice palette.
- Validate named voices and ACSS values without generating backend commands in
  the pure resolver.

Deliverable: explicit sound and voice provider contracts behind one scheme
model.

Checks: 10 focused provider tests and 53 combined aural tests covering cue
intent, requirement profiles, direct and fallback resources, compatibility
aliases, inheritance, scheme-derived requirements, unknown assets, the ACSS
palette, fresh theme caches, and registered selection; =check-parens= and
=git diff --check=.  The Emacs 31 full-suite gate remains unavailable in the
current Emacs 30.2 environment.

** DONE Slice 5: Contextual User Schemes
CLOSED: [2026-07-26 Sun]

- Add active scheme selection and inheritance.
- Add persistent personal rules and buffer-local/session overrides.
- Support module, exact mode, derived mode, combined context, attributes, and
  states.
- Add safe storage, schema versioning, validation, and migration hooks.
- Provide simple compatibility customization for aliasing, replacing, or
  suppressing one legacy icon or personality.

Deliverable: users can customize aural presentation deterministically without
editing module code.

Implemented details:

- safe scheme registration, parent-to-child inheritance, active selection,
  provider inheritance, cycle detection, and duplicate inherited-rule
  detection;
- read-only module fragments followed by active scheme, persistent personal,
  session, and buffer-local rule layers;
- source module, exact and derived mode, occasion, semantic attribute/state,
  legacy cue, and legacy personality selectors;
- the dedicated versioned =~/.emacsvox/aural-schemes.el= store, data-only
  reader, explicit migration hooks, staged atomic loading, mode-0600 atomic
  save, and one-file backup;
- compatibility builders to remap or suppress one legacy cue or personality
  globally or with module/mode selectors;
- preservation of an explicit or face-derived legacy personality when no
  winning rule sets or clears voice, while registered semantic rules remain
  more specific than a compatibility hint;
- sound-pack switching when active scheme selection changes.

Checks: 17 focused contextual-scheme ERT tests and 71 combined semantic,
rule-engine, resource, scheme, sound, and voice tests with no unexpected
results; =check-parens=, =checkdoc=, and =git diff --check=.  The Emacs 31
full-suite gate remains unavailable in the current Emacs 30.2 environment.

** DONE Slice 6: Resolve Before Queueing
CLOSED: [2026-07-26 Sun]

- Capture source buffer, major mode, module, and facts before scratch-buffer
  processing loses context.
- Compile cue names to concrete paths or sample identifiers.
- Compile named or ACSS voices through the selected TTS adapter.
- Route immediate icon calls, queued =auditory-icon= properties, and
  =personality= styling through compatible concrete-plan adapters.
- Define strict ordering across speech-server and local-player paths.
- Implement Pulse/PipeWire sample identifiers and invalidation for contextual
  resource overrides.
- Verify that semantic and scheme resolution never occurs in the speech
  server.

Deliverable: all playback paths queue concrete ordered actions and agree on
fallback, silence, and context.

Implemented details:

- =tts-speak= and notification entry points capture the source buffer, buffer
  name, major-mode lineage, explicit module, occasion, and semantic facts
  before scratch-buffer processing or logging;
- formatted source runs carry frozen concrete plans while the normally
  cleaned scratch-buffer text remains the final speech payload;
- cue resources and content-addressed sample identifiers, palette
  personalities and raw ACSS voices, pauses, content suppression, and ordered
  actions are compiled in Emacs before queueing;
- concrete before, content, and after actions share one strict speech-server
  queue, while a legacy presentation containing only one standalone cue may
  use the selected local player;
- legacy immediate icons, symbol-valued =auditory-icon= properties,
  =personality= properties, face fallback, the icon toggle, SoX, and
  Pulse/PipeWire remain compatibility inputs or adapters;
- unknown legacy cues compile to the concrete =button= fallback and record
  degradation rather than reaching the queue unresolved;
- owned Pulse/PipeWire uploads use =emacsvox-PACK-CUE-DIGEST= identifiers and
  are invalidated on pack changes and released at exit;
- scratch-buffer text replacements preserve frozen plans, and queue tests
  prove that no semantic, scheme, resource, or voice resolver is called after
  compilation.

Checks: 17 focused concrete-transport ERT tests and 89 combined semantic,
rule-engine, resource, scheme, transport, sound, and voice tests with no
unexpected results; 27 source-loaded semantic traces matching the golden
output; =check-parens=, =checkdoc=, byte compilation, =org-lint=, and
=git diff --check=.  The normal =make unit-test= gate requires Emacs 31, and
the normal =make trace-test= initially loads incompatible Emacs 31 bytecode;
both standard targets remain unavailable in the current Emacs 30.2
environment.

** DONE Slice 7: Explanation and Scheme Editor
CLOSED: [2026-07-26 Sun]

- Add list, describe, preview, validate, explain, set, reset, and copy
  commands.
- Show semantic facts and rule provenance for presentation at point.
- Build the accessible scheme editor for selectors, phases, ordered actions,
  scope, and inheritance.
- Report unreachable rules, ambiguous ties, suppressed actions, missing
  assets, and backend degradation.
- Implement training mode with concise semantic explanations.

Deliverable: users can create, understand, test, and repair schemes without
reading internal caches or raw protocol output.

Implemented details:

- tabulated semantic and scheme browsers with completion-driven description
  and validation commands;
- point inspection of namespaced semantic facts or already frozen concrete
  plans;
- reproducible explanations containing facts, source context, matching rules,
  origin and specificity vectors, scalar provenance, ordered concrete
  resources, removed or suppressed actions, and backend degradation;
- previews against current facts or selector-derived representative facts and
  context;
- validation for inheritance and providers, missing or unknown assets,
  unavailable voices after voice initialization, ineffective rules,
  ambiguous stable-ID ties, and disabled rules;
- active-scheme set/reset, inherited or flattened personal copies, and
  persistent/session/buffer override reset commands;
- schema-v1 optional =:enabled= state, default enabled, with full validation
  and identifier uniqueness even while disabled;
- the accessible special-mode editor described above, with atomic personal
  scheme and persistent override saves and isolated session/buffer commits;
- global training mode that follows normal presentation with concise
  registered semantics and offers the full explanation command for detail;
- main Emacsvox startup loads the tools and editor commands.

Checks: 16 focused tooling/editor ERT tests and 105 combined semantic,
rule-engine, resource, scheme, transport, tooling, sound, and voice tests with
no unexpected results; 27 source-loaded semantic traces matching the golden
output; =check-parens=, =checkdoc=, byte compilation, =org-lint=, and
=git diff --check=.  The normal Emacs 31 gates remain unavailable in the
current Emacs 30.2 environment as recorded for Slice 6.

** DONE Slice 8: Org Vertical Slice
CLOSED: [2026-07-26 Sun]

- Register heading role, level attribute, folded state, and other deliberately
  selected Org semantics.
- Annotate or present them through the new semantic API.
- Supply scheme examples for voice-only, spoken-label, cue-only, combined,
  before/after, and folded-state presentation.
- Verify user overrides globally, for =org-mode=, for a derived mode, for the
  Org module, and for one buffer.
- Compare output and latency with the compatibility path.

Deliverable: the motivating example works end to end and validates the
contract before broad module migration.

Implemented details:

- Org interprets the registered core =heading= role, =level= attribute,
  =folded= state, and =focus-entered=, =state-changed=, and =object-changed=
  events rather than creating synonymous module-private identifiers;
- =org-mode= buffers identify their aural module and fontified heading lines
  carry namespaced facts for continuous reading and point inspection;
- heading facts use effective structural depth, refresh folded state after
  visibility changes, and capture navigation, state-change, or edit occasion
  with source mode ancestry before speech enters the scratch buffer;
- heading navigation and editing now queue their compatibility movement or
  open cue after content in the same concrete plan; non-heading Org feedback
  retains its previous direct path and ordering;
- six selectable built-in examples, =org-voice-only=, =org-spoken-label=,
  =org-cue-only=, =org-combined=, =org-before-after=, and
  =org-folded-state=, demonstrate each motivating modality without changing
  the default choice;
- their data-only definitions load before personal scheme data, so a saved Org
  scheme remains selectable at startup without eagerly loading Org itself;
- the combined example resolves a spoken label, concrete =section= resource,
  ACSS-backed content voice, heading text, and reset codes into one strict
  queue;
- focused tests exercise global, =org-mode=, derived-mode, =org= module, and
  one-buffer user selectors against real Org facts and context.

The compatibility comparison preserves the observable default order of
heading text followed by =large-movement=.  A 2,000-iteration, source-loaded
planning benchmark on the development machine, excluding speech and audio
I/O, measured 820.6 microseconds per heading for separate compatibility speech
and icon plans and 655.8 microseconds for the single semantic plan.  This is a
local regression baseline, not a cross-machine performance guarantee.

Checks: 37 focused Org ERT tests and 142 combined Org, semantic, rule-engine,
resource, scheme, transport, tooling, sound, and voice tests with no unexpected
results; 27 source-loaded semantic traces matching the golden output;
=check-parens=, =checkdoc=, byte compilation, =org-lint=, and
=git diff --check=.  The normal Emacs 31 gates remain unavailable in the
current Emacs 30.2 environment as recorded for Slice 6.

** DONE Slice 9: Portable Spatial Presentation
CLOSED: [2026-07-26 Sun]

- Implement stereo balance for speech and cues where supported.
- Add capability reporting and centred fallback.
- Add global disable, independent speech/cue controls, maximum separation, and
  user remapping.
- Prevent double spatialization of assets marked as pre-spatialized.
- Validate ordering, intelligibility, mono output, and local-player versus
  speech-server behavior.
- Prototype azimuth, elevation, and distance only after the stereo contract is
  stable.

Deliverable: schemes can position speech and cues portably without requiring
3D support.

Implemented details:

- schema-v1 spatial values accept normalized =:balance= from =-1.0= through
  =+1.0= or listener-relative =:azimuth= from =-180= through =+180= degrees;
  invalid ranges, mixed coordinates, unknown properties, and spatial pauses
  fail validation;
- azimuth reduces predictably through =sin(azimuth)=, while elevation,
  distance, width, and motion remain deferred until their units, capabilities,
  and intelligibility contract are reviewed;
- concrete speech, content, and cue records freeze requested space, compiled
  balance, backend capability, and reductions before queueing;
- speech and ordered-cue adapters can advertise and queue stereo balance;
  unsupported or explicitly mono transports produce audible centred output
  and an explainable degradation;
- standalone local SoX cues use a two-channel balance remix, while current
  Pulse/PipeWire sample and speech-server paths centre unless an ordered
  adapter supplies spatial support;
- speech balance brackets exactly one speech action or content run and resets
  to centre before the next ordered action;
- global enable, independent speech/cue enable, final mono output, maximum
  separation, direction reversal, unilateral-side collapse, centring, and a
  custom remapping function are user controls applied during concrete
  compilation;
- resolved assets inherit spatial metadata from their actual owning pack, and
  the bundled =3d= pack's =pre-spatialized= assets are never positioned a
  second time;
- explanation output and the capability command expose compiled balance,
  capability, and reduction reasons, and the accessible editor creates balance
  or azimuth data without raw plist editing.

Checks: 78 focused rule, resource, transport, tooling/editor, and sound ERT
tests and 153 combined Org, semantic, rule-engine, resource, scheme,
transport, tooling, sound, and voice tests with no unexpected results; 27
source-loaded semantic traces matching the golden output; source-only startup
smoke; a real SoX mono input through the documented two-channel remix;
=check-parens=, =checkdoc=, byte compilation, =org-lint=, and
=git diff --check=.  Byte compilation and =checkdoc= retain only the
pre-existing warnings in the legacy sound and tooling files.  The normal
Emacs 31 gate remains unavailable because the current environment provides
Emacs 30.2; =make unit-test= stops at its explicit version check.

** DONE Slice 10: Representative Module Migration
CLOSED: [2026-07-26 Sun]

Migrate a small cross-section:

- Notmuch for field voices, message states, and notifications;
- Agent Shell for existing processing-icon variables;
- one programming mode for face-derived and semantic voice interaction;
- one completion interface for rapid event presentation.

Use these migrations to refine extension registration and compatibility.
Migrate remaining modules later in small functional batches.

Deliverable: the model handles persistent text, transient events, states, and
legacy face mappings outside Org.

Implemented details:

- a package-independent representative vocabulary registers shared message,
  field, refresh, source-construct, boundary, candidate, selection, and
  acceptance semantics at startup, plus Agent Shell-owned session, response,
  thought, tool, permission, and processing semantics;
- Notmuch search and show fields carry persistent =field= and =field-kind=
  facts without losing their existing face-derived voices, while message
  speech and status cues share unread, flagged, and attachment state and
  refresh completion or failure is a semantic notification;
- Agent Shell session and viewport buffers identify their module, lifecycle
  events describe processing start, completion, and failure, and content
  describes response, thought, tool, or permission intent;
- Agent Shell's existing processing-start, processing-end, task, warning,
  verbosity, and speech controls remain the compatibility presentation, so
  current configurations retain their exact output while schemes can match
  the submitted intent;
- Python navigation supplies source-construct role, language syntax role, and
  boundary-entry facts around its existing face-aware line speech and
  paragraph cue;
- Vertico navigation and acceptance supply candidate, selected, accepted, and
  zero-based position facts around the existing rapid speech and cues;
- dynamically scoped facts, module, occasion, and captured context now reach
  both direct compatibility icons and text speech before either path resolves
  a concrete plan;
- Notmuch and Agent Shell use distinct native-advice identities, allowing
  their visual-line wrappers to coexist while retaining independently
  reversible enable and disable behavior;
- no integration changes its established text, icon choice, ordering,
  filtering, speech-level option, or face-to-voice fallback when no semantic
  rule matches.

Checks: 227 focused registry, transport, Notmuch, Agent Shell, Python, and
Vertico ERT tests and 345 combined Org, semantic, rule-engine, resource,
scheme, transport, tooling, representative-module, sound, and voice tests
with no unexpected results; 27 source-loaded semantic traces matching the
golden output; =check-parens=, =checkdoc=, byte compilation, =org-lint=, and
=git diff --check=.  The normal Emacs 31 gate remains unavailable because the
current environment provides Emacs 30.2; =make unit-test= stops at its
explicit version check.

** DONE Slice 11: Audit, Documentation, and Rollout
CLOSED: [2026-07-26 Sun]

- Replace or supplement =utils/count-icons.pl= with registry-aware audits.
- Generate semantic, scheme-author, module-author, sound-pack, and voice-palette
  references from the registries.
- Document the cascade, merge operators, render phases, queue contract, and
  spatial fallback.
- Add migration guidance for direct icon calls and =personality= properties.
- Add release notes when user-facing scheme selection becomes available.
- Mark completed slices and record their check results in this document.

Deliverable: enforceable contracts, maintained documentation, and a staged
path for the remaining integrations.

Implemented details:

- =utils/emacsvox-aural-audit.el= validates semantic, resource, and scheme
  registry cross-references, every registered pack, and every built-in scheme;
- its non-evaluating Lisp reader inventories literal =emacsvox-icon= and
  =emacsvox-queue-icon= calls while ignoring comments, strings, and quoted
  data, rejects unregistered literal cues, reports dynamic calls separately,
  and explicitly rejects evaluating =#.= reader forms;
- =make aural-audit= supplements the historical textual
  =utils/count-icons.pl= report with registered intent, provider validation,
  source parsing, and generated-document drift checks;
- =make aural-reference= atomically regenerates
  =etc/aural-presentation-reference.org=, with deterministic tables for
  semantics, occasions, built-in schemes, module fragments, cues, sound packs,
  and voice palettes;
- the generated reference also documents user controls, the cascade and
  specificity vector, merge operators, render phases, concrete queue contract,
  spatial fallback, scheme authoring, module extension registration, sound
  pack and voice palette authoring, compatibility migration, and staged
  rollout;
- focused ERT coverage enforces safe reading, unregistered-cue failure,
  deterministic generation, stale-reference detection, and the complete
  current-tree audit; the standard test runner loads those checks;
- =Readme.org= makes schemes and the generated reference discoverable, and the
  current release notes describe the compatibility-preserving user-facing
  rollout.

The clean audit parsed 1,667 literal cue calls and 101 dynamic calls.  All 58
literal cue identifiers in use belong to the 76-entry registered cue
vocabulary.  Dynamic calls remain reported rather than guessed statically;
their concrete values retain runtime registry fallback and validation.

Checks: 6 focused audit/generation ERT tests and 351 combined Org, semantic,
rule-engine, resource, scheme, transport, tooling, representative-module,
audit, sound, and voice tests with no unexpected results; all 27
source-loaded semantic traces matching the golden output; clean
=make aural-audit= and TTS namespace audits; idempotent reference generation;
=check-parens=, =checkdoc=, byte compilation, =org-lint=, and
=git diff --check=.  The normal full suite remains unavailable because
=make unit-test= requires Emacs 31 while this environment provides Emacs 30.2.
The independent name audit continues to report eight pre-existing legacy names
in the Windows Speech module and its tests; this slice neither introduced nor
changed them.

* Test Gates

Every behavior-changing slice must run checks proportional to its scope:

1. Dedicated semantic, scheme, voice, resource, and render-plan ERT tests.
2. Full =make unit-test=.
3. =make trace-test= when speech or cue traces may change.
4. Complete Lisp byte compilation when public Lisp interfaces change.
5. Registry, scheme, resource-pack, and documentation audits.
6. Startup smoke tests after changing default scheme or provider selection.
7. Backend-specific integration tests when concrete queue plans change.

The dedicated tests must cover:

- unique registrations, ownership, valid fields, intent, and fallback;
- selector matching and derived-mode ancestry;
- presentation-occasion matching without changing semantic identity;
- origin-layer and specificity ordering;
- deterministic ties independent of hash or registration order;
- independent heading-level and folded-state composition;
- ordered before, content, and after actions;
- prepend, append, replace, remove, suppress, and silence;
- inheritance and cycle detection;
- buffer isolation and explicit module context;
- existing personality precedence and face fallback;
- named and generated ACSS voices;
- required sound coverage and optional extension fallback;
- unknown cues, semantics, voices, rules, and theme assets;
- clean cache rebuilding on provider changes;
- source-context capture before scratch-buffer processing;
- concrete resolution before queueing;
- queued and immediate equivalence;
- SoX, Pulse/PipeWire, and speech-server adapters;
- stereo, mono, unsupported spatial capability, and pre-spatialized assets;
- compatibility of existing icon calls, text properties, voices, and sound
  directories;
- explanation provenance sufficient to reproduce every winning decision.

* Resolved Decisions

The decisions required for Slice 6 are:

1. Resolve and compile at =tts-speak= and notification entry, attaching the
   frozen concrete decision to each source-text run while allowing normal
   cleanup to determine the final text payload.
2. Use one strict ordered speech-server queue for a complete multi-action
   presentation.  Permit local playback only for a standalone one-cue
   compatibility presentation.
3. Name Pulse/PipeWire samples
   =emacsvox-PACK-CUE-CONTENT-DIGEST=, own only successful uploads, and unload
   obsolete owned identifiers.

For Slice 7, retain schema version 1 and add optional boolean =:enabled= to
rule data.  Omission means enabled; disabled rules validate but do not enter
the cascade.

For Slice 9, use normalized balance =-1.0..+1.0= as the portable coordinate,
negative left and positive right.  Listener-relative azimuth
=-180..+180= degrees reduces with =sin(azimuth)= so front and rear remain
centred.  Apply user remapping and maximum separation after coordinate
reduction.  Speech and cues use the same coordinate contract but retain
independent controls and transport capabilities.  Unsupported and mono output
remain audible at the centre, and pre-spatialized assets are not positioned
again.

* Open Decisions

These decisions must be resolved before their corresponding implementation
slice:

1. Whether critical alerts may be completely suppressed or require an
    alternative presentation.
2. The ordering policy required before minor-mode selectors are enabled.
3. Whether future integrations need additional occasions and whether those
    are core or module extensions.

Initial recommendations are:

- use versioned declarative Lisp data with no evaluation;
- resolve all context and resources in Emacs before queueing;
- treat existing personalities as presentation hints, not semantic identity;
- make stereo balance the portable spatial baseline;
- use explicit action operations rather than implicit last-writer behavior;
- warn once per session for unknown legacy inputs during migration.

* Risks

- Calling everything an icon would preserve a sound-first design and make
  speech or voice alternatives second-class.
- Treating visual faces as semantic identity would couple accessibility
  behavior to display styling and package implementation details.
- A rule engine without explicit composition would make independently useful
  facts such as heading level and folded state conflict.
- Context inferred after text enters the TTS scratch buffer would select the
  wrong mode or buffer rules.
- Backend-specific semantic resolution would make presentation depend on the
  selected player.
- Queueing unresolved cue names would push Emacs configuration and fallback
  policy into the speech server.
- Runtime spatialization of the existing =3d= assets could position them
  twice.
- Unstable or excessive spatial speech could reduce intelligibility.
- Requiring every scheme to cover every extension would make third-party
  schemes impractical.
- An editor that cannot explain rule provenance would make a powerful cascade
  impossible to debug.
- Migrating every caller at once would obscure semantic review and make
  regressions difficult to isolate.

The staged plan addresses these risks with a registered semantic vocabulary, a
pure and explainable rule engine, concrete pre-queue resolution, capability
fallback, an Org vertical slice, and small subsequent migrations.

* Definition of Done

The migration is complete when:

- registered semantic facts are the documented source of truth;
- modules describe meaning without choosing a modality or file;
- schemes can compose speech, voice, cues, pauses, and spatial style in
  explicit before, content, and after phases;
- users can inspect and override presentation globally and by module, major
  mode, combined context, derived mode, or buffer;
- independent rules such as heading level and folded state compose
  deterministically;
- existing ACSS personalities and sound packs are providers within the common
  system;
- the concrete render plan is resolved before queueing;
- the speech server contains no scheme, mode, module, or semantic resolution;
- supported backends honor the same ordering, silence, and fallback behavior;
- portable spatial presentation degrades predictably and can be disabled;
- training and explanation commands identify what was presented and why;
- schemes, resource packs, voice palettes, and semantic extensions validate;
- existing icon calls, =personality= properties, face mappings, and bundled
  directories remain functional through documented compatibility adapters;
- the Org vertical slice and representative module migrations pass all
  required test gates;
- public documentation explains authoring, customization, migration, and
  backend capability behavior.
