#+title: Emacs 31 Advice Migration Plan
#+author: Emacsvox Contributors
#+date: <2026-07-24 Fri>

* Goal

Replace the remaining legacy and partially converted advice with correct native
Emacs advice for Emacs 31.  Preserve Emacsvox's spoken interaction while the
conversion proceeds, then remove the temporary compatibility bridge from
=emacsvox-preamble.el=.

The bridge is migration scaffolding.  It is not the final advice architecture.

* Baseline

The initial raw inventory of the =lisp/= tree is:

| Pattern             | Occurrences | Files |
|---------------------+-------------+-------|
| =(defadvice=        |         439 |   127 |
| =ad-get-arg=        |         129 |    39 |
| =ad-set-arg=        |           4 |     3 |
| =ad-do-it=          |          69 |    28 |
| =ad-return-value=   |          46 |    25 |
| =(advice-add=       |        1404 |   149 |
| =ems-interactive-p= |        1644 |   175 |

These are textual counts, so they include macro templates, comments, and other
forms that require classification.  In particular, some backquoted macros
generate =defadvice= forms and are not handled by the existing converter.

The current converter is not yet safe for general use.  For example, its sample
output retains =ad-get-arg= in a function whose arguments are discarded.  It
must become conservative and test-driven before further bulk use.

* Progress Checkpoint

As of the completed core-file migration and built-in integration slices through
RST, RefTeX, BS, BibTeX, and Bookmark on 2026-07-24 on branch =bart=:

- the Emacs 31 suite contains 559 passing ERT tests;
- all 27 structured core speech traces match;
- the pinned Emacspeak reference is commit
  =7482f8e2713b5b8ea87132d9d2fcea6664ff4037=;
- the complete 236-file default Lisp object set byte-compiles with Emacs
  31.0.90, with warnings but no errors;
- the real =~/bin/emacsvox-test= launcher loads Emacsvox under Emacs 31.0.90;
- =lisp/emacsvox-loaddefs.el= is generated, ignored, and not tracked;
- =lisp/emacsvox-advice.el= contains no =defadvice=, =ad-get-arg=,
  =ad-set-arg=, =ad-do-it=, or =ad-return-value= forms;
- =lisp/emacsvox-org.el= contains no =defadvice=, positional compatibility
  accessors, or bridge-era =ems--= advice helpers.
- =lisp/emacsvox-outline.el= contains no =defadvice= or positional
  compatibility accessors, including in its optional Foldout integration.
- =lisp/emacsvox-eww.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-gnus.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-ibuffer.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-comint.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-tab-bar.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-c.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-eshell.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-which-key.el= contains no =defadvice=, positional
  compatibility accessors, bridge-era =ems--= advice helpers, or implicit
  interactive-target checks.
- =lisp/emacsvox-eterm.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-ispell.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-widget.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-rst.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-reftex.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-bs.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-bibtex.el= contains no =defadvice=, positional compatibility
  accessors, bridge-era =ems--= advice helpers, or implicit interactive-target
  checks.
- =lisp/emacsvox-bookmark.el= contains no =defadvice=, positional
  compatibility accessors, bridge-era =ems--= advice helpers, or implicit
  interactive-target checks.

The syntax-aware repository audit currently reports:

| Pattern             | Occurrences | Files |
|---------------------+-------------+-------|
| =defadvice=         |         269 |    96 |
| =ad-get-arg=        |          50 |   n/a |
| =ad-set-arg=        |           2 |   n/a |
| =ad-do-it=          |          31 |   n/a |
| =ad-return-value=   |          21 |   n/a |
| =advice-add=        |        1302 |   157 |
| =ems-interactive-p= |        1340 |   176 |

** Remaining Emacs-bundled inventory

For progress reporting, "native advice installed" and "fully final-form" are
different states:

- *legacy or positional* means a module still contains executable
  =defadvice= or uses =ad-get-arg=, =ad-set-arg=, =ad-do-it=, or
  =ad-return-value=;
- *helper cleanup* means the module uses =advice-add= but still has
  bridge-recognized =ems--= advice helpers or argument-less
  =ems-interactive-p= checks;
- *fully final-form* means advice is directly registered under distinct
  =emacsvox--= names, interactive targets are explicit, and none of the
  legacy or bridge compatibility constructs remain.

The previous checkpoint used "converted" for some modules at the
native-advice-installed stage.  Those modules remain in this inventory until
they satisfy the final-form gate.

As of 2026-07-24, 52 Emacs-bundled integration modules remain: 26 legacy or
positional modules and 26 helper-cleanup modules.

| State                | Modules |
|----------------------+---------|
| legacy or positional | =buff-menu=, =cmuscheme=, =cperl=, =custom=, =ediff=, =eglot=, =enriched=, =epa=, =erc=, =flyspell=, =gomoku=, =gud=, =lua=, =message=, =newsticker=, =nxml=, =perl=, =python=, =ruby=, =sh-script=, =speedbar=, =supercite=, =tetris=, =todo-mode=, =transient=, =treesit= |
| helper cleanup       | =arc=, =calc=, =calculator=, =desktop=, =dictionary=, =dired=, =eudc=, =flymake=, =hideshow=, =ido=, =info=, =kmacro=, =make-mode=, =man=, =package=, =re-builder=, =rmail=, =ses=, =sgml-mode=, =solitaire=, =sql=, =tar=, =tcl=, =tempo=, =texinfo=, =woman= |

=emacsvox-entertain.el= is tracked separately because it combines built-in
Doctor and Dunnet support with optional Hangman support.  Emacsvox core files
and removal of the compatibility bridge are also separate from this
integration-module count.

Converted core areas include movement and editing, buffers and windows,
minibuffer and completion, search and replacement, file and process lifecycle,
messages, overlays, timers, source navigation, property search, Help
navigation, IELM, rectangle editing, comments, registers, button creation, and
tooltips.  All advice registered by =lisp/emacsvox-advice.el= now uses direct,
named native advice and bypasses the compatibility bridge.  Its three remaining
=ems--= definitions are ordinary utility functions, not advice helpers.

This phase also corrected two bridge-era semantic regressions: native
interactive specifications now restore the intended spoken prompts for
=local-set-key=, =global-set-key=, and =modify-syntax-entry=; and Ange FTP
progress advice now preserves the processed string returned by the original
function rather than returning the result of speech output.

The built-in integration slices converted Compilation, Windmove, FFAP, IDO,
Diff Mode, Project, Flymake, Package, Info, Debugger, table editing, Proced,
View, Calendar, Wdired, Tabulated List, Net Utils, Forms, Xref, and Org.
Outline and its optional Foldout integration, and EWW with its SHR rendering
integration, Gnus, Ibuffer, Comint, Tab Bar, C Mode, Eshell, and Which-Key are
also converted, along with Eterm, Ispell, Widget, RST, RefTeX, BS, BibTeX, and
Bookmark.
Each module has direct-registration and target-aware feedback tests, and each
conversion is an independently revertible commit.  The modules explicitly
listed as clean earlier in this checkpoint are completely independent of the
compatibility bridge; modules in the inventory above still require final-form
work.  Repository-wide, 40 files still contain positional bridge compatibility
symbols.

The Debugger conversion uses native =:filter-return= advice so interactive
evaluation can speak the result without changing the value returned to callers.
The table conversion also corrected converter regressions where deletion and
newline-and-indent advice invoked the original command twice.  Those wrappers
now give feedback in upstream order and invoke the original exactly once.

The View conversion restored a dead generated scroll-advice family and targets
the View scrolling commands that exist in Emacs 31.  The Calendar conversion
likewise replaces obsolete command names with current Emacs 31 Calendar and
Diary APIs.  It also corrects duplicate original calls in =calendar-read= and
Diary marking while preserving their return values and message-silencing
semantics.  The Tabulated List conversion also corrects the misspelled
=select-objet= auditory icon to the existing =select-object= icon.

The Forms conversion replaces obsolete =forms-kill-record= advice with the
current =forms-delete-record= command, removes the dead generic =forms-search=
target, and corrects a converted around-wrapper that called =forms-next-field=
twice.  The Xref conversion removes the undefined =xref-find-regexp= target and
a duplicate obsolete-alias registration while retaining target-aware feedback
for callable aliases.

The Org conversion is split into structure, agenda/table, editing, capture, and
completion/export commits.  It preserves unconditional table-cell and capture
lifecycle feedback where Org invokes commands internally.  It also corrects
converter regressions that called =org-delete-char= and =org-complete= multiple
times.  Both wrappers now call the original exactly once and preserve its
result.

Emacs 31 Org no longer defines =org-complete=.  Current Org completion reaches
the existing Emacsvox =completion-at-point= advice through =complete-symbol=.
The Org-specific legacy wrapper is therefore installed only when an older Org
actually defines =org-complete=, avoiding a phantom advised command and
duplicate current completion feedback.  Org export advice now receives the
native =org-export--dispatch-action= and =org-export-to-file= arguments
directly instead of reading positional bridge state.

The Outline conversion keeps hide/show announcements target-aware and gives
=outline-hide-sublevels= its explicit =levels= argument.  Its
=outline-flag-region= wrapper now calls the original exactly once, preserves
the original return value, and mirrors Outline invisibility with native
arguments.  Foldout advice remains deferred until Foldout loads.

The EWW conversion is split into navigation, URL and media handling, SHR
rendering, and lifecycle commits.  It targets the Emacs 31 SHR renderer
=shr-tag-i= instead of the obsolete =shr-tag-it=, omits nonexistent
=shr-tag-form= and =eww-quit= targets, and installs the local =shr-tag-math=
implementation before its advice.  Unique native advice names prevent multiple
advice on =shr-tag-div= and =eww-reload= from replacing each other.

The URL conversion expresses user-agent transformation as =:filter-return= and
Google URL cleanup as =:filter-args=.  The rendering conversion corrects
bridge-era wrappers that called =shr-tag-table-1= and =shr-tag-div= twice.
Lifecycle wrappers now call ordinary reload and readable operations exactly
once and preserve their original return values.  Moving EWW setup after its
renderer-filter definition also makes the module load correctly as source in a
fresh Emacs.

The Gnus conversion is split into startup and group commands, summary marking
and navigation, summary article display, and article, server, and
authentication commits.  It removes advice for the obsolete
=gnus-summary-mark-as-unread-forward= and
=gnus-summary-mark-as-unread-backward= commands, moves button feedback to
=gnus-summary-button-forward=, and moves server-edit feedback from the
now-variable =gnus-server-edit-buffer= to =gnus-server-edit-server=.

Gnus around advice now calls summary exit, subject movement, and XOAuth
credential operations exactly once and preserves their original return values.
The two native advice attached to =gnus-summary-show-article= have distinct
names, so they coexist.  Optional XOAuth advice is installed only after its
provider defines =auth-source-xoauth2--file-creds=.

The Ibuffer conversion is split into Emacs 31 target contracts, entry and
navigation, marking and buffer operations, filtering and sorting, and remaining
BS utilities.  It removes advice for the obsolete =ibuffer-limit-disable=,
=ibuffer-quit=, and misspelled =ibuffer-occur-*-occurence= commands.  Current
filter disabling and generic Occur advice already provide the corresponding
feedback.  Ibuffer quitting now uses mode-scoped =quit-window= advice with a
distinct native advice name, allowing it to coexist with generic
=quit-window= feedback.

Ibuffer bury, filter-group pop, and filter-group yank wrappers now call their
original operations exactly once for both interactive and programmatic calls
and preserve the original result.  Filter-group kill feedback receives its
native =name= argument instead of relying on obsolete dynamic scope.  Duplicate
legacy definitions for predicate filtering, sorting-mode toggling, and filename
copying are collapsed to one registration each.

The Comint conversion is split into Emacs 31 target contracts,
navigation/history, input, output/process, and completion/history-display
commits.  It removes advice for the obsolete =comint-dynamic-complete= and
=comint-kill-output= commands.  Emacs 31 completion now reaches the existing
Emacsvox =completion-at-point= advice through =comint-completion-at-point=, and
=comint-delete-output= provides the current output-deletion command.

Comint magic-space, previous-argument insertion, delete-or-EOF, and process
output wrappers now call their originals exactly once and preserve the original
result.  In particular, each process chunk is inserted only once before
autospeech.  The accessible input-history display replaces the stock display
only for interactive calls; programmatic calls still invoke the stock function
once.

The Tab Bar conversion is split into Emacs 31 target contracts, lifecycle
commands, and Tab Switcher commits.  It removes the obsolete =tab-bar-list-*=
targets in favor of the current =tab-switcher-*= command family, retains the
callable =tab-list= alias, and covers the separate
=tab-switcher-backup-unmark= command introduced by the current interface.
Explicit interactive targets prevent aliases such as =tab-next= and
=tab-bar-switch-to-next-tab= from producing duplicate feedback.  Closing a tab
by name now receives the native =name= argument directly instead of reading
positional bridge state.

The C Mode conversion is split into Emacs 31 target contracts, deletion and
electric commands, navigation, and formatting and toggle commits.  It replaces
the misspelled =c-awk-end-of-defunm= target with
=c-awk-end-of-defun= and removes the =c-toggle-auto-state= compatibility alias
that Emacs removed in version 29.  AWK advice is now installed only after
=cc-awk= loads, so the integration does not create phantom commands.

Deletion feedback now uses native =:before= advice.  This corrects a converter
regression where =c-electric-delete-forward= called the original deletion
twice, while preserving the reference character selection and feedback order.
All remaining C navigation, formatting, and toggle advice uses direct named
registration and explicit interactive targets.

The Eshell conversion is split into selection and history, then editing, output,
and completion commits.  It replaces the obsolete =eshell-kill-output= target
with =eshell-delete-output=.  Emacs 31 line-start and TAB handling now reach the
existing generic Emacsvox advice through =move-beginning-of-line= and
=completion-at-point=, so the obsolete =eshell-bol= and =eshell-pcomplete=
registrations are removed.  Optional =eshell-toggle= advice is deferred until
that third-party library loads and no longer creates phantom commands.

Eshell deletion feedback now uses native =:before= advice, correcting bridge-era
wrappers that invoked the original deletion twice.  The duplicate
=eshell-insert-process= feedback path is collapsed to one registration, and Lisp
symbol completion calls the original exactly once while preserving its return
value.  Advice for Eshell submodules is installed only after the corresponding
=em-hist=, =em-prompt=, =em-rebind=, or =em-cmpl= feature loads.

The Which-Key conversion targets the API bundled with Emacs 31; all 14 advised
functions are present and current.  The pinned Emacspeak checkout has no
corresponding Which-Key module, so the existing Emacsvox implementation is the
behavioural contract.  Internal =which-key--show-page= calls continue to speak
unconditionally when automatic speech is enabled, while popup, paging, help,
undo, and display commands now use explicit interactive targets.

The Eterm conversion covers all 21 advised targets, which remain present and
current in Emacs 31.  Command, mode, history, prompt, job-control, completion,
and terminal-output advice now uses direct named registration.  Launch and
input-ring notices retain their reference-defined unconditional behaviour,
while interactive command feedback names its target explicitly.

The completion wrapper now calls =term-dynamic-complete= exactly once, speaks
only text inserted by that call, and preserves the original result.  The
=term-emulate-terminal= wrapper receives its native =proc= and =str= arguments
directly, retains the reference live-process gate and feedback cases, calls the
terminal emulator exactly once, and preserves its result.

The Ispell conversion covers all six current Emacs 31 targets.  Command-loop
feedback now receives the native misspelling, choice, and buffer-position
arguments directly.  Buffer, region, comments-and-strings, help, and word
operations call their originals exactly once and preserve their return values.
In particular, =ispell-word= no longer invokes the spelling operation up to
three times while gathering interactive feedback.

The Widget conversion covers all nine current Emacs 31 targets.  Motion,
button, text-conversion, and setup advice now uses direct named registration
and explicit interactive targets.  Echo-help, end-of-line, and ordinary button
presses call their originals exactly once; the EWW custom button executor
retains its reference-defined replacement semantics.  Text conversion
preserves the widget returned by the original operation.  Widget setup now
repairs the actual field and text keymaps rather than iterating over quoted
keymap variable names, restoring the intended bindings.

The RST conversion covers 29 current Emacs 31 targets and omits the removed
=rst-promote-region= command.  Navigation, compilation, TOC, marking, list, and
paragraph feedback now uses direct named registration and explicit interactive
targets.

The RefTeX conversion covers 26 current Emacs 31 targets and omits the removed
=reftex-index-toggle-labels= and =reftex-index-toggle-file-boundary= commands.
Highlight feedback receives the native =begin= and =end= arguments rather than
reading positional bridge state.

The BS conversion covers all 23 current Emacs 31 targets.  Mode setup retains
its reference-defined unconditional voice-lock behaviour, while selection,
marking, deletion, navigation, and cycling feedback uses direct target-aware
advice.

The BibTeX conversion covers 22 current Emacs 31 targets.  It moves feedback to
the current entry-navigation, OPT/ALT removal, field-killing, =bibtex-String=,
and =bibtex-Preamble= APIs, and collapses the duplicated
=bibtex-InProceedings= definition to one registration.

The Bookmark conversion covers 18 current Emacs 31 targets.  Annotation
confirmation now follows =bookmark-edit-annotation-confirm= rather than its
obsolete alias.  Removed bookmark-name insertion commands and the removed
Bookmark-specific quit command are omitted instead of advising noninteractive
or global replacements, and the duplicated annotation-edit definition is
collapsed to one registration.

Continue by migrating remaining built-in integration modules before optional
third-party integrations.  The compatibility bridge remains necessary until
the repository-wide legacy audit is clean.

* Working Principles

1. Preserve behaviour before improving or simplifying it.
2. Convert in small, coherent commits that can be reverted independently.
3. Prefer =:before= and =:after= advice when they express the behaviour exactly.
4. Use =:filter-args= for argument transformation and =:filter-return= for
   return-value transformation where practical.
5. Use =:around= only when the original call genuinely needs explicit control.
6. Make the advised interactive command explicit; do not depend on
   =called-interactively-p= inside an advice helper.
7. Automatically convert only patterns whose semantics are covered by tests.
8. Manually review conditional calls, multiple =ad-do-it= forms, argument
   mutation, return-value mutation, and advice ordering.
9. Do not commit generated =lisp/emacsvox-loaddefs.el= changes as part of advice
   migration slices.

* Testing Strategy

** Emacs 31 ERT suite

Add a batch ERT suite that does not require a real speech server.  Tests record
speech and auditory events using test doubles.  The initial suite covers:

- interactive and non-interactive invocation;
- nested and recursive interactive commands;
- =:before=, =:after=, and =:around= ordering;
- argument inspection and replacement;
- return-value inspection and replacement;
- autoload and function redefinition behaviour;
- advice membership, removal, and module reload;
- timers, overlays, font locking, and redisplay-sensitive paths where they can
  be exercised deterministically in batch mode.

The standard entry point will be similar to:

#+begin_src sh
make test EMACS=/home/bart/opt/emacs-31/bin/emacs
#+end_src

** Structured speech traces

Compare semantic events rather than synthesized audio.  A trace may contain:

#+begin_src emacs-lisp
((speak "Beginning of buffer")
 (icon select-object)
 (voice annotate)
 (stop))
#+end_src

Scenarios should also capture relevant editor state:

- buffer contents;
- point and mark;
- selected buffer and window;
- command return value;
- messages;
- whether invocation was interactive.

Normalize dynamic values such as paths, timestamps, generated buffer names, and
version strings before comparison.

** Three-way behavioural comparison

Use three implementations when practical:

1. A pinned fresh checkout of the original Emacspeak codebase, as an additional
   source of intended behaviour.
2. Emacsvox at the bridge baseline, as the strict local regression reference.
3. The converted Emacsvox implementation, as the candidate.

Record the reference Emacspeak commit and Emacs version.  Do not treat current
upstream Emacspeak automatically as correct: Emacspeak and Emacsvox may have
intentionally diverged, and upstream behaviour may have changed since the fork.

The normal test suite must remain self-contained and offline.  Reference tests
will be optional and accept an explicit checkout, for example:

#+begin_src sh
make reference-test \
  EMACS=/home/bart/opt/emacs-31/bin/emacs \
  EMACSPEAK_DIR=/path/to/pinned/emacspeak
#+end_src

Commit normalized golden traces for a small set of valuable core scenarios.
Use the live checkout comparison to investigate differences and refresh a trace
only after reviewing the behavioural change.

Audio waveform comparison is not a primary test: synthesis engines, timing,
rate, and engine versions make it noisy.  TTS protocol traces may be added later
when Lisp-level semantic traces are insufficient.

** Manual acceptance

Automated tests establish which speech operation occurred, but not whether the
interaction sounds natural.  At core milestones, run a short audible checklist
covering movement, editing, buffer boundaries, copying, minibuffer operation,
Dired, completion, overlays, and font locking.

* Migration Slices

** 1. Record this plan

Commit this document independently so the agreed strategy remains available to
future contributors.

** 2. Establish the ERT harness

Add the batch runner and Make target.  Capture the compatibility bridge and
interactive-command behaviour as regression tests before changing them.

** 3. Add speech-event differential testing

Add a reusable structured event recorder, scenario format, normalization, and
the optional pinned-Emacspeak runner.  Begin with approximately ten core
scenarios rather than attempting every optional package.

** 4. Harden conversion and audit tooling

Add ERT tests for parsing, classification, generation, dry runs, and failure
cases.  Detect backquoted advice-generating macros.  Convert safe forms only and
produce a deterministic manual-review report for complex forms.

** 5. Make interactive targets explicit

Introduce the final target-aware form, for example:

#+begin_src emacs-lisp
(ems-interactive-p 'kill-word)
#+end_src

Temporarily retain the argument-less compatibility behaviour for unmigrated
code.  Fully converted native advice helpers use the package-private
=emacsvox--= prefix, while the bridge remains limited to converted helpers with
the legacy =ems--= prefix.  This makes newly corrected advice bypass the bridge
without another wrapper or marker property.

** 6. Remove visible legacy redefinition warnings

Convert and test the Dired/Locate advice and the Mail advice in separate commits.
This validates autoload and redefinition behaviour early and removes the
=ad-handle-definition= messages observed during manual testing.

** 7. Convert core advice by behavioural area

Use separate commits for coherent areas such as:

- movement and editing;
- buffers and windows;
- minibuffer and completion;
- search and replace;
- files and processes;
- overlays, font locking, timers, and redisplay.

Avoid converting all of =emacsvox-advice.el= in one commit.

** 8. Convert remaining modules

Convert Emacs built-in integrations first, followed by optional third-party
packages.  Prefer one module, or a small closely related group, per commit.

** 9. Remove the bridge

When the repository-wide legacy audit is clean, remove:

- the advice on =advice-add=;
- =ad-get-arg= and =ad-set-arg= compatibility definitions;
- dynamic converted-advice argument and return-value state;
- generated wrapper storage;
- compatibility-only interactive target state.

Do this in its own commit after the full suite and manual core checklist pass.

* Commit Gate

Before each migration commit:

1. Run the Emacs 31 ERT suite.
2. Byte-compile every touched Lisp file with Emacs 31.
3. Run the legacy-symbol audit on the converted files.
4. Run an integration load test where applicable.
5. Inspect the exact staged diff and exclude generated or unrelated files.
6. Record remaining warnings or manual-test requirements in the handoff.

At major boundaries, compile the complete default Emacsvox object set and run a
fresh startup smoke test.

* Completion Criteria

The migration is complete when:

- no executable legacy =defadvice= remains;
- no converted advice depends on =ad-get-arg=, =ad-set-arg=, =ad-do-it=, or
  =ad-return-value=;
- interactive advice uses the explicit, tested target mechanism;
- advice can be inspected, removed, and reloaded through native APIs;
- the compatibility bridge is absent;
- all automated tests pass under Emacs 31;
- the default object set byte-compiles under Emacs 31;
- startup and the audible core acceptance checklist pass;
- documentation no longer claims a stronger migration status than the audit
  supports.
