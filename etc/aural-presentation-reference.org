#+title: Emacsvox Aural Presentation Reference
#+author: Emacsvox Contributors
#+options: toc:3

# This file is generated by utils/emacsvox-aural-audit.el.
# Do not edit it directly; run make aural-reference.

* Using Aural Presentation Schemes

A semantic fact says what an object or event means.  A scheme decides whether that fact is conveyed by speech, voice, a cue, a pause, spatial placement, or a composition of those modalities.  Modules therefore do not need to agree on one preferred presentation.

Exactly one scheme is active globally.  It is the base recipe: named presentation rules plus optional default sound-pack and voice-palette providers.  Core and integration libraries may register read-only built-in schemes; users provide editable personal schemes.  Entering a mode never silently selects a scheme.  Every presentation consults the active scheme, but only matching selectors contribute.  Applying a saved profile may explicitly select its scheme.  Automatic module compatibility, optional presentation fragments, and overrides are separate layers.

The built-in =default= scheme preserves existing Emacsvox voices and auditory icons.  The Org example schemes are opt-in demonstrations.  Use =M-x emacsvox-aural= or =C-e H= to open the spoken aural home.  It reports current status and routes to explanation, recent exact feedback, schemes, presentation options, presentation overrides, presentation profiles, source-buffer rules, semantic vocabulary, sound packs, spatial controls, training, and the Aural Doctor.  Press =h= in any routed manager or editor to return home.  From an ordinary buffer, =C-e E= always invokes =emacsvox-aural-explain-presentation= at point.

Visual face presentation is a separate default-on control.  Use the =Visual face presentation= row, press =v= in the aural home, or run =M-x emacsvox-aural-toggle-face-presentation= to toggle rules with a =:legacy-face= selector.  Turning it off does not disable semantic role, event, state, or attribute rules.  Voice Lock remains a per-buffer compatibility control: it gates only historical face/personality-to-voice mapping and =:legacy-personality= matching.  The source state of both controls is frozen into each presentation and reported by explanation and the Aural Doctor.

Use =M-x emacsvox-aural-list-profiles= to save and switch complete named configurations.  A profile references one base scheme and ordered feature fragments and may capture sound-pack, voice-palette, and portable-spatial settings.  It never copies rules: edit those in the scheme and fragment managers.  Profile activation validates every reference before changing the live configuration and rolls the complete change back on failure.  Exactly one persisted profile identity is selected.  It is active while live values match its saved configuration and modified after settings diverge; other profiles remain inactive even when their values match.

Use =M-x emacsvox-aural-doctor= to inspect the live bindings, loaded source or byte-code, active scheme and fragments, saved profiles, sound resources, personal data, spatial fallback, speech server, and training state, including the independent face and Voice Lock controls.  It does not start the server or repair anything automatically.  The =r= command runs the explicitly offered safe repair for one row.

Use =M-x emacsvox-aural-list-schemes= to open the spoken scheme manager.  It views direct and inherited presentations and provides edit, copy, delete, rename, activate, preview, and validation actions.  Its table, spoken row, and detail view identify who provided each scheme.  Use =M-x emacsvox-aural-reset-scheme= to return to the default.  Established verb-first command names remain compatible.

Use =M-x emacsvox-aural-list-overrides=, or press =O= in Aural Home, to manage persistent personal, temporary session, and remembered source-buffer rules in one spoken table.  It reports each rule's target, change, state, and live match.  Editing opens the exact selected rule; preview resolves the complete cascade; disabling or removing a rule restores the next weaker behavior.  This is a unified view over existing layers, not another scheme or cascade layer.

Use =M-x emacsvox-aural-list-feature-fragments= to manage independent presentation additions over the active base scheme.  The user interface calls these presentation options and groups them into expandable integration collections.  Core, integration modules, third-party extensions, and personal data may provide options; collection membership affects discovery only.  Press =a= to show enabled options in exact weakest-to-strongest cascade order.  Enabling or disabling an option preserves that stable order.  Press =P= to preview an option composed with the active configuration without enabling it, or =C-u P= to hear the option alone.  Preview uses matching facts from the remembered source buffer when possible and announces that as live source context; otherwise it announces and uses a simulated example.  Create, copy, edit, delete, validate, toggle, and reorder changes use the same atomic personal-data store.  Built-in Org options can add a dynamic heading-level label, section cue, or visibility-change wording without replacing the active scheme.

Use =M-x emacsvox-aural-list-sound-packs= to inspect active state, inheritance, native and effective assets, coverage, spatialization, validation, directory, and intent.  Its cue browser identifies native, inherited, fallback, and missing resources and auditions the frozen concrete file without activating the inspected pack.  Discovered personal packs have a guided, atomic, data-only manifest editor.

Use =M-x emacsvox-aural-edit-scheme= for a spoken field editor over a persistent personal scheme.  It can create a flattened editable copy of a built-in scheme.  Use =M-x emacsvox-aural-edit-scheme-advanced= for the declarative rule view, or =M-x emacsvox-aural-edit-rules= for persistent, session, or buffer-local rules.  =M-x emacsvox-explain-aural-presentation= shows facts, context, matching rules, provenance, concrete resources, suppression, and backend degradation.  When available it explains the last exact presentation queued from the source buffer, including its source position and object/run identity, even after configuration changes.  Both spoken and visual output label this as exact heard output.  Without a queued record it labels current rule resolution as a simulation and automatically chooses the occasion with the most matching rules.  Use a prefix argument to deliberately simulate another occasion.  History is bounded by =emacsvox-aural-presentation-history-limit= and never retains source buffers; set the limit to zero to disable it.  Use =M-x emacsvox-aural-list-recent-feedback=, or press =H= in Aural Home, to browse those exact records newest first.  =RET= explains the selected heard output, =P= replays its full ordered presentation, =c= auditions only its earcons without speech or another history record, and =r= prepares a voice remap from it.  =R= auditions one exact concrete earcon and prepares a scoped replacement, suppression, or removal of a prior generated override to restore inherited behavior.  Earcon rules retain the selected event, occasion, phase, action identifier, and lifecycle anchor without copying the rest of the presentation.  A buffer-local remap is offered only while the browser is attached to the matching live source; personal and session remaps remain available after it closes.  Presentations originating inside registered Aural interfaces are excluded from history by default so manager navigation cannot displace source feedback.  Press =i= in Recent Aural Feedback, or customize =emacsvox-aural-history-record-interface-presentations=, to include them temporarily for diagnosis.  Press =L= to change the current-session history limit.  Any natural number is accepted; larger values retain more frozen spoken text in memory.  Customize =emacsvox-aural-presentation-history-limit= to persist the value.

** Cascade and Deterministic Selection

Matching rules are applied from weaker to stronger.  Origin layers are =core=, module fragments, the active inherited scheme, ordered enabled presentation options, persistent user rules, session rules, and buffer rules.  One base scheme is active, while any number of independent presentation options may add presentation without replacing it.  Automatic module compatibility fragments are a separate default layer and do not appear as toggleable options.  Within an origin, semantic identity, an exact combined module/mode match, exact or nearest derived mode, module, additional constraints, inheritance layer, and explicit rule order determine specificity.  A stable rule identifier breaks a remaining true tie, so hash and registration order cannot change output.

A buffer rule can therefore override one module in one buffer without changing the same semantic in other buffers.  A mode selector also matches derived modes, with the nearest ancestor winning.  Combine =:module= and =:mode= when the same mode should sound different in one integration.  Interactive physical or visual Up and Down movement uses the =navigation= occasion and =focus-entered= event, so arrowing onto a semantic object matches structural navigation.  Continuous-reading commands retain the =continuous= occasion.

** Render and Queue Contract

Each aural object has ordered =before= actions, one or more styled content runs, and ordered =after= actions.  A face or personality change creates a formatting run, not another object, so object labels and cues do not repeat merely because styling changes.  Speech, cue, and pause actions may be added.  Phase operators are =:prepend=, =:append=, =:replace=, =:remove=, and =:suppress=.  Content independently controls =:speak=, =:voice=, =:volume=, =:space=, and =:suppress=; the strongest rule that sets a scalar wins.  Ordered actions may use =:anchor object=, =:anchor run=, or =:anchor transition=.  An omitted anchor defaults to =run= for face and personality selectors and =object= otherwise.

Named source faces are captured before source text is copied.  Ordered overlay faces use Emacs overlay priority and precede explicit =face= and =font-lock-face= text properties; both text properties are retained, with =face= first.  Only explicit symbol or string face names and named =:inherit= values are captured.  Theme inheritance and face remapping are not expanded into identity.  Frozen context retains data-only source, property, priority, range, and order provenance.

Semantic and contextual resolution happens in Emacs at the source submission boundary.  Safe speech templates become literal text, cue names become concrete files or sample IDs, voices become adapter commands, and spatial requests become backend values before anything is queued.  The speech server never resolves templates, schemes, modes, modules, semantics, or resource fallbacks.  A complete multi-action plan uses one strict queue.  Only a standalone compatibility cue may use the selected local player.  Validated rule snapshots and inherited providers are reused within one configuration generation.  Sound content digests are reused by canonical file identity and modification metadata and are cleared when resource packs refresh.

The current speech queue has no portable volume operation.  Explicit content or action volume therefore records an =unsupported-volume= capability degradation by default instead of being silently ignored.  Set =emacsvox-aural-unsupported-volume-policy= to =reject= to fail before queueing any presentation that requests volume.

** Spatial Fallback

Portable space is normalized =:balance= from =-1.0= left through =+1.0= right.  Listener-relative =:azimuth= from =-180= through =+180= degrees reduces to stereo balance.  Unsupported or mono transports remain audible at the centre, and pre-spatialized assets are not positioned twice.  Global, speech-only, cue-only, output, maximum-separation, and remapping controls are available through the =emacsvox-aural-spatial= Customize group.  Use =M-x emacsvox-describe-aural-spatial-capabilities= to inspect the current fallback.

* Scheme Author Reference

Personal data lives in =~/.emacsvox/aural-schemes.el=.  It is versioned declarative Lisp data; evaluation syntax is rejected by the reader.  Prefer the accessible editor; if data is authored directly, retain the outer =:schema-version=, =:schemes=, =:feature-fragments=, =:feature-fragment-order=, =:enabled-feature-fragments=, =:voice-palettes=, =:profiles=, =:active-profile=, and =:user-rules= fields.

A scheme contains =:schema-version=, =:id=, nonempty =:summary=, optional =:parent=, optional =:resource-pack= and =:voice-palette=, and =:rules=.  Every rule has a globally stable =:id=, optional boolean =:enabled=, optional integer =:order=, a =:match= plist, and a =:render= plist.

#+begin_src emacs-lisp
(:schema-version 1
 :id personal-headings
 :summary "Speak and position first-level Org headings"
 :parent default
 :rules
 ((:id personal-org-heading-1
   :match (:role heading :module org :mode org-mode
           :occasion navigation :level 1)
   :render
   (:before
    (:append ((:id heading-label :kind speech :text "Heading 1"
              :anchor object)))
    :content (:voice bolden :space (:balance -0.2))
    :after
    (:append ((:id heading-end :kind cue :cue section)))))))
#+end_src

Selectors use =:role=, =:event= or =:events=, =:state= or =:states=, registered attribute keywords, =:requires= for registered attributes that must exist without fixing their value, =:module=, =:mode=, =:occasion=, =:legacy-cue=, =:legacy-face=, and =:legacy-personality=.  Unknown semantics, attributes, cues, voices, providers, fields, or schema versions fail validation.  Singular and plural state and event forms are canonicalized, range-local scalar facts override base facts, and local state and event sets compose with base sets.  Explanation displays this same authoritative fact plist.  Declared role, occasion, and phase contracts reject impossible selectors.  Validation warns about deprecated semantic aliases and rules whose fallback selectors overlap more-specific rules.

Ordered actions require an =:id= and =:kind=.  A speech action supplies either literal =:text= or a safe =:text-template= such as ="Heading {level}"=, and may supply =:voice=, =:volume=, and =:space=.  A template can name only =role= or registered attributes, and its selector must guarantee every field by exact match or =:requires=.  Templates perform substitution only; they cannot evaluate Lisp.  A cue action supplies a registered =:cue= and may supply =:volume= and =:space=.  A pause supplies a nonnegative =:duration=.  Optional =:anchor= is =object=, =run=, or =transition=.  Semantic rules default to one action per object; compatibility face and personality rules default to one per formatting run.  A before transition action occurs when entering matching presentation and an after transition action when leaving it.  Action IDs are the handles used by later =:remove= operations.

* Module Author Reference

Register meaning before emitting it.  A semantic registration supplies a unique identifier, =:kind= (=role=, =event=, =state=, or =attribute=), intent summary, owner, and any value, occasion, phase, fallback, or usage contract.  Optional =:roles= restricts a state, event, or attribute to named roles; optional =:attributes=, =:states=, and =:events= restrict a role.  These restrictions, =:occasions=, and =:phases= are enforced for facts and rules rather than serving only as documentation.  The registry owner defines the intent, type, and allowed values; modules emit those facts, while schemes and fragments decide only how to present them.  For example, core owns the =visibility= attribute and its =folded= and =expanded= values.  Do not use a visual face, voice name, cue name, or file name as semantic identity.

#+begin_src emacs-lisp
(emacsvox-aural-register-semantic
 'diagnostic
 :kind 'role
 :summary "A source diagnostic"
 :owner 'example-module
 :occasions '(navigation continuous)
 :phases '(before content after))
#+end_src

Use =emacsvox-aural-register-semantic-alias= when an identifier must be renamed.  Aliases remain readable migration hooks tagged with the semantic contract version, compile to the canonical identifier, and produce deprecation diagnostics.  Never silently reuse an old identifier for a different intent.  A fallback must retain the same semantic kind.  When emitted facts name a more specific semantic, rules selecting its fallback also match weakly; exact rules compose later and explanation shows the complete path and distance.

For persistent formatted text, attach =emacsvox-aural-facts= and =emacsvox-aural-module= text properties.  A submission is one inferred object until facts, module, occasion, or a new queued icon changes.  Attach the same non-nil =emacsvox-aural-object= value across adjacent runs when an explicitly identified object must span those changes.  For transient output with explicit content, call =emacsvox-aural-submit= with =:facts=, =:module=, and =:occasion=.  Put preserved cue order in =:compatibility-actions= using =emacsvox-aural-compatibility-icon=; its optional phase is =before= by default or =after=.  The submission resolves semantic policy once, merges only cue-specific legacy adapter policy, compiles once, and retains one history transaction across formatting and speech-clause runs.  Do not call =tts-speak=, =emacsvox-icon=, or another complete resolver inside that boundary.  When an existing single speech or cue call cannot yet become explicit content, use =emacsvox-aural-call-with-submission= to centralize its frozen facts and context; that wrapper is a compatibility boundary, not a native transaction.  Use =emacsvox-aural-source-substring= instead of =buffer-substring= when copied source text can be styled by overlays; the returned string carries the same authoritative face snapshot used by point explanation.  Capture context in the source buffer before text enters a scratch buffer or notification log.  Keep preserved legacy output as data-only compatibility-action builders where possible.  The source audit rejects complete resolution nested beneath native submissions.

A module may register a read-only fragment for compatibility defaults, but the fragment still matches semantic facts and emits modality.  Keep meaning in the registry and presentation in rules.  Preserve established output during migration, add facts around it, and let users override the new facts.  Add new core semantics only when their intent is shared; use a module-owned identifier for genuinely private meaning.

A module or third-party extension may also register optional, read-only feature fragments, presented to users as presentation options.  Supply a =:collection= symbol to group related options under the integration that owns them.  Use a broader collection when an option intentionally spans several integrations.  Collection membership never changes rule matching or cascade order; enabled precedence is controlled separately.  Providers may call =emacsvox-aural-register-feature-fragment-example= after the option is registered.  Name the exact option rule demonstrated and supply a short summary plus data-only =:facts= and =:context=.  Registration normalizes the input and rejects examples that do not match their named rule.  Curated examples are runtime provider metadata, not persistent personal data.  Preview derives a representative simulation for each enabled rule not covered by a curated example.

Repository data-only providers use the flat library name =emacsvox-aural-provider-SCOPE.el=.  A provider may register semantic metadata and presentation data, but must not load its external package, inspect live buffers, install package hooks or advice, or speak.  The corresponding =emacsvox-SCOPE.el= integration requires the provider and owns live fact capture and feedback.  Cross-integration definitions use a descriptive shared scope such as =workflows=.

Extension checklist:

1. Search the registry for an existing intent and reuse it when exact.
2. Register new metadata before a saved scheme could reference it.
3. Emit facts and source context without choosing a resource.
4. Submit explicit content and ordered compatibility actions together.
5. Test one resolution, one history transaction, exact ordering, settings, and a user override.
6. Keep cue-only or implicit-content legacy paths adapted until their content can be represented without losing behavior.
7. Run =make aural-reference= and =make aural-audit=.

* Semantic Registry

Operational semantic contract version: =1=.

| Identifier | Kind | Owner | Value | Roles | Attributes | States | Events | Occasions | Phases | Fallback | Intent |
|------------+------+-------+-------+-------+------------+--------+--------+-----------+--------+----------+--------|
| =accepted= | =event= | =core= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | The current candidate or choice was accepted |
| =activity-ended= | =event= | =core= | - | - | - | - | - | - | - | - | A repeating or ongoing activity ended |
| =agent-block= | =role= | =agent-shell= | - | - | - | - | - | =navigation=, =state-change=, =inspection= | =before=, =content=, =after= | - | One navigable structural block in an agent transcript |
| =agent-block-kind= | =attribute= | =agent-shell= | =agent-response=, =user-prompt=, =thought=, =tool-call=, =activity-group=, =plan=, =permission=, =error=, =table=, =source-block=, =other= | - | - | - | - | - | - | - | The structural kind of an Agent Shell transcript block |
| =agent-content-copied= | =event= | =agent-shell= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | Agent Shell content was copied for reuse |
| =agent-content-inspected= | =event= | =agent-shell= | - | - | - | - | - | =inspection= | =before=, =content=, =after= | - | The user explicitly requested Agent Shell content |
| =agent-error= | =role= | =agent-shell= | - | - | - | - | - | =continuous=, =navigation=, =inspection=, =notification= | =before=, =content=, =after= | - | Error content produced while running an agent session |
| =agent-permission-requested= | =event= | =agent-shell= | - | - | - | - | - | =notification= | =before=, =content=, =after= | - | An Agent Shell action requested user permission |
| =agent-permission-resolved= | =event= | =agent-shell= | - | - | - | - | - | =state-change=, =notification= | =before=, =content=, =after= | - | An Agent Shell permission request was resolved |
| =agent-permission-result= | =attribute= | =agent-shell= | =allowed=, =denied=, =cancelled=, =sent= | - | - | - | - | - | - | - | The result of an Agent Shell permission decision |
| =agent-plan= | =role= | =agent-shell= | - | - | - | - | - | =continuous=, =navigation=, =inspection= | =before=, =content=, =after= | - | A plan produced by an agent |
| =agent-prompt-cancelled= | =event= | =agent-shell= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | Agent Shell prompt composition was cancelled |
| =agent-prompt-disposition= | =attribute= | =agent-shell= | =submitted=, =queued=, =sent= | - | - | - | - | - | - | - | Whether an Agent Shell prompt was submitted, queued, or sent |
| =agent-prompt-editor= | =role= | =agent-shell= | - | - | - | - | - | =navigation=, =edit=, =state-change= | =before=, =content=, =after= | - | An editor used to compose an Agent Shell prompt |
| =agent-prompt-opened= | =event= | =agent-shell= | - | - | - | - | - | =edit=, =state-change= | =before=, =content=, =after= | - | An Agent Shell prompt editor was opened |
| =agent-prompt-submitted= | =event= | =agent-shell= | - | - | - | - | - | =state-change=, =notification= | =before=, =content=, =after= | - | A composed Agent Shell prompt was submitted or queued |
| =agent-response= | =role= | =agent-shell= | - | - | - | - | - | =continuous=, =navigation=, =inspection=, =notification= | =before=, =content=, =after= | - | User-facing response content produced by an agent |
| =agent-session= | =role= | =agent-shell= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =inspection=, =notification= | =before=, =content=, =after= | - | One interactive agent conversation |
| =agent-session-interrupted= | =event= | =agent-shell= | - | - | - | - | - | =state-change=, =notification= | =before=, =content=, =after= | - | The user interrupted an Agent Shell request |
| =agent-session-opened= | =event= | =agent-shell= | - | - | - | - | - | =navigation=, =state-change= | =before=, =content=, =after= | - | An Agent Shell session was opened or selected |
| =agent-setting-changed= | =event= | =agent-shell= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | An Agent Shell presentation or session setting changed |
| =agent-source-block= | =role= | =agent-shell= | - | - | - | - | - | =navigation=, =inspection=, =state-change= | =before=, =content=, =after= | - | A rendered source-code block in an agent response |
| =agent-source-language= | =attribute= | =agent-shell= | =string= | - | - | - | - | - | - | - | The declared language of an Agent Shell source block |
| =agent-speech-level= | =attribute= | =agent-shell= | =auto=, =full=, =response=, =notify=, =quiet= | - | - | - | - | - | - | - | The selected automatic speech level for an agent session |
| =agent-table= | =role= | =agent-shell= | - | - | - | - | - | =navigation=, =inspection=, =state-change= | =before=, =content=, =after= | - | A rendered table in an agent response |
| =agent-table-cell= | =role= | =agent-shell= | - | - | - | - | - | =navigation=, =inspection=, =state-change= | =before=, =content=, =after= | - | One logical cell in a rendered agent-response table |
| =agent-table-column= | =attribute= | =agent-shell= | =integer= | - | - | - | - | - | - | - | The zero-based logical column of an Agent Shell table cell |
| =agent-table-entered= | =event= | =agent-shell= | - | - | - | - | - | =navigation= | =before=, =content=, =after= | - | Navigation entered a rendered Agent Shell table |
| =agent-table-exited= | =event= | =agent-shell= | - | - | - | - | - | =navigation= | =before=, =content=, =after= | - | Navigation left a rendered Agent Shell table |
| =agent-table-row= | =attribute= | =agent-shell= | =integer= | - | - | - | - | - | - | - | The zero-based logical row of an Agent Shell table cell |
| =agent-thought= | =role= | =agent-shell= | - | - | - | - | - | =continuous=, =navigation=, =inspection= | =before=, =content=, =after= | - | Intermediate reasoning content exposed by an agent |
| =agent-tool= | =role= | =agent-shell= | - | - | - | - | - | =continuous=, =navigation=, =inspection=, =notification= | =before=, =content=, =after= | - | A tool invocation or its output in an agent session |
| =agent-tool-status= | =attribute= | =agent-shell= | =pending=, =in-progress=, =completed=, =failed= | - | - | - | - | - | - | - | The current lifecycle status of an Agent Shell tool call |
| =agent-tool-status-changed= | =event= | =agent-shell= | - | - | - | - | - | =notification= | =before=, =content=, =after= | - | An Agent Shell tool call changed lifecycle status |
| =agent-user-prompt= | =role= | =agent-shell= | - | - | - | - | - | =continuous=, =navigation=, =edit=, =inspection= | =before=, =content=, =after= | - | User-authored prompt content in an agent conversation |
| =agent-viewport= | =role= | =agent-shell= | - | - | - | - | - | =navigation=, =state-change=, =notification= | =before=, =content=, =after= | - | An Agent Shell response or prompt viewport |
| =agent-viewport-mode= | =attribute= | =agent-shell= | =view=, =edit= | - | - | - | - | - | - | - | The interaction mode of an Agent Shell viewport |
| =agent-viewport-opened= | =event= | =agent-shell= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | An Agent Shell viewport was opened |
| =agent-viewport-refreshed= | =event= | =agent-shell= | - | - | - | - | - | =state-change=, =notification= | =before=, =content=, =after= | - | An Agent Shell viewport finished refreshing |
| =aural-interface= | =role= | =core= | - | - | - | - | - | =navigation=, =state-change=, =inspection= | =before=, =content=, =after= | - | An Emacsvox aural manager, browser, or editor |
| =aural-interface-closed= | =event= | =core= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | An Emacsvox aural interface was dismissed |
| =aural-interface-opened= | =event= | =core= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | An Emacsvox aural interface was displayed |
| =boundary-entered= | =event= | =core= | - | - | - | - | - | =navigation= | =before=, =content=, =after= | - | Navigation arrived at a source-code boundary |
| =candidate= | =role= | =core= | - | - | - | - | - | =navigation=, =state-change= | =before=, =content=, =after= | - | One available completion candidate |
| =checked= | =state= | =markdown= | - | =markdown-task= | - | - | - | =navigation=, =continuous=, =state-change=, =inspection= | =before=, =content=, =after= | - | A checkable item is complete |
| =code-construct= | =role= | =core= | - | - | - | - | - | =navigation=, =continuous=, =edit=, =inspection= | =before=, =content=, =after= | - | A structural construct in source code |
| =completion-index= | =attribute= | =core= | =integer= | - | - | - | - | - | - | - | Zero-based position of a candidate in a completion list |
| =confirmation-request= | =role= | =core= | - | - | - | - | - | =notification= | =before=, =content=, =after= | - | A prompt asking the user to confirm an action |
| =deletion-flagged= | =state= | =dired= | - | - | - | - | - | =navigation=, =state-change= | =before=, =content=, =after= | - | A filesystem entry is flagged for deletion |
| =entry-deletion-flagged= | =event= | =dired= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | A filesystem entry was flagged for deletion |
| =entry-inspected= | =event= | =dired= | - | - | - | - | - | =inspection= | =before=, =content=, =after= | - | The user requested a property of a filesystem entry |
| =entry-inspection-kind= | =attribute= | =dired= | =size=, =modification-time=, =access-time=, =symbolic-link-target=, =permissions=, =file-type= | - | - | - | - | - | - | - | The requested property of a filesystem entry |
| =entry-kind= | =attribute= | =dired= | =file=, =directory=, =symbolic-link=, =other= | =filesystem-entry= | - | - | - | - | - | - | The filesystem kind of a directory entry |
| =entry-marked= | =event= | =dired= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | A filesystem entry was marked |
| =entry-opened= | =event= | =dired= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | A filesystem entry or directory was opened |
| =entry-staged= | =event= | =magit= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | A version-control entry was staged |
| =entry-unmarked= | =event= | =dired= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | A filesystem entry mark was removed |
| =entry-unstaged= | =event= | =magit= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | A version-control entry was unstaged |
| =field= | =role= | =core= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =inspection= | =content= | - | A named field within a structured object |
| =field-kind= | =attribute= | =core= | =symbol= | =field= | - | - | - | - | - | - | The module-defined purpose of a structured field |
| =filesystem-entry= | =role= | =dired= | - | - | - | - | - | =navigation=, =state-change=, =inspection= | =before=, =content=, =after= | - | A file, directory, symbolic link, or other directory entry |
| =filesystem-listing= | =role= | =dired= | - | - | - | - | - | =navigation=, =state-change=, =inspection= | =before=, =content=, =after= | - | A directory or search-result listing of filesystem entries |
| =filesystem-listing-closed= | =event= | =dired= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | A filesystem listing was dismissed |
| =flagged= | =state= | =core= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =inspection=, =notification= | =before=, =content=, =after= | - | An item has been explicitly flagged for attention |
| =focus-entered= | =event= | =core= | - | - | - | - | - | - | - | - | Navigation arrived at an aural object |
| =folded= | =state= | =core= | - | - | - | - | - | - | - | - | Descendants of an object are hidden |
| =game-over= | =event= | =core= | - | - | - | - | - | - | - | - | A game ended without successful completion |
| =has-attachments= | =state= | =core= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =inspection= | =before=, =content=, =after= | - | A message contains one or more attachments |
| =heading= | =role= | =core= | - | - | - | - | - | - | - | - | A titled structural section |
| =level= | =attribute= | =core= | =positive-integer= | - | - | - | - | - | - | - | One-based structural depth |
| =mail-action-kind= | =attribute= | =core= | =open=, =close=, =compose=, =select=, =unsubscribe=, =catch-up=, =restore=, =list=, =customize=, =toggle-topic=, =show=, =hide=, =scroll=, =activate=, =modify=, =save=, =archive=, =tag=, =search=, =refresh= | - | - | - | - | - | - | - | The mail-reader operation whose feedback is being presented |
| =mail-group= | =role= | =gnus= | - | - | - | - | - | =navigation=, =state-change=, =inspection=, =notification= | =before=, =content=, =after= | - | A mail or news group containing messages |
| =mail-view= | =role= | =core= | - | - | - | - | - | =navigation=, =state-change=, =inspection=, =notification= | =before=, =content=, =after= | - | A mail reader group, summary, article, compose, or server view |
| =mail-view-closed= | =event= | =core= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | A mail reader view was closed, buried, or left |
| =mail-view-kind= | =attribute= | =core= | =group=, =summary=, =article=, =compose=, =server=, =search=, =thread=, =topic=, =other= | =mail-view= | - | - | - | - | - | - | The kind of mail-reader view being presented |
| =mail-view-opened= | =event= | =core= | - | - | - | - | - | =navigation=, =state-change= | =before=, =content=, =after= | - | A mail reader view was opened or selected |
| =markdown-code-block= | =role= | =markdown= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =edit=, =inspection=, =notification= | =before=, =content=, =after= | - | A fenced Markdown source-code block |
| =markdown-completion-completed= | =event= | =markdown= | - | - | - | - | - | =edit= | =before=, =content=, =after= | - | A Markdown completion command updated document content |
| =markdown-content= | =role= | =markdown= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =edit=, =inspection=, =notification= | =before=, =content=, =after= | - | Ordinary prose content in a Markdown document |
| =markdown-footnote= | =role= | =markdown= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =edit=, =inspection=, =notification= | =before=, =content=, =after= | - | A Markdown footnote definition or reference |
| =markdown-heading-navigated= | =event= | =markdown= | - | - | - | - | - | =navigation= | =before=, =content=, =after= | - | Structural navigation arrived at a Markdown heading |
| =markdown-language= | =attribute= | =markdown= | =string= | =markdown-code-block= | - | - | - | - | - | - | The declared language of a Markdown fenced code block |
| =markdown-link= | =role= | =markdown= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =edit=, =inspection=, =notification= | =before=, =content=, =after= | - | A Markdown link, image, or reference |
| =markdown-link-navigated= | =event= | =markdown= | - | - | - | - | - | =navigation= | =before=, =content=, =after= | - | Navigation arrived at a Markdown link |
| =markdown-list-item= | =role= | =markdown= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =edit=, =inspection=, =notification= | =before=, =content=, =after= | - | One ordered or unordered Markdown list item |
| =markdown-list-kind= | =attribute= | =markdown= | =ordered=, =unordered= | =markdown-list-item=, =markdown-task= | - | - | - | - | - | - | Whether a Markdown list item is ordered or unordered |
| =markdown-navigation-kind= | =attribute= | =markdown= | =line=, =structural= | =heading=, =markdown-content=, =markdown-list-item=, =markdown-task=, =markdown-link=, =markdown-code-block=, =markdown-table-row=, =markdown-footnote=, =markdown-separator= | - | - | - | - | - | - | Whether Markdown navigation was by line or structure |
| =markdown-operation-completed= | =event= | =markdown= | - | - | - | - | - | =notification=, =state-change= | =before=, =content=, =after= | - | A Markdown validation or export operation completed |
| =markdown-separator= | =role= | =markdown= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =edit=, =inspection=, =notification= | =before=, =content=, =after= | - | A thematic or structural separator in Markdown |
| =markdown-structure-navigated= | =event= | =markdown= | - | - | - | - | - | =navigation= | =before=, =content=, =after= | - | A Markdown structural navigation command moved point |
| =markdown-table-row= | =role= | =markdown= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =edit=, =inspection=, =notification= | =before=, =content=, =after= | - | One logical row of a Markdown table |
| =markdown-task= | =role= | =markdown= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =edit=, =inspection=, =notification= | =before=, =content=, =after= | - | A Markdown task-list item |
| =markdown-task-state= | =attribute= | =markdown= | =checked=, =unchecked= | =markdown-task= | - | - | - | - | - | - | Whether a Markdown task is checked or unchecked |
| =marked= | =state= | =dired= | - | - | - | - | - | =navigation=, =state-change= | =before=, =content=, =after= | - | An item is marked for a later operation |
| =message= | =role= | =core= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =inspection=, =notification= | =before=, =content=, =after= | - | A mail or communication message |
| =message-deleted= | =event= | =core= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | A message was marked or removed as deleted |
| =message-marked= | =event= | =core= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | A message mark or persistent attention state changed |
| =message-opened= | =event= | =core= | - | - | - | - | - | =state-change=, =notification= | =before=, =content=, =after= | - | A mail or communication message was opened for reading |
| =message-part= | =role= | =core= | - | - | - | - | - | =navigation=, =state-change=, =inspection=, =notification= | =before=, =content=, =after= | - | A body, attachment, link, button, or other part of a message |
| =message-part-kind= | =attribute= | =core= | =body=, =page=, =attachment=, =link=, =button=, =mime-part=, =header=, =other= | =message-part= | - | - | - | - | - | - | The kind of content part within a message |
| =message-thread= | =role= | =core= | - | - | - | - | - | =navigation=, =state-change=, =inspection= | =before=, =content=, =after= | - | A conversation thread containing related messages |
| =object-changed= | =event= | =core= | - | - | - | - | - | - | - | - | A registered object was created or modified |
| =operation-completed= | =event= | =core= | - | - | - | - | - | =state-change=, =inspection=, =notification= | =before=, =content=, =after= | - | A requested module operation completed successfully |
| =operation-failed= | =event= | =core= | - | - | - | - | - | =navigation=, =state-change=, =inspection=, =notification= | =before=, =content=, =after= | - | A requested module operation failed |
| =org-action= | =attribute= | =org= | =item-navigation=, =structure-navigation=, =paragraph-navigation=, =list-style-changed=, =heading-edited=, =subtree-changed=, =option-toggled=, =timestamp-changed=, =agenda-navigation=, =agenda-opened=, =agenda-closed=, =table-mode-toggled=, =line-inserted=, =checkbox-toggled=, =item-boundary=, =line-start=, =line-end=, =capture-target=, =capture-saved=, =capture-cancelled=, =edit-opened=, =edit-closed=, =paragraph-filled=, =todo-changed=, =list-item-created=, =export-completed=, =publish-completed= | =heading=, =org-content=, =org-item=, =org-paragraph=, =org-agenda-entry=, =org-table=, =org-capture=, =org-edit-buffer=, =org-export= | - | - | - | - | - | - | The user-visible Org operation being presented |
| =org-agenda-entry= | =role= | =org= | - | - | - | - | - | - | - | - | One entry in an Org agenda |
| =org-capture= | =role= | =org= | - | - | - | - | - | - | - | - | An Org capture target or capture lifecycle operation |
| =org-content= | =role= | =org= | - | - | - | - | - | - | - | - | Ordinary content in an Org document |
| =org-edit-buffer= | =role= | =org= | - | - | - | - | - | - | - | - | A temporary Org source-editing buffer |
| =org-export= | =role= | =org= | - | - | - | - | - | - | - | - | An Org export or publishing operation |
| =org-item= | =role= | =org= | - | - | - | - | - | - | - | - | One list item in an Org document |
| =org-paragraph= | =role= | =org= | - | - | - | - | - | - | - | - | One prose paragraph in an Org document |
| =org-table= | =role= | =org= | - | - | - | - | - | - | - | - | An Org table or table cell |
| =permission-request= | =role= | =agent-shell= | - | - | - | - | - | =navigation=, =state-change=, =notification= | =before=, =content=, =after= | - | An agent action awaiting user permission |
| =processing= | =state= | =agent-shell= | - | - | - | - | - | =continuous=, =notification= | =before=, =content=, =after= | - | An agent session is processing a request |
| =processing-completed= | =event= | =agent-shell= | - | - | - | - | - | =notification= | =before=, =content=, =after= | - | An agent completed initialization or a request |
| =processing-failed= | =event= | =agent-shell= | - | - | - | - | - | =notification= | =before=, =content=, =after= | - | An agent request ended exceptionally |
| =processing-started= | =event= | =agent-shell= | - | - | - | - | - | =notification= | =before=, =content=, =after= | - | An agent began initialization or request processing |
| =product-identity= | =event= | =core= | - | - | - | - | - | - | - | - | Emacsvox identity or readiness was presented |
| =refresh-completed= | =event= | =core= | - | - | - | - | - | =notification= | =before=, =content=, =after= | - | A displayed collection finished refreshing |
| =refresh-failed= | =event= | =core= | - | - | - | - | - | =notification= | =before=, =content=, =after= | - | A displayed collection failed to refresh |
| =section-kind= | =attribute= | =magit= | =symbol= | =vcs-section= | - | - | - | - | - | - | The integration-defined kind of a structural section |
| =selected= | =state= | =core= | - | - | - | - | - | =navigation=, =state-change= | =before=, =content=, =after= | - | An item is the current selection |
| =selection-cleared= | =event= | =core= | - | - | - | - | - | - | - | - | A mark or selection was removed |
| =staged= | =state= | =magit= | - | - | - | - | - | =navigation=, =state-change= | =before=, =content=, =after= | - | A version-control change is staged for commit |
| =state-changed= | =event= | =core= | - | - | - | - | - | - | - | - | A registered state changed |
| =syntax-role= | =attribute= | =core= | =symbol= | - | - | - | - | - | - | - | The language-defined structural role of source code |
| =unchecked= | =state= | =markdown= | - | =markdown-task= | - | - | - | =navigation=, =continuous=, =state-change=, =inspection= | =before=, =content=, =after= | - | A checkable item is incomplete |
| =unread= | =state= | =core= | - | - | - | - | - | =navigation=, =continuous=, =state-change=, =inspection=, =notification= | =before=, =content=, =after= | - | An item has not yet been read |
| =unstaged= | =state= | =magit= | - | - | - | - | - | =navigation=, =state-change= | =before=, =content=, =after= | - | A version-control change is not staged for commit |
| =vcs-blame-chunk= | =role= | =magit= | - | - | - | - | - | =navigation=, =inspection= | =before=, =content=, =after= | - | One annotated source chunk in a version-control blame view |
| =vcs-commit-displayed= | =event= | =magit= | - | - | - | - | - | =navigation=, =state-change= | =before=, =content=, =after= | - | A version-control commit was displayed |
| =vcs-diff-scrolled= | =event= | =magit= | - | - | - | - | - | =navigation= | =before=, =content=, =after= | - | Navigation moved within a version-control diff |
| =vcs-process= | =role= | =magit= | - | - | - | - | - | =notification= | =before=, =content=, =after= | - | An asynchronous version-control operation |
| =vcs-section= | =role= | =magit= | - | - | - | - | - | =navigation=, =state-change=, =notification= | =before=, =content=, =after= | - | A structural section in a version-control view |
| =vcs-view= | =role= | =magit= | - | - | - | - | - | =navigation=, =state-change=, =inspection=, =notification= | =before=, =content=, =after= | - | A version-control status, log, blob, blame, or diff view |
| =vcs-view-closed= | =event= | =magit= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | A version-control view was closed or buried |
| =vcs-view-kind= | =attribute= | =magit= | =status=, =log=, =blob=, =blame=, =diff=, =commit=, =other= | =vcs-view= | - | - | - | - | - | - | The kind of version-control view being presented |
| =vcs-view-opened= | =event= | =magit= | - | - | - | - | - | =navigation=, =state-change= | =before=, =content=, =after= | - | A version-control view was opened or selected |
| =visibility= | =attribute= | =core= | =folded=, =expanded= | - | - | - | - | - | - | - | Whether structural descendants are visible |
| =visibility-changed= | =event= | =core= | - | - | - | - | - | =state-change= | =before=, =content=, =after= | - | The visible descendants of a structural object changed |

** Stable Semantic Aliases

| Deprecated identifier | Canonical identifier | Owner | Since contract version | Migration note |
|-----------------------+----------------------+-------+------------------------+----------------|
| =collapsed= | =folded= | =core= | =1= | Use the canonical folded state identifier |

** Presentation Occasions

| Identifier | Owner | Intent |
|------------+-------+--------|
| =continuous= | =core= | Continuous reading reached the object |
| =edit= | =core= | The object was created or changed |
| =inspection= | =core= | The user requested detail or explanation |
| =navigation= | =core= | Point or focus moved to the object |
| =notification= | =core= | An asynchronous event requested attention |
| =state-change= | =core= | An action changed a registered state |

* Built-in Schemes

| Identifier | Provided By | Parent | Sound Pack | Voice Palette | Rules | Intent |
|------------+-------------+--------+------------+---------------+-------+--------|
| =default= | Emacsvox core | - | =chimes= | =acss-default= | =0= | Compatibility-preserving Emacsvox presentation |
| =org-before-after= | Org integration | =default= | =chimes= | =acss-default= | =1= | Org headings use explicit speech before and after contents |
| =org-combined= | Org integration | =default= | =chimes= | =acss-default= | =1= | Org headings combine a label, cue, and content voice |
| =org-cue-only= | Org integration | =default= | =chimes= | =acss-default= | =1= | Org headings introduced by one semantic section cue |
| =org-folded-state= | Org integration | =default= | =chimes= | =acss-default= | =1= | Folded Org headings announce their state after contents |
| =org-spoken-label= | Org integration | =default= | =chimes= | =acss-default= | =8= | Org heading levels spoken before heading contents |
| =org-voice-only= | Org integration | =default= | =chimes= | =acss-default= | =9= | Org heading levels presented only through distinct voices |

** Built-in Feature Fragments

| Identifier | Collection | Rules | Curated Examples | Source | Intent |
|------------+------------+-------+------------------+--------+--------|
| =agent-shell-block-type-cues= | =agent-shell= | =11= | =0= | emacsvox-aural-provider-workflows | Use distinct semantic cues for Agent Shell block types |
| =agent-shell-block-type-labels= | =agent-shell= | =1= | =0= | emacsvox-aural-provider-workflows | Speak the semantic type when entering an Agent Shell block |
| =agent-shell-block-visibility-cues= | =agent-shell= | =4= | =0= | emacsvox-aural-provider-workflows | Cue folded and expanded Agent Shell blocks on entry and toggle |
| =dired-entry-state-labels= | =dired= | =3= | =0= | emacsvox-aural-provider-workflows | Speak Dired mark and deletion-flag changes after the entry |
| =magit-section-visibility-cues= | =magit= | =2= | =0= | emacsvox-aural-provider-workflows | Add semantic open and close cues when Magit section visibility changes |
| =mail-message-status-cues= | =mail= | =3= | =2= | emacsvox-aural-provider-workflows | Add semantic cues for unread, flagged, and attached messages |
| =markdown-heading-level-labels= | =markdown= | =1= | =0= | emacsvox-aural-provider-markdown | Speak Markdown heading levels before heading contents |
| =markdown-task-state-labels= | =markdown= | =2= | =0= | emacsvox-aural-provider-markdown | Speak checked and unchecked state after Markdown tasks |
| =org-heading-level-labels= | =org= | =1= | =1= | emacsvox-aural-provider-org | Speak an Org heading's level before inherited presentation |
| =org-heading-section-cues= | =org= | =1= | =0= | emacsvox-aural-provider-org | Add a section cue before navigated Org headings |
| =org-heading-visibility-changes= | =org= | =2= | =1= | emacsvox-aural-provider-org | Speak an Org heading's level and new visibility after cycling |

** Module Compatibility Fragments

| Identifier | Module | Rules | Source | Intent |
|------------+--------+-------+--------+--------|
| =dired-entry-navigation= | =dired= | =1= | emacsvox-dired | State-aware navigation feedback for Dired entries |
| =markdown-compatibility= | =markdown= | =11= | emacsvox-aural-provider-markdown | Compatibility cues for semantic Markdown presentation |
| =org-compatibility= | =org= | =2= | emacsvox-aural-provider-org | Compatibility presentation for semantic Org headings |

* Sound Pack Author Reference

Register a cue with intent before adding an asset.  Register a pack with an identifier, summary, =sound= or =prompt= kind, directory, optional parent, requirement profiles, and default spatialization.  File basenames are cue identifiers.  Unknown files fail audit; missing required cues fail pack validation.  Every standalone sound pack must resolve =button=.  Use a parent for deliberate fallback.

Bundled packs live below =sounds/packs/=.  Personal packs live below =~/.emacsvox/sounds/packs/= by default; customize =emacsvox-aural-personal-sound-packs-directory= to use another root.  Immediate subdirectories containing =button.ogg= are discovered automatically.  Direct =sounds/PACK/= children remain a deprecated, lowest-precedence compatibility source.  A partial pack without =button.ogg= can opt in with a data-only =emacsvox-sound-pack.el= manifest containing =:schema-version 1= and optional =:summary=, =:parent=, =:profiles=, and =:default-spatialization= fields.  Discovery runs before pack completion and selection; =M-x emacsvox-aural-refresh-discovered-resource-packs= forces it explicitly.  Call =emacsvox-aural-refresh-resource-pack= after changing files in an already selected pack.

Use =M-x emacsvox-aural-list-sound-packs= for the spoken workbench.  It can rescan, validate, activate, audition, open the pack directory, browse cue intent and provenance, and guidedly edit manifests belonging to discovered packs.  Registered built-in metadata remains read-only.  Manifest replacement is atomic, retains the previous file as =~=, and rolls back when refreshed metadata cannot be validated.

Default spatialization is =neutral=, =stereo=, or =pre-spatialized=.  Mark HRTF or otherwise positioned assets =pre-spatialized= so runtime balance does not position them again.

** Registered Packs

| Identifier | Kind | Parent | Profiles | Spatial | Assets | Directory | Intent |
|------------+------+--------+----------+---------+--------+-----------+--------|
| =3d= | =sound= | - | =legacy-complete= | =pre-spatialized= | =55= | sounds/packs/3d | HRTF-generated spatial auditory cues |
| =chimes= | =sound= | - | =legacy-complete= | =neutral= | =56= | sounds/packs/chimes | Short chime-based auditory cues |
| =prompts= | =prompt= | - | - | =neutral= | =17= | sounds/prompts | Theme-independent prompts |

** Registered Cues

| Identifier | Kind | Owner | Fallback | Intent |
|------------+------+-------+----------+--------|
| =alarm= | =cue= | =core= | - | An urgent alarm |
| =alert-user= | =cue= | =core= | - | Immediate attention is required |
| =ask-question= | =cue= | =core= | - | A question is awaiting an answer |
| =ask-short-question= | =cue= | =core= | - | A short question is awaiting an answer |
| =battery-low= | =prompt= | =core= | - | Battery charge is low |
| =button= | =cue= | =core= | - | Generic interaction feedback |
| =center= | =cue= | =core= | - | A spatial or directional value is centered |
| =char= | =cue= | =core= | - | A character-level operation occurred |
| =chime-start= | =prompt= | =core= | - | A chime service started |
| =close-object= | =cue= | =core= | - | An object, view, or container was closed |
| =complete= | =cue= | =core= | - | A completion was accepted or completed |
| =delete-object= | =cue= | =core= | - | An object was deleted |
| =deselect-object= | =cue= | =core= | - | A mark or selection was removed |
| =desktop-login= | =prompt= | =core= | - | A desktop session login completed |
| =desktop-logout= | =prompt= | =core= | - | A desktop session logout completed |
| =dialog-question= | =prompt= | =core= | - | A desktop dialog is asking a question |
| =doc= | =cue= | =core= | - | Documentation content was reached |
| =ellipses= | =cue= | =core= | - | Content is hidden, omitted, or abbreviated |
| =emacspeak= | =prompt= | =core= | - | The Emacspeak product identity |
| =emacsvox= | =compatibility= | =core= | =startup= | Legacy Emacsvox identity cue |
| =fill-object= | =cue= | =core= | - | An object was filled, formatted, or reflowed |
| =help= | =cue= | =core= | - | Help content became available |
| =item= | =cue= | =core= | - | An item or candidate was reached |
| =key= | =cue= | =core= | - | A key or key binding was presented |
| =large-movement= | =cue= | =core= | - | Navigation moved across a structural boundary |
| =launch-wm= | =prompt= | =core= | - | A window manager is starting |
| =left= | =cue= | =core= | - | Movement or placement is toward the left |
| =locking-up= | =prompt= | =core= | - | The desktop session is being locked |
| =mark-object= | =cue= | =core= | - | An object was marked or selected |
| =modified-object= | =cue= | =core= | - | An object has unsaved modifications |
| =more= | =cue= | =core= | - | Additional content is available |
| =n-answer= | =cue= | =core= | - | The letter N answer was selected |
| =network-down= | =cue= | =core= | - | Network connectivity became unavailable |
| =network-up= | =cue= | =core= | - | Network connectivity became available |
| =new-mail= | =cue= | =core= | - | New mail arrived or an unread message was reached |
| =news= | =cue= | =core= | - | News or release information became available |
| =no-answer= | =cue= | =core= | - | A negative answer was selected |
| =off= | =cue= | =core= | - | A setting or state was turned off |
| =on= | =cue= | =core= | - | A setting or state was turned on |
| =open-object= | =cue= | =core= | - | An object, view, or container was opened |
| =paragraph= | =cue= | =core= | - | A paragraph boundary or operation was reached |
| =process-active= | =cue= | =core= | - | A process remains active |
| =progress= | =cue= | =core= | - | An operation is making progress |
| =pwd= | =prompt= | =core= | - | A password is requested |
| =repeat-active= | =cue= | =core= | - | Repeated activity remains active |
| =repeat-end= | =cue= | =core= | - | Repeated activity ended |
| =repeat-start= | =cue= | =core= | - | Repeated activity started |
| =repeat-stop= | =compatibility= | =core= | =repeat-end= | Legacy repeated-activity completion cue |
| =resume= | =prompt= | =core= | - | The system or session resumed |
| =right= | =cue= | =core= | - | Movement or placement is toward the right |
| =save-object= | =cue= | =core= | - | An object was saved |
| =scroll= | =cue= | =core= | - | A view was scrolled |
| =search-hit= | =cue= | =core= | - | A search found a match |
| =search-miss= | =cue= | =core= | - | A search did not find a match |
| =section= | =cue= | =core= | - | A document section was reached |
| =select-object= | =cue= | =core= | - | An object became current or selected |
| =service-login= | =prompt= | =core= | - | A service login completed |
| =service-logout= | =prompt= | =core= | - | A service logout completed |
| =shutdown= | =cue= | =core= | =close-object= | A game or activity ended |
| =startup= | =prompt= | =core= | - | Emacsvox is ready or identified |
| =success= | =prompt= | =core= | - | An external operation succeeded |
| =task-done= | =cue= | =core= | - | A task completed successfully |
| =tick-tick= | =cue= | =core= | - | A short repeated clock tick |
| =time= | =cue= | =core= | - | Time information was presented |
| =tock-tock= | =cue= | =core= | - | A contrasting repeated clock tick |
| =tvr-emacs= | =prompt= | =core= | - | A personalized Emacs identity prompt |
| =unlocked= | =prompt= | =core= | - | The desktop session was unlocked |
| =unmark-object= | =compatibility= | =core= | =deselect-object= | Legacy cleared-selection cue |
| =unmodified-object= | =cue= | =core= | - | An object has no unsaved modifications |
| =voice-mail= | =cue= | =core= | - | A voice message arrived or was reached |
| =waking-up= | =prompt= | =core= | - | Emacsvox speech is becoming ready |
| =warn-user= | =cue= | =core= | - | A warning requires attention |
| =window-resize= | =cue= | =core= | - | A window or pane was resized |
| =y-answer= | =cue= | =core= | - | The letter Y answer was selected |
| =yank-object= | =cue= | =core= | - | Content was inserted from saved text |
| =yes-answer= | =cue= | =core= | - | An affirmative answer was selected |

* Voice Palette Author Reference

A palette maps stable scheme voice names to either device-independent Emacsvox personality symbols or declarative ACSS style data.  Schemes should name palette entries such as =bolden= instead of backend commands.  A palette may inherit another palette and override selected names.  Raw ACSS remains valid for a rule that deliberately needs a generated voice.

The ACSS =:family= dimension selects a base voice.  =male=, =female=, and =child= are portable requests: they resolve again through the active speech adapter whenever the style is compiled.  Adapter-specific names such as =outloud-v2= or =betty= request one exact synth voice.  If that exact voice is unavailable after changing synth, Emacsvox applies the new adapter default and reports an =unavailable-voice-family= degradation; it does not silently reinterpret the identifier.

Each adapter publishes whether family selection is enumerated, free-form, or unsupported, together with available families, portable traits, supported normalized dimensions, and parameter ranges.  Outloud exposes its eight Eloquence presets, DECtalk exposes its nine built-ins, Mac and SwiftMac accept installed voice names, and eSpeak currently performs voice selection through its language interface rather than inline ACSS.  Catalogues are static today; the function-valued capability interface allows later discovery when a speech-server response channel exists.

Numeric ACSS dimensions remain normalized from zero through nine and are applied only when explicitly requested.  For Eloquence and DECtalk non-default base voices, current numeric overrides reuse the established Paul calibration tables; omitted dimensions preserve the native preset's own characteristics.

** acss-default

Existing device-independent ACSS personalities

| Scheme Voice | Personality |
|--------------+-------------|
| =animate= | =voice-animate= |
| =animate-extra= | =voice-animate-extra= |
| =animate-medium= | =voice-animate-medium= |
| =annotate= | =voice-annotate= |
| =bolden= | =voice-bolden= |
| =bolden-and-animate= | =voice-bolden-and-animate= |
| =bolden-extra= | =voice-bolden-extra= |
| =bolden-medium= | =voice-bolden-medium= |
| =brighten= | =voice-brighten= |
| =brighten-extra= | =voice-brighten-extra= |
| =brighten-medium= | =voice-brighten-medium= |
| =indent= | =voice-indent= |
| =lighten= | =voice-lighten= |
| =lighten-extra= | =voice-lighten-extra= |
| =lighten-medium= | =voice-lighten-medium= |
| =monotone= | =voice-monotone= |
| =monotone-extra= | =voice-monotone-extra= |
| =monotone-medium= | =voice-monotone-medium= |
| =overlay-0= | =voice-overlay-0= |
| =overlay-1= | =voice-overlay-1= |
| =overlay-2= | =voice-overlay-2= |
| =overlay-3= | =voice-overlay-3= |
| =smoothen= | =voice-smoothen= |
| =smoothen-extra= | =voice-smoothen-extra= |
| =smoothen-medium= | =voice-smoothen-medium= |

* Compatibility, Migration, and Rollout

Existing direct =emacsvox-icon= calls, queued =auditory-icon= properties, =personality= properties, face-to-voice mappings, sound directories, and backend choices remain supported inputs.  They are compatibility presentation hints, not semantic identity.  If no semantic rule replaces or clears an existing voice or cue, the old presentation remains.

To migrate a direct icon path, first characterize its text, cue, and order. Register or reuse the intent, then pass explicit text and the preserved cue to one =emacsvox-aural-submit= call.  Express the cue as an ordered =emacsvox-aural-compatibility-icon= action rather than calling =emacsvox-icon= beside the submission.  Legacy cue remaps and suppression rules that must affect this adapter should select =:legacy-cue= explicitly; broader semantic rules remain in the one object-level resolution.  Keep a cue-only or implicit-content path on the legacy adapter until it can be represented without dropping tones, indentation, overlays, notification routing, or other established behavior.  Do not replace cue symbols with file paths and do not queue unresolved semantic names.

To migrate styled text, keep existing =personality= or face properties while adding semantic fact properties.  A semantic scheme can then override the voice for one module, mode, derived mode, or buffer.  Voice Lock controls only the legacy personality voice fallback; it does not disable semantic rules or explicit face rules.  A =:legacy-face= compatibility selector can deliberately match one named visual face when no semantic intent is available.  It can add before or after cues and select a palette voice, and can be scoped by module, mode, occasion, or buffer like any other rule.  This is a presentation fallback, not semantic identity.  Users can disable every explicit face rule with =emacsvox-aural-face-presentation-enabled= or its spoken toggle while leaving semantic presentation and Voice Lock unchanged.

#+begin_src emacs-lisp
(:id warning-face-presentation
 :match (:legacy-face font-lock-warning-face
         :mode emacs-lisp-mode :occasion navigation)
 :render
 (:before
  (:append ((:id warning-cue :kind cue :cue warn-user)))
  :content (:voice bolden)))
#+end_src

A text run may carry an ordered list of named faces.  Every matching face rule participates in normal rule composition, so append or prepend operations can accumulate cues.  The first, strongest Emacs face wins a tie between face-only scalar settings such as content voice.  Semantic identity remains stronger within the same rule origin, while stronger user, session, and buffer origins can intentionally override module defaults.  Source overlay faces are snapshotted strongest-first before copying, followed by explicit =face= and =font-lock-face= text names.  String face names are normalized to their existing face symbols, and anonymous faces contribute only explicit named =:inherit= values.  Module speech paths should copy styled ranges with =emacsvox-aural-source-substring= so heard output and point explanation share this snapshot.

Agent Shell, Markdown, Org, Notmuch, Gnus, Dired, and Magit now attach registered conversation, document, organizer, message, filesystem, and version-control facts around their established feedback.  Their speech and cue order remains unchanged unless a scheme or fragment matches the new facts.  Optional fragments demonstrate customization without requiring the corresponding third-party package at startup.

Rollout is deliberately staged.  The default scheme is compatibility preserving, the Org variants are selectable examples, and integrations move in small tested batches.  Critical alerts currently follow the same explicit suppression contract as other actions; deployments that require a mandatory alternative should enforce that policy in a site scheme until a shared critical-alert contract is registered.

* Maintenance

Run =make aural-reference= after changing a registry or this generator.  Run =make aural-audit= to validate registry cross-references, every registered pack and built-in scheme, literal cue calls, voice palettes, semantic icon boundaries in migrated modules, and this generated file.  =utils/count-icons.pl= remains a historical text counter; the registry-aware audit is authoritative.
